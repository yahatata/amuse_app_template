---
description: Flutter のローディング表示・操作ロック（CPI / 3類型）
globs: lib/**/*.dart
alwaysApply: false
---

# Flutter — ローディング表示

**正本:** `docs/共通化/flutter/04_仕様書/ローディング表示/ローディング表示.md`  
**改修単位・To-be:** `docs/共通化/flutter/05_changeSpec/ローディング表示.md`

実装前に処理を **3 類型**に分類する: **画面表示時の読込** / **ユーザー操作の読取系** / **ユーザー操作の更新系**。同一 Widget 内でも類型ごとに扱いを分ける。

## 待機表示（CPI）

- 待機を出す場合は **`CircularProgressIndicator` に統一**（仕様上「CPI」）。
- **読込・読取**: 骨格や AppBar を残し**主領域**に CPI 可。画面全体 CPI も可。
- **軽微で無反応に見えないなら** CPI なしの現状維持を認める（無理に足さない）。

## ロック（操作不可の範囲）

| 類型 | ロック |
|------|--------|
| 画面読込 | **原則なし** |
| 読取系 | **原則なし**（更新系ほど強くしない） |
| 更新系 | **ローディング + ロックを原則必須** |

## 更新系（最重要）

- **ボタン内スピナーのみ**を標準にしない。処理文脈に合わせ**表示単位**（本文・ダイアログ全体・タブ等）で CPI。
- ロックは**表示単位以上**に広くてよい。少なくとも **同一処理の再実行・状態不整合を招く操作・処理中 UI の回避（閉じる・戻る・別導線）**を防ぐ。
- **順序:** 処理終了 → **ローディング解除・ロック解除** → SnackBar 等の**結果表示**。結果表示のためだけにロックを継続しない。
- 成功・失敗の**両方**でローディングを止める。解除は **`finally` 相当**で漏れを防ぐ。

## 非標準（避ける）

- 更新中なのに待機が視覚的に分からない。
- 読込・読取・更新を**同じ強度**で扱うこと。

## サーバ側

- UI ロックは**一次防御**。重複実行の最終防衛は **Functions の冪等性**等で別途確認（仕様 §9）。
