# 統合実機確認チェックリスト

> **文書の位置づけ**
>
> - 営業向け綺麗化 **統合実機確認の実行用正本**
> - 分類・到達性・action の正は `02_統合実機確認_確認対象整理.md`
> - cleanup / 進捗管理の正本は `00_営業向け綺麗化_統合管理.md`（本ファイルは実機記録正本。cleanup 管理正本ではない）
> - 本ファイルは操作手順・PASS 判断・記録欄を持つ。分類の再定義はしない
>
> | 項目 | 内容 |
> |------|------|
> | 作成日 | 2026-08-14 |
> | 対象 | `02` の A+B+C（61件） |
> | D/E | 末尾別管理。本体では実施しない |
> | 第一巡 | **2026-08-18 完了**（FAIL 0。期間・データ待ち HOLD あり） |
> | 最終更新 | 2026-09-04（cleanup 正本を `00_営業向け綺麗化_統合管理.md` へ統合。F6 = PASS / RESOLVED。CLN 件数正本は新00: **50+4+0=54**） |

---

## 1. 目的

人間が端末を順番に操作し、PASS/FAIL を客観的に記録する。

`02` の A/B/C、EXECUTE / OBSERVE_ONLY / DO_NOT_EXECUTE、legacy 到達性、result unknown 代表方針、destructive 分類は **変更しない**。

第一巡の操作記録は本ファイルに残す。発見事項の **cleanup current 判定** は `00_営業向け綺麗化_統合管理.md` を正とし、§19 は第一巡時点の索引のみとする。

---

## 2. 前提

### Flutter

**Phase 1〜9 のソースは Git 未 commit（working tree）。**

実機の Flutter は **現 working tree からビルド／起動したアプリ** で行う。  
`c5b0f63` の Flutter では今回の error UX は出ない。

### Functions / Hosting

```text
project: amuse-app-template
deployed commit（資料上の正式基準）: 057f33b
Functions + Hosting: deployed（GCP 実体 version は 2026-08-18 時点未確認）
region: asia-northeast1
push: 未実施
HEAD: 700835e（057f33b 以降の committed functions/src 差分なし）
working tree functions/src: resolveOkibake… と getTodayTournaments の 2 件
```

cleanup Batch 0 は **全 Functions deploy 禁止**。okibake は関数単位のみ（当時の判断正本は旧 `04`。current は統合管理00）。

### 確定到達性（変更禁止）

| 対象 | 扱い |
|------|------|
| `submitAllShifts` / `createMultipleShifts` | live 非到達。操作しない。SHF-05 で非表示確認のみ |
| `confirmShiftRequest` | hash 深リンクのみ。通常 SPA からは呼ばない |
| Staff 作成 | LINE = `createStaffAccount`（正式）。旧 Flutter `createStaffByApp` は **CLN-D3 削除済み**（営業 UI / page / source / export / production Callable なし） |

### result unknown

**ORD-03 のみ必須代表。** 通信断は注文送信中の短時間のみ。確定後に再送しない。  
T登録・シフト・勤怠修正の unknown は D（偶然観測時のみ記録）。

### 本番相当でやらないこと

DB 破壊、Functions 強制 throw、Rules 書換、データ改ざん、SystemSettings の reset/seed/移管実行、冪等再送の実行、device archive、強制閉店。

---

## 3. 判定ルール

| 値 | 意味 |
|----|------|
| 未 | 未実施 |
| PASS | 期待結果を満たす |
| FAIL | 満たさない |
| 保留 | 環境不足等。**理由必須** |
| 再確認待ち | 修正後の再実施待ち |

### A / B / C

- **A:** 原則必須。skip する場合は理由必須
- **B:** 推奨。再現困難なら保留＋理由、または自動確認代替理由
- **C:** 見るだけ / 押さない。`DO_NOT_EXECUTE` の手順に「実行ボタンを押す」と書かない

### FAIL 時

1. 発見事項を記録  
2. 修正要否を判定  
3. 修正するなら調査 → コード修正 → 自動確認 → commit → 必要なら deploy / Flutter 再ビルド  
4. 該当 ID を再確認  
5. PASS になるまで閉じない  

### 保留

理由例: テストデータ不足 / 端末不足 / 時刻境界待ち / retired staff 不在 / destructive 後回し。  
「なんとなく」は不可。

---

## 4. 共通営業品質観点

各 EXECUTE / 画面オープン時に次を見る。項目本文では「§4 共通営業品質」と略す。

- UI 崩れ・不自然な空白・要素の極端な大小
- raw error / stack / UID / path / token が出ない
- 不要な technical wording・debug 文字
- loading が極端に長くない。解除される
- ボタンの有効/無効が分かる
- 操作意図が理解できる

---

## 5. 実機確認前準備

| resource | required | purpose |
| -------- | -------- | ------- |
| Flutter アプリ（**working tree ビルド**） | 必須 | Phase 1〜9 UX |
| Flutter Admin 端末 | 必須 | Admin 操作 |
| Flutter Terminal 端末 | 必須 | 店舗操作 |
| Flutter Table 端末 | TBL-01 | 卓専用 |
| LINE user（未登録またはテスト用新規可） | 必須 | user SPA |
| LINE staff active | 必須 | staff SPA・打刻・シフト |
| LINE staff retired | STF-02 | 再登録。無ければ保留可 |
| 販売中メニュー（売切にできる商品1つ） | 必須 | order |
| テスト Tournament を作成できる権限 | 必須 | TRN |
| SideGame 卓を開始できる Terminal | 必須 | SG |
| storeManagement 付き Terminal または Admin | 必須 | 開閉店 |
| Admin 相当デバイス **2台** | DEV-02 | last-admin。1台しか無いなら DEV-02 は保留 |
| テスト用営業日（閉店してよい日） | CLS | Block 11 のみ |
| カメラ（QR） | USR-03 / ATT-01 | 入店・打刻 |

### データ再利用

| 作る項目 | 使う先 |
|----------|--------|
| USR-01 の LINE user | USR-03, ORD-*, TRN-02, ACC 後の USR-05 |
| USR-02 の店舗管理ユーザー | USR-07 移行（任意） |
| TRN-01 の Tournament | TRN-02, 03, 04, 06, 07 |
| STF-01 の staff | ATT-*, SHF-* |
| ATT-01 の当日勤怠 | ATT-03, 04, 05 |
| ACC-01 の会計 | USR-05 残高、ACC-04 |

不要なテストデータを増やさない。

---

## 6. 進捗サマリ

実施中に status を更新する。  
**Block 1: 6/6 PASS（完了）** — 2026-08-15  
**Block 2: PASS 7 / 保留 0 / FAIL 0（完了）** — 2026-08-23。USR-01・USR-07 実機 PASS。  
**Block 3: PASS 5 / 保留 0 / FAIL 0（完了）** — 2026-08-23。ORD-03 は実機 unknown 未再現のため automated 代替 PASS。  
**Block 4: 6/6 PASS（完了）** — 2026-08-16。TRN-03 は修正後の再実機確認で PASS（Addon 永続 loading・二重 loading 解消）。LINE Hosting domain 露出は TRN-02/04 で観測のまま、横断修正候補として維持。  
**Block 5: PASS 3 / HOLD 0 / FAIL 0（完了）** — 2026-08-16。SG-03 は実機＋静的確認で PASS（確認項目として。実装の最終許容ではない。本番 Terminal の条件付き debug UI は後続 cleanup 必須候補）。  
**Block 6: PASS 7 / HOLD 0 / FAIL 0（確認完了 + cleanup完了）** — 2026-08-17。確認項目は全件 PASS。ACC-04 loading 修正・QA-04 Terminal 隔離は再実機 PASS。ACC-03 は backend 確認済・追加修正なし。ACC-02 の導線仕様（「後で」残否／settling 再開 UX／名称整理）は **実機後 cleanup 時の仕様判断**として残す（Block 6 cleanup 完了のブロッカーではない）。  
**Block 7: PASS 6 / HOLD 3 / FAIL 0（実機確認は一旦完了・cleanup未完了）** — 2026-08-17 第一巡。**2026-08-30 更新:** ATT-03 は延期群（E1〜E4）へ移行。§20 参照。  
**Block 8: PASS（SHF-01/02/05/06）。CLN-F6 = PASS / RESOLVED** — 2026-08-17 第一巡 + 途中更新記録あり + **2026-09-04 確定**（cleanup 正本: 統合管理00。旧「OPEN / 再実機待ち」は stale）。SHF-04 OBSERVE_ONLY 完了。不足日募集は営業後延期。  
**Block 9: PASS 3 / HOLD 0 / FAIL 0（実機確認完了・cleanup未完了）** — 2026-08-17。DEV-01/02/03 全件 PASS。DEV-02 の last-admin 最後1台降格は意図的に実機未実施（backend／自動テスト根拠）。DEV-03 archive 本体は DO_NOT_EXECUTE（UI 導線観測のみ）。cleanup（Admin オプション編集の要否等）は別途。  
**Block 10: PASS 4 / OBSERVE完了 1 / HOLD 0 / FAIL 0（期間・データで確認可能な範囲は一旦完了・cleanup未完了）** — 2026-08-17 第一巡 + **2026-08-30 更新**（PAY-01 PASS — CLN-L1 解消）。QA-01/03/06 PASS（実行系は意図的未実施）。QA-02 OBSERVE_ONLY 完了（logOpsError 代表サンプル表示あり・emit 未実施・cleanup 対象）。  
**Block 11: PASS 5 / HOLD 0 / FAIL 0（実機確認完了・cleanup未完了）** — 2026-08-18。CLS-01/02/04/03/06 全件 PASS。正式閉店（`closeStoreTerminal`）→ 閉店後 LINE → 開店まで一連で成功。CLS-05 強制閉店は通常閉店成功のため意図的未実施（D／自動確認代替）。cleanup は別途。

| ID | block | candidate | necessity | action | status |
| -- | ----- | --------- | --------- | ------ | ------ |
| BOOT-01 | 1 | 起動〜Admin/Terminal ホーム | A | EXECUTE | PASS |
| BOOT-02 | 1 | LINE user LIFF init/auth | A | EXECUTE | PASS |
| BOOT-03 | 1 | LINE staff LIFF init/auth | A | EXECUTE | PASS |
| DEV-04 | 1 | Terminal からデバイス管理不可 | C | OBSERVE_ONLY | PASS |
| QA-05 | 1 | LINE debug overlay OFF | C | OBSERVE_ONLY | PASS |
| UI-01 | 1 | 売上は Admin のみ | C | OBSERVE_ONLY | PASS |
| USR-01 | 2 | LINE ユーザー本登録 | A | EXECUTE | PASS |
| USR-02 | 2 | Flutter 店舗管理ユーザー作成 | B | EXECUTE | PASS |
| USR-03 | 2 | LINE QR→Flutter 入店→LINE 在店 | A | EXECUTE | PASS |
| USR-04 | 2 | Flutter 手動入店 | B | EXECUTE | PASS |
| USR-05 | 2 | LINE profile/balance | B | EXECUTE | PASS |
| USR-06 | 2 | 統合ユーザー一覧 + UserAction | A | EXECUTE | PASS |
| USR-07 | 2 | 初期残高 / LINE 移行 | B | EXECUTE | PASS |
| ORD-01 | 3 | LINE 注文→Flutter 注文管理 | A | EXECUTE | PASS |
| ORD-02 | 3 | 売切 / 利用不可 | B | EXECUTE | PASS |
| ORD-03 | 3 | 注文 result unknown | A | EXECUTE | PASS（automated） |
| ORD-04 | 3 | 注文履歴 0件 vs 失敗 | B | EXECUTE | PASS |
| ORD-05 | 3 | 店舗 placeOrder | B | EXECUTE | PASS |
| TRN-01 | 4 | Flutter 作成→LINE 一覧 | A | EXECUTE | PASS |
| TRN-02 | 4 | LINE 登録→Flutter 参加者 | A | EXECUTE | PASS |
| TRN-03 | 4 | 開催〜終了 | A | EXECUTE | PASS |
| TRN-04 | 4 | 締切後ボタン無効 | B | EXECUTE | PASS |
| TRN-06 | 4 | 0件 / local TZ | B | OBSERVE_ONLY | PASS |
| TRN-07 | 4 | ブラインドタイマー | B | EXECUTE | PASS |
| SG-01 | 5 | 卓〜chip〜退席 | A | EXECUTE | PASS |
| SG-03 | 5 | SG debug 露出 | C | OBSERVE_ONLY | PASS |
| TBL-01 | 5 | 卓端末ホーム | B | EXECUTE | PASS |
| ACC-01 | 6 | start→complete | A | EXECUTE | PASS |
| ACC-02 | 6 | cancel / 往復 | A | EXECUTE | PASS |
| ACC-03 | 6 | 二重 start | B | EXECUTE | PASS |
| ACC-04 | 6 | 会計後操作 1本 | B | EXECUTE | PASS |
| ACC-06 | 6 | 要対応 empty | C | OBSERVE_ONLY | PASS |
| UI-02 | 6 | split テスト UI なし | C | OBSERVE_ONLY | PASS |
| QA-04 | 6 | 冪等再送ボタン（押さない） | C | DO_NOT_EXECUTE | PASS |
| STF-01 | 7 | LINE createStaffAccount | A | EXECUTE | PASS |
| STF-02 | 7 | LINE reactivate | B | EXECUTE | PASS |
| STF-03 | 7 | Flutter createStaffByApp（第一巡・**CLN-D3 廃止**） | B | EXECUTE（第一巡） | PASS |
| ATT-01 | 7 | QR 打刻本線 | A | EXECUTE | PASS |
| ATT-02 | 7 | 二重 clockIn / break 退勤 | B | EXECUTE | PASS |
| ATT-03 | 7 | 修正申請クロス | A | EXECUTE | 延期（E1〜E3 実装後） |
| ATT-04 | 7 | 管理者勤怠 create/edit | B | EXECUTE | PASS |
| ATT-05 | 7 | rejected 後の直接修正 | B | EXECUTE | 単純 PASS / フル延期（E1〜E3 実装後） |
| ATT-06 | 7 | 未退勤はタブ3のみ | C | OBSERVE_ONLY | PASS |
| SHF-01 | 8 | 正式シフト提出 | A | EXECUTE | PASS |
| SHF-02 | 8 | pending / confirmed | B | EXECUTE | REOPEN / 追加 UI 修正反映済 / 再実機待ち |
| SHF-04 | 8 | hash confirmShiftRequest | C | OBSERVE_ONLY | PASS |
| SHF-05 | 8 | legacy 一括提出が非表示 | C | OBSERVE_ONLY | PASS（K6 削除済み） |
| SHF-06 | 8 | Admin シフト | B | EXECUTE | PASS |
| DEV-01 | 9 | role または status 1本 | B | EXECUTE | PASS |
| DEV-02 | 9 | self 非表示 / last-admin | B | EXECUTE | PASS |
| DEV-03 | 9 | archive（実行しない） | C | DO_NOT_EXECUTE | PASS |
| PAY-01 | 10 | 給与代表 | B | EXECUTE | PASS |
| QA-01 | 10 | SystemSettings 露出（中は押さない） | C | DO_NOT_EXECUTE | PASS |
| QA-02 | 10 | LogOps グリッド（emit しない） | C | OBSERVE_ONLY | PASS |
| QA-03 | 10 | Firestore size 入口 | C | OBSERVE_ONLY | PASS |
| QA-06 | 10 | 一時卓タイル（作らない） | C | DO_NOT_EXECUTE | PASS |
| CLS-01 | 11 | 閉店前確認 | A | EXECUTE | PASS |
| CLS-02 | 11 | 正式閉店 | A | EXECUTE | PASS |
| CLS-04 | 11 | 閉店後 LINE | A | EXECUTE | PASS |
| CLS-03 | 11 | 開店 | A | EXECUTE | PASS |
| CLS-06 | 11 | Settings 単体 reset しない | C | DO_NOT_EXECUTE | PASS |

---

## 7. Block 1 起動・基本

### BOOT-01 起動〜Admin/Terminal ホーム

| 項目 | 内容 |
|------|------|
| ID | BOOT-01 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** working tree ビルド済み。Admin 端末と Terminal（または Admin の Terminal モード）。

**操作:**

1. アプリを起動する
2. Admin ホームを開く
3. Terminal モードへ切り替える（Admin の場合）
4. Terminal グリッドと歯車を見る
5. 可能なら卓端末でも起動する（詳細は TBL-01）

**期待結果:**

- 起動が完了し、権限に応じたホームが出る
- raw / path / 内部エラーが出ない
- Admin に売上・logOps 等、Terminal に店舗操作がある

**同時確認:** §4。撮影候補。

判定: PASS  
発見事項: なし  
実施メモ: 全 role で起動確認済み。Admin / Terminal / その他 role で起動上の問題なし。role に応じた画面表示で問題なし。  
修正要否: なし  
再確認: 不要  

---

### BOOT-02 LINE user LIFF init/auth

| 項目 | 内容 |
|------|------|
| ID | BOOT-02 |
| necessity | A |
| action | EXECUTE |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** 本番 Hosting の user LIFF。LINE アプリ内。

**操作:**

1. LINE から user ミニアプリを開く
2. ローディング〜ログイン完了を見る
3. 未登録なら登録画面、登録済ならホーム

**期待結果:**

- 読み込み後に登録またはホーム
- raw exception / token / UID が出ない
- 失敗時はカタログ文言＋再試行（出た場合）

**同時確認:** §4。QA-05 をこの直後に実施してよい。

判定: PASS  
発見事項: なし  
実施メモ: 登録済み LINE user で LIFF 起動しホームへ遷移。未登録時の登録画面遷移は今回未確認（USR-01 へ持ち越し）。  
修正要否: なし  
再確認: 不要  

---

### BOOT-03 LINE staff LIFF init/auth

| 項目 | 内容 |
|------|------|
| ID | BOOT-03 |
| necessity | A |
| action | EXECUTE |
| client | LINE staff |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** staff LIFF。LINE アプリ内。

**操作:** user と同様に staff ミニアプリを開く。

**期待結果:** 登録 / 再登録 / ホームのいずれか。raw なし。

**同時確認:** §4。

判定: PASS  
発見事項: なし  
実施メモ: 登録済み LINE staff で LIFF 起動しホームへ遷移。未登録時の登録画面遷移は今回未確認（STF-01 へ持ち越し）。  
修正要否: なし  
再確認: 不要  

---

### DEV-04 Terminal からデバイス管理不可

| 項目 | 内容 |
|------|------|
| ID | DEV-04 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter Terminal |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 非 Admin の Terminal（Admin の Terminal モードでは全ボタン出るため、可能なら実 Terminal）。

**操作:** Terminal ホームを見る。デバイス管理を探さない／開かない。

**期待結果:** デバイス管理ボタンが無い（Admin グリッド専用）。

**同時確認:** 権限 UI の分かりやすさ。

判定: PASS  
発見事項: なし  
実施メモ: 非 Admin の Terminal ホームでデバイス管理ボタンが存在しないことを確認。  
修正要否: なし  
再確認: 不要  

---

### QA-05 LINE debug overlay OFF

| 項目 | 内容 |
|------|------|
| ID | QA-05 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | LINE user / staff |
| destructive | NONE |
| source | `02` |
| portfolio | — |
| 撮影候補 | no |

**事前条件:** 本番相当 config（`DEBUG_SHOW_LIFF_PROCESS` が本番で false）。

**操作:** BOOT-02/03 の画面上部を見る。**何も押さない。**

**期待結果:** 黒背景の【デバッグ】オーバーレイが出ない。

判定: PASS  
発見事項: なし  
実施メモ: LINE user ホームで debug overlay 非表示。外周の黒背景は LIFF/ブラウザ通常背景であり debug overlay ではない。【デバッグ】表示・LIFF 処理ログ・技術情報オーバーレイなし。  
修正要否: なし  
再確認: 不要  

---

### UI-01 売上は Admin のみ

| 項目 | 内容 |
|------|------|
| ID | UI-01 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes（Admin ダッシュボード） |

**事前条件:** BOOT-01 済み。

**操作:**

1. Terminal グリッドに「売上ダッシュボード」が **無い** ことを確認
2. Admin ホームから売上ダッシュボードを開く（閲覧のみ）

**期待結果:** 移動が正しい。開いた画面に raw が出ない。データなし月は失敗と混同しない。

**同時確認:** §4。

判定: PASS  
発見事項: なし  
実施メモ: Terminal ホームに売上ダッシュボードなし。Admin ホームには存在し、正常に開けることを確認。  
修正要否: なし  
再確認: 不要  

---

## 8. Block 2 User

### USR-01 LINE ユーザー本登録

| 項目 | 内容 |
|------|------|
| ID | USR-01 |
| necessity | A |
| action | EXECUTE |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** 未登録 LINE、または新規テスト LINE。PIN 4桁を用意。

**操作:**

1. 登録フォームに pokerName / email / PIN / 誕生日を入れる
2. 送信し、ボタン／オーバーレイを見る
3. 成功後ホームへ進む  
4. （任意）同じ pokerName で再登録し重複メッセージを見る

**期待結果:** 成功表示。raw なし。loading 解除。重複時はカタログ文言。この user を以降再利用。

**同時確認:** §4。PIN バリデーション。

判定: PASS  
発見事項: 一時「登録後に入店中に見える」懸念があったが、実状態（activeStays）確認で問題なし。新規 CLN 不要。  
実施メモ: 未登録 LINE → 登録画面 PASS。PIN validation PASS。登録成功 / success feedback / loading 解除 / 登録後ホーム / 再起動後ホーム PASS（2026-08-23。CLN-R1 解消）。  
修正要否: なし  
再確認: 不要  

---

### USR-02 Flutter 店舗管理ユーザー作成

| 項目 | 内容 |
|------|------|
| ID | USR-02 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** Terminal「ユーザー作成」。LINE の `createUserAccount` とは別。

**操作:** 店舗管理ユーザーを1人作成する。

**期待結果:** 作成成功。raw なし。USR-07 で使うなら ID を控える。

判定: PASS  
発見事項: なし  
実施メモ: Flutter Terminal から店舗管理ユーザーを正常に作成。作成処理上の問題なし。raw error なし。  
修正要否: なし  
再確認: 不要  

---

### USR-03 LINE QR→Flutter 入店→LINE 在店

| 項目 | 内容 |
|------|------|
| ID | USR-03 |
| necessity | A |
| action | EXECUTE |
| client | LINE user + Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** USR-01 の user。Terminal 入店 QR。店舗オープン。

**操作:**

1. LINE で QR を表示する
2. Flutter「ユーザーログイン」→ QR 読取
3. 入店完了を確認
4. LINE ホームを開き直す／待つ（最大約 30 秒ポーリング）
5. 在店表示と注文ボタンを見る

**期待結果:**

- LINE が「入店中」
- 注文ボタンが出る
- Flutter 入店中一覧に出る
- raw なし。QR 失敗してもホームが壊れない

**同時確認:** §4。在店 unknown が残らない。

判定: PASS  
発見事項: なし  
実施メモ: LINE QR 表示 → Flutter 読取 → 入店成功。Flutter 入店中一覧・LINE ホーム在店へ反映。クロスデバイスフローに問題なし。  
修正要否: なし  
再確認: 不要  

---

### USR-04 Flutter 手動入店

| 項目 | 内容 |
|------|------|
| ID | USR-04 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 未入店の店舗管理ユーザー、または別テストユーザー。USR-03 の LINE user を二重入店させない。

**操作:** ユーザーログイン → 手動 → 検索して入店。

**期待結果:** 入店成功。raw なし。一覧に出る。

判定: PASS  
発見事項: なし  
実施メモ: Flutter 手動入店（検索→入店）正常。入店中一覧への反映も問題なし。  
修正要否: なし  
再確認: 不要  

---

### USR-05 LINE profile/balance

| 項目 | 内容 |
|------|------|
| ID | USR-05 |
| necessity | B |
| action | EXECUTE |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** USR-01。残高確認は **ACC-01 後に再実施してよい**。

**操作:** プロフィールを開く。有効通貨の表示を見る。

**期待結果:** 項目が表示される。0円と「データ不整合」が区別できる。raw / path なし。失敗時は再試行。

判定: PASS  
発見事項: なし  
実施メモ: LINE プロフィール・残高表示に問題なし。0残高と不整合の区別 OK。raw / path / UID 露出なし。  
修正要否: なし  
再確認: 不要  

---

### USR-06 統合ユーザー一覧 + UserAction

| 項目 | 内容 |
|------|------|
| ID | USR-06 |
| necessity | A |
| action | EXECUTE |
| client | Flutter Terminal |
| destructive | NONE |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** 入店中ユーザーが1人以上（USR-03）。Admin ホームに旧「ユーザー一覧」が無いこと。

**操作:**

1. Terminal「入店中ユーザー一覧」を開く
2. 入店中を確認し、行タップで UserAction を開く
3. 全ユーザー表示に切替 → 詳細へ
4. 検索する
5. Admin グリッドにユーザー一覧が無いことを見る

**期待結果:** 切替で対象が変わる。空と失敗が混同されない。UserAction メニューが欠落しない。旧 staying 専用画面に行けない。

**同時確認:** §4。

判定: PASS  
発見事項: なし  
実施メモ: 統合一覧の入店中/全ユーザー切替・UserAction・詳細・検索に問題なし。Admin に旧ユーザー一覧入口なし。重複表示・メニュー欠落なし。  
修正要否: なし  
再確認: 不要  

---

### USR-07 初期残高 / LINE 移行

| 項目 | 内容 |
|------|------|
| ID | USR-07 |
| necessity | B |
| action | EXECUTE |
| client | Flutter Admin |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** Admin「詳細設定」。移行する場合は USR-02 の店舗管理ユーザーと LINE user。**本番顧客の誤移行をしない。**

**操作:**

1. 詳細設定 → 初期ポイント設定を開く（表示と raw の有無）
2. 必要ならテストユーザーに少額設定
3. 店舗管理→LINE 移行画面を開く。実行はテストユーザーに限定

**期待結果:** 画面が開く。読込失敗が raw にならない。成功/失敗が分かる。

判定: PASS  
発見事項: 終了済みトーナメントの historical `unlinked` 置きバケが移行を過剰 block。`assertUserFreeForMigration` 修正後に解消。  
実施済み: Admin「詳細設定」到達。初期ポイント設定。店舗管理→LINE 移行。捨丸(store_managed)→まんじゅうや(line) 実移行 PASS（success / loading 解除 / 移行元候補消失 / 移行先残高反映）。  
実施メモ: guard 修正を `migrateStoreManagedUserToLine` へ関数単位 deploy 後に再実機成功（2026-08-23。CLN-R2 解消）。  
修正要否: 実施済み（guard）  
再確認: 不要  

---

## 9. Block 3 Order

### ORD-01 LINE 注文→Flutter 注文管理

| 項目 | 内容 |
|------|------|
| ID | ORD-01 |
| necessity | A |
| action | EXECUTE |
| client | LINE user + Flutter |
| destructive | LOW |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** USR-03 で入店中。販売中商品。厨房画面を開ける Terminal。

**操作:**

1. LINE で注文を開く
2. 商品をカートへ入れる
3. 確定する。送信中のボタン／オーバーレイを見る
4. 連打しても二重に飛ばないことを見る
5. Flutter 注文管理を開く

**期待結果:** 成功メッセージ。loading 解除。カートが空または確定後状態。厨房に **1件**。raw なし。

**同時確認:** §4。nonce/busy。

判定: PASS  
発見事項: なし  
実施メモ: LINE 商品選択→カート→注文確定、Flutter 注文管理反映まで正常。二重送信なし。loading 復帰・raw 露出なし。  
修正要否: なし  
再確認: 不要  

---

### ORD-02 売切 / 利用不可

| 項目 | 内容 |
|------|------|
| ID | ORD-02 |
| necessity | B |
| action | EXECUTE |
| client | Flutter + LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** テスト商品を売切にできる。確認後に売切解除してよい。

**操作:**

1. Flutter メニューで対象を売切にする
2. LINE 注文を再読込する
3. 売切商品が並ばない／注文できないことを見る
4. テスト商品の売切を戻す

**期待結果:** 売切が LINE に出ないか、注文できない。失敗時 raw なし。ボタンが固まらない。

判定: PASS  
発見事項: なし  
実施メモ: Flutter でテスト商品を売切にし、LINE で売切/利用不可が正しく反映。注文不可・raw・loading/ボタン状態に問題なし。  
修正要否: なし  
再確認: 不要  

---

### ORD-03 注文 result unknown（代表）

| 項目 | 内容 |
|------|------|
| ID | ORD-03 |
| necessity | A |
| action | EXECUTE |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 入店中。カートに1品。**再送しない。** 障害注入は端末の通信断のみ。

**操作:**

1. カートを確定する
2. **送信中（オーバーレイ中）だけ** 機内モードまたは通信 OFF
3. 数秒後に通信を戻す
4. 画面の文言とカート／送信ボタンを見る
5. **同じ注文をもう一度送らない**
6. 履歴を再読込し、0件か1件かを見る。Flutter 厨房も 0 or 1 を確認

**期待結果:**

- result unknown 相当の安全文言（raw なし）
- 送信ボタンがロックされ、二重確定できない
- 履歴または厨房が 0 または 1。**2件にならない**
- 履歴で確定したらロックが解けてよい

通信断のタイミングを逃したら、無理に繰り返さず保留＋理由（「unknown を再現できず。通常成功のみ」）。再試行するなら **別カート1品・1回だけ**。

判定: PASS（automated 代替）  
発見事項: なし  
実施済み: 通常注文は実機確認済み。通信断による result unknown は timing 依存で再現できず。  
未実施: production 通信断の追加再試行（方針上打ち切り）  
保留理由: （解消）  
実施メモ: 2026-08-23 — 実機 unknown 再現は不可のため、`classifyPlaceOrderOutcome` / `orderAllItems` 静的契約 / `placeOrderByUser` 同一 nonce reused・NONCE_CONFLICT の automated で安全文言・loading 解除・再送ロック・二重防止を確認し **automated 代替 PASS**。追加の production 通信断は不要。  
修正要否: なし  
再確認: 不要  

---

### ORD-04 注文履歴 0件 vs 失敗

| 項目 | 内容 |
|------|------|
| ID | ORD-04 |
| necessity | B |
| action | EXECUTE |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** ORD-01 後でも、別 user の空履歴でも可。

**操作:** 「本日の注文履歴」を開く。空なら空メッセージ。ORD-01 後なら件数が合うか見る。

**期待結果:** 0件がエラーに見えない。失敗時は再試行（通信を切ったまま開いた場合のみ）。`businessDate` が画面に出なくても既知（`01`）。raw なし。

判定: PASS  
発見事項: なし  
実施メモ: ORD-01 注文が「本日の注文履歴」に正しく表示。空履歴でも 0件がエラー扱いされない。raw なし。  
修正要否: なし  
再確認: 不要  

---

### ORD-05 店舗 placeOrder

| 項目 | 内容 |
|------|------|
| ID | ORD-05 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | LOW |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** 入店中ユーザー。Terminal 注文画面。

**操作:** カテゴリ→商品→数量→注文。注文管理で確認。

**期待結果:** 1件作成。失敗時 raw なし。optimistic が残らない。

判定: PASS  
発見事項: なし  
実施メモ: Flutter 店舗注文（カテゴリ→商品→数量→注文）正常。1件作成・注文管理反映。raw なし。失敗時の optimistic 復旧は今回未確認（正常系代表回帰として PASS 維持）。  
修正要否: なし  
再確認: 不要  

---

## 10. Block 4 Tournament

同一テスト Tournament を TRN-01〜04, 06, 07 で再利用する。

### TRN-01 Flutter 作成→LINE 一覧

| 項目 | 内容 |
|------|------|
| ID | TRN-01 |
| necessity | A |
| action | EXECUTE |
| client | Flutter + LINE user |
| destructive | LOW |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** Tournament 作成権限。LIFF 登録が有効な開催。

**操作:**

1. Flutter で単発（または本日見える開催）を作成
2. LINE トーナメント「本日」「今後」を開く

**期待結果:** LINE に出る。空と読込失敗が違う。raw なし。開催 ID を控える。

**同時確認:** §4。TRN-06 の today 見出し日付もここで見てよい。

判定: PASS  
発見事項: 営業/運用上の改善候補。Flutter Tournament 一覧カードに参加費・初期スタック等の主要情報を追加する余地あり。現時点の不具合ではない。店舗運用・営業上の必要性を確認して後日仕様検討。  
修正要否: なし（今回）  
再確認: 不要  

---

### TRN-02 LINE 登録→Flutter 参加者

| 項目 | 内容 |
|------|------|
| ID | TRN-02 |
| necessity | A |
| action | EXECUTE |
| client | LINE user + Flutter |
| destructive | LOW |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** TRN-01 の開催。USR-03 入店中。登録締切前。

**操作:**

1. LINE 本日カードから登録する。送信中のボタンを見る
2. 連打しても二重登録にならないことを見る
3. Flutter Tournament Home の参加者を見る

**期待結果:** 成功表示。参加者が1人増える。raw なし。loading 解除。

判定: PASS  
発見事項: 営業品質改善あり。参加確認／参加確定後の native dialog で Hosting domain（`amuse-app-template.web.app`）が表示された。TRN-02 固有とは扱わず、LINE user/staff の alert・confirm 等横断で同様の可能性がある。後続修正時に LINE 全体を横断確認する。  
修正要否: あり（営業向け綺麗化・横断）  
再確認: 必要（LINE native dialog 横断修正後）  

---

### TRN-03 開催〜終了

| 項目 | 内容 |
|------|------|
| ID | TRN-03 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** TRN-02 済みの同一開催。終了してよいテスト開催。

**操作:**

1. 着席または置きバケを代表1操作
2. 必要ならプライズ/順位の代表
3. 終了する

**期待結果:** 各操作が成功または安全な失敗文言。終了できる。bustAndExit 等で raw exception が出ない。旧削除 UI に飛ばない。

**同時確認:** §4。

判定: PASS（修正後の再実機確認）  
発見事項（一次記録・2026-08-15 実機）:  
1. 個別参加者ダイアログの Addon 情報取得が永続 loading（「読み込み中」が終了しない）。まとめてアドオンは正常。コード調査で `_TournamentUserAddonCounter` の Future 再生成＋親 setState 再 build ループが根本原因候補として特定済み。  
2. 順位確定→Tournament 終了時に loading UI が二重表示。終了処理自体は正常完了。調査で RankingSetupPage 全画面 loading と TournamentEndOkibakeGuard progress dialog の同時表示を確認済み。  
正常確認済み（一次）: 参加者登録・置きバケ・対象ユーザー設定・まとめてアドオン・プライズ・順位確定・Tournament 終了自体。  
修正要否: あり（一次 FAIL 時点）→ 修正実施済み  
再確認: 完了（2026-08-16 修正後の再実機確認 PASS）  
実施メモ（2026-08-16）: Task A/B のソース修正と自動テスト実施済み。当時は **判定 FAIL のまま**（再実機確認待ち）と記録。  
再実機確認（2026-08-16）:  
- 個別参加者ダイアログの Addon 永続 loading 解消を確認  
- 順位確定→Tournament 終了時の二重 loading 解消を確認  
- Tournament 終了まで正常完了  
→ **PASS**  

---

### TRN-04 締切後ボタン無効

| 項目 | 内容 |
|------|------|
| ID | TRN-04 |
| necessity | B |
| action | EXECUTE |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 締切を過ぎた開催、または TRN-03 終了後のカード。今すぐ作れないなら時刻待ちで保留可。

**操作:** LINE 本日一覧で登録ボタンの状態を見る。

**期待結果:** 締切後／終了後は登録できない（無効またはブロック理由）。disabled でも意図が分かる。

**時間:** 締切直後が必要なら「今すぐ実行不可」と理由を書く。

判定: PASS  
発見事項: TRN-02 と同様、この導線の native dialog でも Hosting domain（`amuse-app-template.web.app`）が表示。Hosting domain 露出が TRN-02 単独ではなく LINE native dialog 横断課題である可能性が高まった。TRN-02 の横断改善と同一課題として管理。  
修正要否: あり（横断修正）  
再確認: 必要（LINE native dialog 横断修正後）  

---

### TRN-06 0件 / local TZ

| 項目 | 内容 |
|------|------|
| ID | TRN-06 |
| necessity | B |
| action | OBSERVE_ONLY |
| client | LINE user |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** TRN-01 実施時で可。

**操作:** 本日／今後／カレンダー（出る場合）の日付と件数を見る。**通信断はしない。**

**期待結果:** 0件がエラーに見えない。端末 local 日付とサーバー一覧がずれる場合は既知（`00` §9.4）。記録して FAIL にしない（ずれ自体は既知）。読込失敗時だけ FAIL。

判定: PASS  
発見事項: 仕様/表示整合性の確認候補。第一巡時点では LINE「今後」と Flutter の当日扱い差を記録。**CLN-J1（2026-08-22）:** Flutter / LINE とも最終UIは **本日 / 今後1週間** に統一。J1-1〜J1-4 実機 **PASS**（詳細は §19 cleanup 再実機記録）。  
修正要否: なし（J1 解消済み）  
再確認: 不要  

---

### TRN-07 ブラインドタイマー

| 項目 | 内容 |
|------|------|
| ID | TRN-07 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** TRN-01 の開催。

**操作:** ブラインドタイマーを開く。短時間見る。

**期待結果:** 表示が破綻しない。raw なし。

**同時確認:** §4。

判定: PASS  
発見事項: 将来の運用/仕様改善候補。Blind template はレベルアップが倍増中心、開始 Blind が主に 100/100 または 100/200。柔軟化の余地はあるが、店舗運用ヒアリング後に仕様検討。現時点では作り込まない。  
修正要否: なし（今回）  
再確認: 不要  

---

## 11. Block 5 SideGame / Table

### SG-01 卓〜chip〜退席

| 項目 | 内容 |
|------|------|
| ID | SG-01 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** Terminal sideGame。入店中ユーザー。テスト卓。

**操作:**

1. 卓を開始／開く
2. 着席またはチップ預入／引出の代表
3. 退席する  
4. **「ドキュメントを作成」「デバッグ実行」は押さない**（SG-03）

**期待結果:** 業務操作が完走。失敗時 raw なし。誤成功に見えない。

**同時確認:** §4。debug ボタンの有無は SG-03。

判定: PASS  
発見事項: UI/UX 改善候補（FAIL にはしない）。  
1. 一部操作ダイアログが内容量に対してサイズが合わずオーバーフローして見える。後続で該当ダイアログを特定し、レイアウト／高さ制約／スクロール可否を確認する。  
2. 引出ダイアログの確定ボタン文言が「引き出し確定」で不自然。「引き出す」等への変更を検討。  
3. 「退席」「預入＋退席」等、退席を伴う操作完了後も対象ユーザーの操作カードが残る（状態と UI 不一致）。成功時はカードを閉じる／非表示にする方向。3点中では優先度高め。  
修正要否: あり（後続 UI/UX 修正候補）  
再確認: **CLN-B3 解消済み**（通常退席 / 預入+退席 / 退席状態整合 PASS）。**CLN-B2 解消済み**（文言確認 PASS）。**CLN-B1 解消済み**（deposit+keyboard 前面/背面 overflow なし、withdraw+keyboard overflow なし。deposit+leave automated PASS）。

**CLN-B3 再実機（実装後）:** PASS（通常退席 / 預入+退席 / 退席状態整合）。

**CLN-B2 再実機（実装後）:** PASS（入力「引き出すchip額」/ ボタン「引き出す」）。

**CLN-B1 再実機（実装後）:** PASS（deposit+keyboard 前面/背面 overflow なし。withdraw+keyboard overflow なし。deposit+leave は automated PASS）。

先に同じ再現手順だけ確認（PASSしてから withdraw 等へ）:

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| B1-0 | SideGame → 操作メニュー → chip預入 → 入力欄 tap → keyboard | 前面 deposit overflow なし。**背後の showUserActionHome に黄黒 indicator なし。** 入力・button 操作可能。親メニューは残る | PASS |
| B1-1 | withdraw dialog を開く（実行不要） | overflow なし。入力・ボタンが自然。keyboard 時も操作可能。B2 文言維持 | PASS |
| B1-2 | deposit dialog を開く（実行不要） | overflow なし。keyboard 時も button が画面外へ消えない。必要なら scroll 可能 | PASS（B1-0 で cover） |
| B1-3 | deposit + leave の該当 dialog を開く（実行不要） | overflow なし。操作領域へ到達できる | PASS（automated） |

**CLN-B3 再実機（実装後）:** PASS（通常退席 / 預入+退席 / 退席状態整合）。

**CLN-B2 再実機（実装後）:**

| ID | 操作 | 期待 |
|----|------|------|
| B2-1 | SideGame 参加中ユーザー → withdraw 画面を開く | 入力ラベルが「引き出すchip額」。旧「引き出しするchip額」なし |
| B2-2 | 同画面 | 実行ボタンが「引き出す」。旧「引き出し確定」なし。raw/internal なし |
| B2-3 | （任意）既存データで 1 回実行 | 挙動は従来どおり。失敗時も「引き出し処理に失敗しました」 |  

---

### SG-03 SG debug 露出

| 項目 | 内容 |
|------|------|
| ID | SG-03 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 除外 |
| 撮影候補 | no |

**事前条件:** SG-01 の Terminal 卓。可能なら卓端末（TBL-01）と比較。

**操作:** 画面を見る。**debug ボタンは押さない。**

**期待結果:** Terminal では `showDebugActions` default ON だが、debug UI（「ドキュメントを作成」「デバッグ実行」）は stream error／卓 doc 未作成のフォールバック時のみ表示。通常の卓ホームでは表示されない。role=table は常に非表示。営業に見せてよいかは cleanup 判断（確認 PASS ≠ 実装の最終許容）。

判定: PASS（確認項目として。実装の最終許容ではない）  
発見事項:  
- 実機: SG-01 実施中を含め通常の Terminal SideGame 画面に debug UI は表示されなかった。  
- 保留理由だった「reachability 不明」は 2026-08-16 静的確認で解消済み。  
静的確認結果:  
- Terminal（`SideGameTableListPage` → `SideGameTableHomePage`）は `showDebugActions` 未指定のため **default ON**。  
- debug UI は **stream error（データ無し）／卓 doc 未作成** のフォールバック時のみ表示。通常の卓ホームでは出ない。  
- role=table は `TableDeviceSideGamePage` で `showDebugActions: false` のため常に非表示。  
- dead ではなく **条件付き reachable**。  
後続 cleanup・修正必須候補（確認 PASS でも未対応のまま残す）:  
1. 本番 Terminal で異常時とはいえ「ドキュメントを作成」「デバッグ実行」が条件付き reachable なのは営業向け製品として望ましくない。  
2. 最終 cleanup では本番 Terminal から debug UI を隔離／OFF する。  
3. `showDebugActions` の default=`true` は安全側ではない。default=`false` 化、または開発／QA 経路からのみ明示的に `true` を渡す構成を検討する。  
4. UI 非表示だけでなく、`debugSideGame` callable 自体の本番実行可否・権限も後続で確認する。  
**2026-08-18 CLN-B4:** 判定 A。Callable source 削除、`showDebugActions` 自体削除、debug ボタン削除。正式卓登録は `registerTableToSideGame`。**live Function は `asia-northeast1` から削除済み。異常状態の実機再現は困難のため automated/static 確認で代替。CLN-B4 完了。**  
修正要否: 実装済み（削除 deploy + 実機が残）  
再確認: 削除 deploy 後に必要  

---

### TBL-01 卓端末ホーム

| 項目 | 内容 |
|------|------|
| ID | TBL-01 |
| necessity | B |
| action | EXECUTE |
| client | Flutter table |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** role=table の端末。無ければ保留（端末不足）。

**操作:** 卓ホームを開く。トーナメント／SG 登録の入口を見る。Firestore path や内部フィールド名が出ないことを見る。SG に入ったら debug が OFF か見る。

**期待結果:** 業務入口がある。path 非表示。SG debug なし。

判定: PASS  
発見事項: なし。role=table 卓ホームで Tournament／SideGame 業務入口あり。Firestore path／内部フィールド名／開発者向け情報の露出なし。SideGame へ入っても debug UI なし。  
修正要否: なし  
再確認: 不要  

---

## 12. Block 6 Accounting

会計は USR-03 の入店ユーザーの伝票を使う。

### ACC-01 start→complete

| 項目 | 内容 |
|------|------|
| ID | ACC-01 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 高 |
| 撮影候補 | yes |

**事前条件:** 未会計伝票（注文済み）。会計権限。

**操作:**

1. 会計管理で対象を開始する
2. 完了まで進める（0円なら0円フロー）
3. Terminal に「支払い分割テスト」が無いことを見る（UI-02 同時可）

**期待結果:** 会計完了。A-7 で完走。raw なし。旧 split テストに行けない。

**同時確認:** §4。要対応タブの空は ACC-06。

判定: PASS  
発見事項: なし。金額あり通常会計・0円会計とも start→complete 正常。raw error／loading 残留なし。Terminal に旧「支払い分割テスト」UI なし（UI-02 同時確認）。  
修正要否: なし  
再確認: 不要  

---

### ACC-02 cancel / 往復

| 項目 | 内容 |
|------|------|
| ID | ACC-02 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** 別の未会計、または ACC-01 の前に実施してもよい（その場合 ACC-01 は別伝票）。

**操作:**

1. 会計開始する
2. 「会計開始前に戻る」で cancel
3. 再度開始できることを見る

**期待結果:** cancel 成功。settling に取り残されない。再 start 可。旧 cancel ダイアログ専用画面は出ない。

判定: PASS  
発見事項（調査結果・コード変更なし）:

**導線の役割（実装上は同一ではない）**

1. 会計開始後の「後で」  
   - 対象: `settling`  
   - mutation: なし  
   - 用途: 会計を完了せず、その場を離れる  
2. 「会計開始前に戻る」  
   - 対象: `settling`（未会計タブ）  
   - mutation: `cancelAccounting`  
   - 用途: 会計開始自体を取り消す  
   - 補足: 会計完了タブの「会計前に戻す」(`reopenAccountedBill`) とは別操作  
3. 独立「会計後操作」  
   - 対象: `settled` / `post_settlement_pending` 系  
   - mutation: post-settlement adjustment 等  
   - 用途: 会計完了後の増減・返金等  

**今回の判断:** コード変更しない。現時点では「後で」を削除しない。

**2026-08-20 追加発見（CLN-C1）**

- 要対応起点 bill で `要対応 → 会計完了 → 会計前に戻す` を実行すると未会計タブへ入る事象を実機で確認
- 期待は「会計開始直前状態への復元」なので、要対応起点 bill は要対応へ戻るべき
- 対応: `reopenAccountedBill` に最小修正（既存 `closeSummary` 履歴から持ち越し未会計由来を判定し、`closeSummary.unresolved` / `closeSnapshot.unresolved` を復元）
- 仕様: 「後で」残否・名称整理は別判断のまま

**2026-08-23 再判定 / C1-C 実装・実機**

- 同日再実機の対象は okibake `pending_review` → remote payment（bill `pNX8mEW9mUDWc0VNM4UQ`）であり、当時は C1-B 判定に使わない
- C1 を A/B/C に整理。旧 CLN-C3 は **C1-C へ吸収**
- C1-C 実装: reopen で entry=`pending_review` + bill=`voided`。Emulator PASS。**C1-C 実機 PASS（2026-08-23）**

**2026-08-25 C1-B 実機 PASS / CLN-C1 解消**

- **C1-B 最終契約:** 通常未会計 → 閉店 → 要対応（入店者の未会計）→ 後日精算（来店なし入金 / 過去伝票精算）→ 必要なら会計前に戻す → 要対応 carryover 復帰。事故閉店の同一 businessDate 再開店は未処理 carryover を通常未会計へ自動復旧。翌営業日開店は carryover 維持
- **金額:** C1-B / okibake とも **claim===received 必須**。差額は settle 後の会計後操作。legacy claim≠received は migration しない
- **会計完了 / 会計後操作:** `ops.accountingCompletedAt` の JST calendar date 基準（`bill.businessDate` は売上帰属のまま変更なし）
- **実機 PASS:** close→carryover / 理由表示 / 来店なし入金 / 会計日表示 / reopen→要対応 / same-day restore / next-day 維持
- **C1-A / C1-C** と合わせ **CLN-C1 解消済み**（cleanup 正本: 統合管理00。詳細履歴: 旧 `04` git history）
- **含めない:** B2 full merge、`AccountingHistoryPage` の businessDate 基準残置、収益認識日の整理、ACC-02「後で」UX

**ACC-02 導線メモ（C1 スコープ外・将来判断）**

- settling 伝票の「会計完了を続ける」再開 UX
- 「後で」残否
- 「会計開始前に戻る」と「会計前に戻す」の名称整理

**実機後 cleanup 時に仕様判断する項目**

- 「後で」を最終的に残すか  
- settling 伝票の「会計完了を続ける」再開 UX  
- 「会計開始前に戻る」と「会計前に戻す」の名称整理  
- 会計途中／完了後の導線全体の分かりやすさ  

修正要否: cleanup 時判断  
再確認: 仕様変更した場合のみ  

---

### ACC-03 二重 start

| 項目 | 内容 |
|------|------|
| ID | ACC-03 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 未会計1件。

**操作:** 開始ボタンを素早く2回。または開始中にもう一度。

**期待結果:** 二重 settling にならない。エラーは安全文言。loading が戻る。

判定: PASS  
発見事項:  
- 実機では UI 遷移が早く 2 回目 tap が成立しなかった（二重 start／二重 settling／raw／loading 残留なし）  
- backend は Firestore transaction + status guard（`open`/`in_progress` のみ）+ `ACCOUNTING_ALREADY_STARTED` / `ACCOUNTING_INVALID_STATE` + idempotency で二重 start 防御あり  
- 本番防御は十分と判断。Functions 変更なし  
- 同時 `Promise.all` 専用 test は現状なし（順次二重 start の emulator test はあり）  

修正要否: なし（今回追加修正なし。後続で必要なら Emulator 同時 start test 追加を検討。Block 6 cleanup の必須修正とはしない）  
再確認: 不要  

---

### ACC-04 会計後操作 1本

| 項目 | 内容 |
|------|------|
| ID | ACC-04 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** ACC-01 で完了した伝票。テスト金額のみ。

**操作:** 会計後操作から **collection または refund を1回**。冪等再送ページは開かない／押さない（QA-04）。

**期待結果:** 1回成功。raw なし。

判定: PASS（修正後の再実機確認完了）  
発見事項: 減額実行時にボタン内 spinner が表示され、`.cursor/rules/flutter-loading-display.mdc` の更新系ルールに違反。  

横断調査: detail 増額／減額・refund・collection で同系統を確認。  

修正（実施済み）: 更新系ルールに従い以下へ統一。  
- 黒半透明全面 overlay  
- AbsorbPointer  
- CircularProgressIndicator  
- mutation 中操作ロック・二重送信防止  
- failure 時解除  
- success 時は loading 解除後に結果表示／遷移  

対象: `postSettlementOperationDetailPage.dart` / `postSettlementRefundDialog.dart` / `postSettlementCollectionDialog.dart` / 関連 test  

再実機確認（2026-08-17）:  
- 減額: 全面 overlay へ修正済み・問題なし  
- 追加代表操作: 同様に問題なし  
- ボタン内 spinner なし  
- loading 解除・成功表示とも正常  

修正要否: 修正実施済み  
再確認: 完了  

---

### ACC-06 要対応 empty

| 項目 | 内容 |
|------|------|
| ID | ACC-06 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**操作:** 「要対応の会計」を開く（ACC ブロック中）。

**期待結果:** 0件がエラーに見えない。失敗時だけエラー。

判定: PASS  
発見事項: なし。0件が正常な empty。error 表示に見えない。raw／loading 残留なし。  
修正要否: なし  
再確認: 不要  

---

### UI-02 split テスト UI なし

| 項目 | 内容 |
|------|------|
| ID | UI-02 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | — |
| 撮影候補 | no |

**操作:** Terminal 会計まわりに「支払い分割テスト」が無いことを見る。**verifyPaymentSplit は呼ばない。**

**期待結果:** 入口なし。会計は ACC-01 で完走済みなら十分。

判定: PASS  
発見事項: なし。ACC-01 時に Terminal 会計周辺へ「支払い分割テスト」入口なしを確認。`verifyPaymentSplit` は未実行。  
修正要否: なし  
再確認: 不要  

---

### QA-04 冪等再送ボタン（押さない）

| 項目 | 内容 |
|------|------|
| ID | QA-04 |
| necessity | C |
| action | DO_NOT_EXECUTE |
| client | Flutter |
| destructive | HIGH |
| source | `02` |
| portfolio | 除外 |
| 撮影候補 | no |

**操作:** Terminal に「会計後操作 冪等再送確認」が見えるかだけ記録する。**開いても再送ボタンは押さない。** 見えたら cleanup 対象としてメモ。

**期待結果:** 有無を記録。実行しないことが PASS 条件（誤実行は FAIL）。

判定: PASS（隔離後の再実機確認完了）  
発見事項: Terminal から「会計後操作 冪等再送確認」へ到達可能であることを確認。テストデータで意図的に実行し、first `reused=false` / second `reused=true`、adjustment count delta=1、nextSequenceNo delta=1、adjustmentId same、cashActionId same。会計後 adjustment の冪等性は正常。ただし正式 Terminal から QA 専用画面へ到達できること自体は営業品質上不適切。ACC-03 の `startAccounting` 二重 start とは別物。  

修正（実施済み）:  
- Terminal Home の entry 削除  
- import 削除  
- QA ページ本体は残置（正式業務導線からの隔離）  
- backend auth / role / status / idempotency は十分のため変更なし  

再実機確認（2026-08-17）:  
- Terminal Home から QA 入口が消えている  
- 正式業務導線から到達不可  

修正要否: 隔離実施済み  
再確認: 完了  

---

## 13. Block 7 Staff / Attendance

STF-01 の staff を ATT / SHF で使う。旧 STF-03（Flutter `createStaffByApp`）は **CLN-D3 廃止**。新規作成は STF-01（LINE）のみ。

**Block 7 サマリ（2026-08-17）:** PASS 6 / HOLD 3 / FAIL 0。実機確認は一旦完了。cleanup 完了とはしない（同日複数 attendance・staff 登録 schema 等は cleanup 課題）。

### STF-01 LINE createStaffAccount

| 項目 | 内容 |
|------|------|
| ID | STF-01 |
| necessity | A |
| action | EXECUTE |
| client | LINE staff |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 未登録 staff LINE、または新規。旧 Flutter `createStaffByApp` 経路は廃止済み（CLN-D3）。STF-01 で登録した staff のみ使用。

**操作:**

1. **未登録 staff:** LIFF 起動 → 登録画面へ遷移 → フォーム送信 → 登録成功後 staff Home
2. **active 既登録 staff:** LIFF 起動時に登録フォームへ遷移せず、そのまま Home へ遷移することを確認

**期待結果:** 未登録は登録成功後 Home。既登録（active）は起動時に Home（通常 live 導線では登録フォームを出さない）。既登録状態でのフォーム再送信は通常 live では起きない（backend に既登録ガードあり）。raw なし。この staff を以降使用。

判定: PASS  
発見事項: 通常登録と再登録で電話番号 placeholder が不統一（通常 `xxx-yyyy-zzzz` / 再登録 `xxxyyyyzzzz`）。cleanup 時に後者（ハイフンなし）へ統一候補。placeholder だけでなく validation / 保存形式も横断確認。  
修正要否: cleanup 時（電話番号 placeholder／入力形式統一）  
再確認: **CLN-D1 解消済み。** Hosting deploy 済み。placeholder 実機 PASS。normalize は automated test PASS。

**CLN-D1 再実機（実装後）:** PASS（placeholder `09012345678`。normalize は automated test）。

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| D1-1 | 新規 staff 登録画面を開く | placeholder が `09012345678`。ハイフンなし入力可能 | PASS |
| D1-1b | （任意）`090-1234-5678` を入力して送信 | 送信前に `09012345678` として扱われる。不要なテスト staff を作る必要があればスキップ可 | PASS（automated） |

**CLN-D4 再実機（mutation feedback。不要な staff は作らない）:**

判定: **CLN-D4 解消済み**（automated 代替。本番 mutation 追加作成なし）。

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| D4-1 | 安全に実行できる代表 mutation 1 つ（登録 / 再登録 / シフト申請） | 成功時に AppDialogs。画面最下部までスクロールしなくても結果が分かる | PASS（automated wiring） |
| D4-2 | 既知の安全な validation / business error 1 つ | 下部メッセージだけではない。AppDialogs または送信前のページ内 validation | PASS（automated wiring） |
| D4-3 | 結果確認 | bottom-only に依存しない | PASS（automated wiring） |

**CLN-D5 staff ↔ user 切替文言（表示確認。データ変更なし）:**

判定: **CLN-D5 解消済み**（repo 文言変更なし。navigation / LIFF 未変更）。

発見事項: 第一巡で「ポーカー1ミニアプリ（ユーザー開発用）へ移動しました。」「ポーカー1ミニアプリ（スタッフ開発用）へ移動しました。」を観測。表示元は repo ではなく **LINE Developers のチャネル名**。user/staff 両チャネル名を正式名称へ変更し解消。

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| D5-1 | staff ホーム「ユーザーに切り替え」から user 画面へ | 遷移する。ボタン／説明に「開発用」が出ない。正式機能として自然 | **PASS** |
| D5-2 | user 画面左上の戻るで staff へ | staff に戻れる。画面下部 message 等に「開発用」が出ない。戻り動作は従来どおり | **PASS** |  

---

### STF-02 LINE reactivate

| 項目 | 内容 |
|------|------|
| ID | STF-02 |
| necessity | B |
| action | EXECUTE |
| client | LINE staff |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** **retired の staff LINE。** 無ければ保留（理由: retired staff 不在）。本番スタッフを退職させない。

**操作:** 再登録画面で同意のうえ送信。

**期待結果:** 再登録成功後 active に戻り Home。raw なし。active のままでは再登録経路に入らない（STF-01 側）。

判定: PASS  
発見事項: 再登録成功・Home 遷移・raw なし。STF-01 と同じ電話番号 placeholder 不統一を記録。  
修正要否: cleanup 時（STF-01 と共通の電話番号形式統一）  
再確認: **CLN-D1 解消済み。** placeholder 実機 PASS。normalize は automated test PASS。

**CLN-D1 再実機（実装後）:** PASS（placeholder `09012345678`。新規登録と表記一致）。

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| D1-2 | 再登録画面を開く | placeholder が `09012345678`。新規登録と表記が一致 | PASS |
| D1-2b | （任意）安全なテスト対象があればハイフン付き入力を 1 回送信 | 正規化後に処理される。対象が無ければスキップ可 | PASS（automated） |  

---

### STF-03 Flutter createStaffByApp（第一巡・**CLN-D3 廃止**）

| 項目 | 内容 |
|------|------|
| ID | STF-03 |
| necessity | B |
| action | EXECUTE（**第一巡のみ**。現行は再実施不要） |
| client | Flutter Admin（**第一巡時**。現行は該当 UI なし） |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**現行（CLN-D3 解消後）:** 正式 staff 新規作成は **LINE `createStaffAccount` のみ**（STF-01）。Flutter Admin の「Staff作成」導線・`createStaffAccountPage.dart`・`createStaffByApp` Callable は **削除済み**（cleanup 正本: 統合管理00。詳細履歴: 旧 `04`）。

#### 第一巡記録（2026-08-17・変更しない）

**事前条件:** Admin「Staff作成」。LINE 登録とは別アカウント。

**操作:** スタッフを1人作成。一覧に出るか見る。退職はテストスタッフのみ（任意）。

**期待結果:** 作成成功。`createStaffAccount` と混同しない。raw なし。

判定: PASS  
発見事項（第一巡）: **重要** — LINE staff 登録と Flutter staff 作成で入力仕様が不一致。LINE は姓・名・かな（姓＋名を分けてひらがな）。Flutter は氏名漢字1フィールド・かなは姓相当のみ・カタカナ。シフト申請・勤怠確認等が LINE staff 側に依存するため、`createStaffByApp` 自体の必要性を cleanup 時に再評価する（調査: Flutter 作成 staff の用途／勤怠打刻対象か／給与対象か／LINE 紐付け経路／Firestore schema 差／downstream／削除影響）。最終判断候補は A. LINE 仕様へ統一 / B. Flutter staff 作成経路を廃止。

**CLN-D3 解消:** 判断 **B**（Flutter staff 作成廃止）を採用。UI / page / source / export / production Callable 削除済み。正式作成は LINE `createStaffAccount` に一本化（cleanup 正本: 統合管理00。詳細履歴: 旧 `04`）。  
修正要否: **解消済み（CLN-D3）**  
再確認: 不要（第一巡 PASS を維持。現行は static で営業 UI 非到達を確認済み）

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| D3-1 | Flutter Admin ホーム・詳細設定 | 「Staff作成」導線なし。`createStaffByApp` 到達不可 | PASS（static / CLN-D3） |

---

### ATT-01 QR 打刻本線

| 項目 | 内容 |
|------|------|
| ID | ATT-01 |
| necessity | A |
| action | EXECUTE |
| client | LINE staff + Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** STF-01 の active staff。Terminal 勤怠 QR。店舗オープン。

**操作:**

1. LINE staff で QR を出す
2. Flutter 勤怠 → 出勤 QR で clockIn
3. LINE 勤怠の当月／当日に出勤が出るか見る（JST）
4. Flutter で退勤 QR（clockOut）
5. LINE 履歴に退勤が出るか見る

**期待結果:** 出勤→退勤が履歴に反映。日付が JST として自然。raw なし。空月は空白カレンダー（専用 empty 文は出なくてよい）。

**同時確認:** §4。businessDate。

判定: PASS  
発見事項: LINE QR → Flutter Terminal 出勤 → LINE 履歴反映 → Flutter 退勤 → LINE 履歴反映まで正常。JST / businessDate に不自然なズレなし。raw なし。  
修正要否: なし  
再確認: 不要  

---

### ATT-02 二重 clockIn / break 退勤

| 項目 | 内容 |
|------|------|
| ID | ATT-02 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** ATT-01 の前半（出勤直後）に実施するのが自然。退勤前に行う。

**操作:**

1. 出勤済みでもう一度出勤 QR を試す（※正式 UI では QR が出勤/退勤兼用のため、open 中は clockOut になる。open 中二重 clockIn は backend 自動確認で代替）
2. 可能なら休憩開始後に退勤を試す（業務上問題ない範囲）

**期待結果:** open 中の二重出勤は拒否または安全文言（正式 UI から連続 clockIn 不可なら backend 代替可）。break 中退勤は現行仕様どおり自動終了して成立してよい。raw なし。その後 ATT-01 の退勤を完了する。

判定: PASS  

**補足（必須）:**

1. **open 中の二重 clockIn:** 正式 UI では LINE staff QR が出勤/退勤兼用。未出勤→clockIn、出勤中→clockOut。正式実機 UI から `clockIn` を2回連続で呼ぶことはできない。backend に未退勤時 `already-clock-in`・transaction ガード・concurrent clockIn test があるため、**open 中二重 clockIn は自動確認で代替済み**として扱う。
2. **break 中退勤（実機確認）:** break 中でも退勤可能。clockOut 時に進行中 break を自動終了（`break.endedAt` = clockOut 時刻、`isOnBreak=false`、`currentBreakStartedAt=null`、`breakMinutes` / `actualWorkMinutes` 等を再計算。break 中時間は勤務に含めない）。**仕様どおり・修正不要。**
3. **cleanup 課題 — 同日再出勤:** 実機で同一 staff / 同一 businessDate に別 attendance が作成可能（例: 04:05〜04:05 と 04:08〜04:09）。backend は未退勤 open のみ拒否し、退勤後は同日別 auto ID を許可。downstream には同日1件前提が多数（LINE 履歴の同日 key 上書き、修正申請・Admin 承認、日数集計が件数ベース等）。製品方針候補は「同日再出勤を単純禁止」ではなく、`1 businessDate = 1 attendance` を維持し、同日再出勤時は既存 attendance を再開・延長する形を検討（中抜け時間は勤務に含めない）。cleanup 時に break モデルでの中抜け表現可否／workSessions 要否／給与・LINE・修正申請・Admin・集計への影響を調査。**今は実装しない。**

修正要否: 営業向けcleanupでは E1〜E3 実装を保留（仕様は確定。open 二重・break 退勤は修正不要）  
再確認: E1〜E3 実装後（CLN-E4）。**現行 schema では PASS にしない**  

---

### ATT-03 修正申請クロス

| 項目 | 内容 |
|------|------|
| ID | ATT-03 |
| necessity | A |
| action | EXECUTE |
| client | LINE staff + Flutter Admin |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** ATT-01 の当日（または過去日）勤怠。Admin「勤怠修正申請」。**同日 attendance は1件であること（同日複数がある状態では対象不一致が出る）。**

**操作:**

1. LINE で当該日の修正申請を出す
2. Flutter Admin で内容を確認する
3. **却下**する（ATT-05 のため。承認する場合は ATT-05 を保留）
4. LINE で再申請できないことを見る（専用 rejected 文言は無く「すでに申請」でよい）

**期待結果:** 申請が Admin に出る。**LINE で見た勤怠と Admin の対象 attendance が一致する。** 却下後 LINE から再申請できない。raw なし。

**ATT-05 との役割:** 本 ID は LINE→Admin→LINE。ATT-05 は Admin 直接編集。

判定: 延期（勤怠再設計実装後に再確認）。**FAIL にしない。** **単独 HOLD ではない**（CLN-E4 / E1〜E3 延期群）  
発見事項: 同日複数 attendance 状態で実施したため、1日1件前提問題が顕在化。LINE staff 側は `04:05〜04:05` を表示して修正申請したが、Flutter Admin では `04:08〜04:09`（2件目）に対する申請として表示。staff が見ている対象と Admin の承認/却下対象が不一致。却下自体は成功し、LINE から同日再申請は不可になった。根本は「同日複数を許容しつつ downstream が1日1件前提」。**E1〜E3 の新 schema 仕様は確定したが、営業向けcleanupでは実装を保留。現行の同日複数 attendance 生成は残存。E1〜E3 実装後に ATT-03 を再確認する。** 追加 cleanup: Flutter Admin 勤怠修正申請の承認/却下 mutation 時に loading 表示がない。repo loading rule に従い、cleanup 時に全面 overlay・操作ロック・完了/失敗後の解除を確認・修正する。  
修正要否: 営業向けcleanupでは実装保留（1日1勤怠化の仕様は確定。loading は CLN-E5）。問題自体は消えていない  
再確認: **E1〜E3 実装後**（対象 attendance の正しさ。loading は CLN-E5 **解消済み**）

**CLN-E5 再実機（loading / lock のみ。対象 attendance の正しさは見ない）:**

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| E5-1 | 安全なテスト申請があれば承認を確定 | 確定直後に全面 loading。approve/reject 含む操作ロック。完了後 overlay 消失。raw error なし | **PASS**（代表 1 操作） |
| E5-2 | または却下理由確定後 | 同様。確認 dialog 中は loading なし。確定後に overlay | 共通 handler のため必須ではない |

共通 handler のため代表 1 操作でよい。ATT-03 の対象一致検証には使わない。  

---

### ATT-04 管理者勤怠 create/edit

| 項目 | 内容 |
|------|------|
| ID | ATT-04 |
| necessity | B |
| action | EXECUTE |
| client | Flutter Terminal |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** 勤怠ページの管理者パスワード。テスト staff のみ。

**操作:**

1. 勤怠 → 管理者用編集 → パスワード
2. 追加または既存の edit
3. 休憩を1つ編集または追加

**期待結果:** 保存できる。パスワード失敗は安全文言。raw なし。

判定: PASS  
発見事項（2026-08-23）: create / timezone（12:00–18:00 がそのまま）/ loading overlay PASS。論理削除行は通常一覧から非表示に修正（DB は残置）。  
実施メモ: デモ staff で実施。過去ズレデータは backfill なし。  
修正要否: なし（実装・実機済み）  
再確認: 不要  

---

### ATT-05 rejected 後の直接修正

| 項目 | 内容 |
|------|------|
| ID | ATT-05 |
| necessity | B |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** ATT-03 で却下済みの日。同じ管理者編集画面。

**操作:** Admin 直接編集で時刻を直して保存する。

**期待結果:** LINE 再申請不可のまま、管理者は直せる。raw なし。

判定: 単純 edit **PASS** / フル（却下後）**延期**（E1〜E4 延期群）  
発見事項（2026-08-23）: 単純 edit / timezone / loading PASS。フル（ATT-03 却下後）は未実施・E1〜E3 実装後（CLN-E4 延期群）。  
修正要否: 単純はなし。フルは案件化後  
再確認: フルのみ **必要**（E1〜E3 後）  

---

### ATT-06 未退勤はタブ3のみ

| 項目 | 内容 |
|------|------|
| ID | ATT-06 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**操作:** Terminal ホームに「未退勤一覧」が無いこと。勤怠のタブ3「未退勤データ一覧」だけあること。

**期待結果:** 独立入口なし。embedded のみ。

判定: PASS  
発見事項: Terminal Home に独立「未退勤一覧」入口なし。勤怠画面タブ3「未退勤データ一覧」は存在。embedded のみで期待どおり。  
修正要否: なし  
再確認: 不要  

---

## 14. Block 8 Shift

SHF-01 と SHF-02 は同一セッションでよい。判定は分ける。

**Block 8 サマリ（2026-09-04 更新）:** SHF-01/02/05/06 等の記録は本ファイルに残す。**CLN-F6 = PASS / RESOLVED**（cleanup 正本: 統合管理00。旧「OPEN / 再実機待ち」表記は stale）。不足日募集は綺麗化スコープ外・営業後延期。

### 補足実機確認: 16〜22日（②期間）の提出済み一覧表示

| 項目 | 内容 |
|------|------|
| ID | （SHF-01 とは別の補足確認） |
| 実施日 | 2026-08-17 |
| 期間状態 | 対象月の前月 16〜22 日（②期間） |

**画面状態:** LINE staff「シフト申請」はカレンダー非表示、「提出済みの申請（YYYY年M月）」一覧表示。

**先に実施した修正:** 提出済み一覧は `pending` 全件ではなく、**現在の申請対象月の dateKey のみ**を表示。

判定: **PASS（16〜22日固有表示）**  
発見事項: 見出し年月は対象月と一致。対象月の申請だけを表示する前提の画面。過去月／別月の申請が混ざる問題なし。カレンダー非表示。raw error / 不自然な表示なし。  
修正要否: なし（対象月 dateKey のみ表示の現修正を維持。別月混在を退行させない）  
再確認: cleanup／退行確認時に必要なら  

---

### SHF-01 正式シフト提出

| 項目 | 内容 |
|------|------|
| ID | SHF-01 |
| necessity | A |
| action | EXECUTE |
| client | LINE staff |
| destructive | LOW |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** STF-01。翌月が申請可能期間。確定済みで閉じている月なら保留（期間待ち）。

**操作:**

1. シフト申請 → カレンダーで日付選択 → 次へ
2. 時刻入力 → 確認へ
3. **「申請」**（`submitShiftRequests`）。「一括提出」は出ない／押さない（SHF-05）
4. 空のまま申請してバリデーションを見てもよい

**期待結果:** 提出成功。loading 解除。raw なし。新規 CF 経路。

**時間:** 16–22 JST ロックや翌月確定済みなら「今すぐ実行不可」＋理由。

判定: **PASS**（2026-09-01 実機）  
発見事項: ①提出期間（前月 1〜15 日）でカレンダー→日付選択→時刻入力→**「申請」**（`submitShiftRequests`）→ pending 作成まで成功。loading 解除・正常 feedback・raw error なし。legacy 一括提出は非表示（SHF-05）。pending 再編集・再提出も実機確認済。  
修正要否: なし  
再確認: 不要  

---

### SHF-02 pending / confirmed

| 項目 | 内容 |
|------|------|
| ID | SHF-02 |
| necessity | B |
| action | EXECUTE |
| client | LINE staff + Flutter Admin |
| destructive | NONE |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** SHF-01 の pending。Admin シフトで確定できる権限。

**操作:**

1. LINE で pending が編集できることを見る
2. Flutter Admin シフトで当該日を確定（テスト月のみ）
3. LINE 確定シフトが非編集になることを見る

**期待結果:** pending 可／confirmed 不可。空一覧がエラーに見えない。Admin draft では **申請時間 = staff 最新**（`startMinute`/`endMinute`）、**割当時間 = Admin local state**（初期値 = 最新申請）。中間確定後も request の start/end は staff 最新を保持し、allocation は `assignments` に保存。

判定: **REOPEN / 追加 UI 修正反映済 / 再実機待ち**（SHF-01 は PASS。SHF-02 本体は未 PASS）  
発見事項: 2026-09-01 実機で (1) Admin 申請時間 `original*` 表示 (2) LINE pending 日 selection 1操作遅れ (3) **Admin draft cross-day selection 残留**（別日 requestId 混入で `selected_request_missing`）を確認。**2026-09-03 追加:** (4) **LINE calendar で interim/final 確定済み日が未申請と区別できない**（マークなし）。**(1)(2) deploy 済。(3) Flutter 修正済。(4) Staff Hosting UI 修正済（working tree・deploy 前）。**  
修正要否: **実装済。再実機待ち**  
再確認: **必要** — §23.6 SHF-02 再実機チェックリスト（A〜E）を通しで PASS すること

**2026-09-01〜03 修正内容:**

| # | 不具合 | 修正 |
|---|--------|------|
| 1 | Admin 申請時間 / 割当時間不整合 | request = latest staff 申請。allocation = Admin local state。interim 後 request.start/end 保持。assignment = Admin allocation（`interimConfirmRequests` **deploy 済**） |
| 2 | LINE pending 日 selection UI 同期不整合 | `selectedDates` SSoT + `syncCalendarDaySelectionStyles()`（Staff Hosting **deploy 済**） |
| 3 | Admin draft cross-day selection 残留 | 中間確定は **1 dateKey 単位**（backend 仕様どおり）。Flutter: 日付切替で `_selectedRequestIds.clear()`、ボタン件数・confirm は current date の pending のみ（`shift_draft_selection.dart`） |
| 4 | LINE calendar 確定済み日が未申請と区別不可 | pending=**緑✓**、interim/final=**青✓**、未申請=なし。凡例更新。確定日は選択ブロック。Firestore `shiftRequests` 直読（Functions 変更なし） |

---

### CLN-F6 不足日募集フロー（調査メモ・2026-08-30）

**扱い:** 綺麗化スコープ外。現状把握完了。営業後の仕様判断・実装へ延期（cleanup 正本: 統合管理00）。

| 区分 | 状態 |
|------|------|
| Admin 不足日算出 / recruitment 作成 / Backend 再提出 guard / Admin pending 受取 | 実装済み |
| `sendRecruitmentNotification` | 部分（admin のみ。Staff 未通知） |
| Staff 不足日再提出 | 部分（Backend OK。LINE 期間 UI 不整合） |
| 不足状態再計算 | 部分（他操作時のみ。Staff 申請直後の自動連鎖なし） |
| Staff Push/UI / recruitment 参照導線 / close / flag lifecycle / 完結フロー | 未実装 |

**`insufficientDaysNotificationSent`:** Staff 通知完了ではない。Admin notification 成功後の period② Staff 再提出解禁 gate。

**期間 UI:** LINE 16〜22 は calendar 非表示。23〜月末は 22 日 hardcode 副作用で calendar 再表示（Backend `schedulingStartDay` 以降〜月末と不一致）。不足日募集未完成のため UI 単独修正はしない。  

---

### SHF-04 hash confirmShiftRequest

| 項目 | 内容 |
|------|------|
| ID | SHF-04 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | LINE staff |
| destructive | LOW |
| source | `02` |
| portfolio | — |
| 撮影候補 | no |

**事前条件:** 運用で `#shift?requestId=` 付きリンクを出している場合のみ。**無ければスキップ＋理由（運用リンクなし）。**

**操作:** そのリンクを LINE 内で **初回オープン**する。通常カレンダー操作では呼ばない。

**期待結果:** 要請確認の overlay が出るか、失敗しても SPA が落ちない。hashchange 後の再トリガーは期待しない。

判定: PASS（OBSERVE_ONLY 完了・**実機実行なし**）  

**調査結果（必須記録）:**

1. **募集作成との関係:** Flutter Admin シフトカレンダー「募集作成」は正式 UI。経路: 不足日選択 → 時間帯設定 →「募集内容を管理者に送信」→ `createRecruitments` → `sendRecruitmentNotification`。保存: `shiftRecruitments/{YYYY-MM}/days/{dateKey}`。**募集作成と `confirmShiftRequest` / `#shift?requestId=...` は別系統。** 募集作成では `shiftRequests` / requestId を作らない。
2. **requestId URL:** repo 内に `#shift?requestId=...` を生成する正式実装なし（Flutter 生成なし／LINE staff は消費のみ／Functions に URL 生成なし／share・clipboard・LINE message 生成なし）。外部手動生成しない限り正式 UI だけでは到達不可。
3. **管理者指定:** 募集送信時に個別管理者指定 UI なし。`sendRecruitmentNotification` は `users.role == admin` の全員へ LINE Push。recipient 指定なし。
4. **管理者 → staff:** 募集後に requestId URL 送信／staff 選択／個別 LINE／broadcast／URL copy・share の正式導線は存在しない。staff 側の正式申請は LINE カレンダーの `submitShiftRequests`。
5. **confirmShiftRequest:** staff 本人が `shiftRequests/{requestId}` を `pending → confirmed` にする確認／ack。Admin のシフト確定とは別。decline webhook はあるが対応する送信側メッセージ生成は見つからない。

**扱い:** 第一巡は OBSERVE_ONLY。**CLN-F2（2026-08-22）で完全削除・production 反映・通常シフト画面 PASS により解消済み。** 旧 deep-link 再現は不要。F6（提出期間）とは別件。

修正要否: なし（CLN-F2 解消済み）  
再確認: 不要（F6 提出期間再実機とは別）  

---

### SHF-05 legacy 一括提出が非表示

| 項目 | 内容 |
|------|------|
| ID | SHF-05 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | LINE staff |
| destructive | NONE |
| source | `02` |
| portfolio | — |
| 撮影候補 | no |

**操作:** シフト申請画面を見る。「一括提出」が営業 UI に出ていないこと。**押さない。**

**期待結果:** live 非到達のまま。見えたら FAIL（露出）として記録し、実行はしない。

判定: **PASS（非表示）** — CLN-K6 で legacy DOM/JS 削除済み（2026-08-25）。コード上 `submitAllShifts` / `#submit-all-shifts-btn` / `#pending-shifts-container` = 0  
発見事項: 一括提出 UI は live 非到達だったことを SHF-05 で確認後、K6 で削除。正式提出は `submitShifts`→`submitShiftRequests`  
修正要否: **不要**（K6 解消）。F6（SHF-01/02）とは別  
再確認: **不要**（削除後 static）。Hosting 未 deploy 時は本番 HTML は旧のまま  

---

### SHF-06 Admin シフト

| 項目 | 内容 |
|------|------|
| ID | SHF-06 |
| necessity | B |
| action | EXECUTE |
| client | Flutter Admin |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** Admin「シフト」。SHF-02 と同一月でよい。

**操作:** シフトカレンダーまたはドラフトを開き、1操作（閲覧または確定の続き）。

**期待結果:** 画面が使える。raw なし。営業日編集へも辿れる。

判定: PASS  
発見事項: 現在、操作対象となる既存シフトデータなし。Flutter Admin「シフト」画面は正常表示。主要導線に問題なし。操作感に不自然な点なし。raw error なし。loading 残留なし。データ依存の確定操作は未実施。現状のデータ条件で確認可能な範囲は問題なし。  
修正要否: なし  
再確認: 不要（SHF-02 再実施時に確定操作をあわせて見てもよい）  

---

## 15. Block 9 Device

**最後の Admin を降格・ブロック・削除しない。操作用 Admin は2台推奨。**

**Block 9 サマリ（2026-08-17）:** PASS 3 / HOLD 0 / FAIL 0。**実機確認完了**（cleanup は別途未完了）。DEV-02 last-admin 最後1台降格・DEV-03 archive 本体は意図的非実行で PASS を妨げない。

### DEV-01 role または status 1本

| 項目 | 内容 |
|------|------|
| ID | DEV-01 |
| necessity | B |
| action | EXECUTE |
| client | Flutter Admin |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** **テスト用の非 last-admin 端末**（例: 予備 Terminal）。本番の唯一の Admin は触らない。

**操作:** デバイス管理で、その端末の role **または** status を1回変え、必要なら戻す。

**期待結果:** 変更できる。SnackBar が安全。raw なし。loading 復帰。

判定: PASS  
発見事項: Flutter Admin → デバイス管理でテスト用非 last-admin 端末の role または status を変更。正常に変更・UI 表示更新も問題なし。raw error なし。loading 残留なし。操作結果表示にも問題なし。必要に応じて元の状態へ復元済み。  
修正要否: なし  
再確認: 不要  

---

### DEV-02 self 非表示 / last-admin

| 項目 | 内容 |
|------|------|
| ID | DEV-02 |
| necessity | B |
| action | EXECUTE |
| client | Flutter Admin |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** Admin 2台。1台しか無いなら **保留（Admin 1台・last-admin 試験不可）**。降格は **2台目があるときだけ** 試し、すぐ戻す。

**操作:**

1. 自端末の role/status/削除ボタンが隠れることを見る
2. 2台あるときだけ、片方の Admin 降格を試し、拒否または成功後に戻す

**期待結果:** 自己操作 UI なし。last-admin なら拒否文言。raw なし。

判定: PASS  

**self 操作:** 自分自身の Admin 端末では role 変更・status 変更・archive／削除の危険操作 UI は非表示。表示は「オプション編集」のみ。自己端末に対する危険操作非表示は PASS。

**複数 Admin 時の降格:** Admin 端末は複数台存在。操作中ではない別 Admin 端末を一時降格→正常変更→Admin へ復元。復元も正常。raw error なし。loading 残留なし。

**last-admin そのもの:** 最後の Admin 1台だけを残してその Admin を降格させる実機試験は **未実施**（実店舗相当データを壊すリスクに対し実機確認メリットが小さいため）。last-admin 保護は **既存 backend 実装／自動テスト側を根拠**として扱う。DEV-02 実機確認としては PASS。

**cleanup 候補:** Admin 端末自身にも「オプション編集」が表示される。Admin に対して意味がない可能性があるため、cleanup 時にオプションの保存先／Admin role でも参照されるか／将来用途／実質無効なら Admin では非表示かを確認。**今は削除しない。**

修正要否: CLN-G1 **解消済み**。CLN-G3 last-admin 保護は **解消済み**（automated。危険実機なし）  
再確認: **CLN-G1 解消済み。** 実機: self Admin にオプション編集なし。他 Terminal には残る。G3 追加実機不要。

**CLN-G1 再実機（実装後）:** PASS（self Admin: オプション編集なし。他 Terminal: オプション編集あり）。

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| G1-1 | デバイス管理 → 自分自身の Admin 端末 | 「オプション編集」がない。role/status/archive も従来どおり危険操作なし。画面崩れ/raw error なし | PASS |
| G1-2 | 別 Terminal 端末を開く | 「オプション編集」が残っている。通常の端末管理 UI 維持。options を実際に変更する必要はない | PASS |

**CLN-G3 last-admin 保護（automated 代替。危険実機は行わない）:**

判定: **CLN-G3 解消済み**。最後の有効 Admin（`role==admin` かつ status 正規化後 `active`）の role 降格 / block / archive は backend が拒否。blocked / archived / retired は有効 Admin に含めない。UI は self 危険操作のみ非表示。複数 Admin の正当変更は許可。destructive 実機は未実施・不要。  

---

### DEV-03 archive（実行しない）

| 項目 | 内容 |
|------|------|
| ID | DEV-03 |
| necessity | C |
| action | DO_NOT_EXECUTE |
| client | Flutter |
| destructive | HIGH |
| source | `02` |
| portfolio | — |
| 撮影候補 | no |

**操作:** 削除＝archive ボタンが **他端末向けに見えるか** だけ見る。**押さない。**

**期待結果:** ボタンの有無を記録。実行しない。誤押下は FAIL。

判定: PASS（**OBSERVE のみ**・archive 本体は未実行）  
発見事項: Flutter デバイス管理で自分以外の端末に削除／archive ボタンが表示されることを確認。自端末には危険操作なし。archive 処理自体は実行していない。誤操作なし。本項目は archive 機能そのものの実行テストではなく、営業 UI 上で他端末向け正式導線が存在する観測確認。destructive HIGH のため統合実機では意図的に未実行。  
修正要否: なし（正式な他端末管理導線として残す。統合実機では実行しない方針を維持）  
再確認: 不要  

---

## 16. Block 10 Admin / QA

**Block 10 サマリ（2026-08-30 更新）:** PASS 4（QA-01, QA-03, QA-06, **PAY-01**）/ OBSERVE完了 1（QA-02）/ HOLD 0 / FAIL 0。PAY-01 は CLN-L1 解消（calc target / carry-over / hourlyWage guard / result summary 実機 PASS）。QA 系の実行ボタンは意図的に未実施。

### PAY-01 給与代表

| 項目 | 内容 |
|------|------|
| ID | PAY-01 |
| necessity | B |
| action | EXECUTE |
| client | Flutter Admin |
| destructive | LOW |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** Admin 給与計算。本番確定を誤って流さない。表示確認が中心。

**操作:** 給与計算画面を開き、候補表示または計算タブの代表1操作（確定はテスト方針があるときだけ）。

**期待結果:** 画面が落ちない。success なし契約でも誤成功に見えない。raw なし。文言が極端に技術的すぎない。

**同時確認:** §4。通知ベルは任意。

判定: **PASS**（2026-08-30）  
発見事項: 2026-08-17 実機時点は対象期間外。「2026/08/01~2026/08/31」はまだ終了していないため、現在計算を行えません、と表示され、正式な代表操作は実行不可。**期間外ガード表示は PASS 相当**（第一巡時点）。  
**2026-08-30（calc target bug）:** `buildPayrollDisplayContext` が active period を返していた一方、calc tab は `today > periodEnd` を要求しており calc window=true になる日が存在しなかった。修正: displayContext を **直前に終了した給与期間**（`getCalcTargetPeriodRange`）へ変更。**deploy 済・実機 PASS。**  
**2026-08-30（carry-over）:** 4件 / 18h01m。backend 計算正常。**PASS。**  
**2026-08-30（hourlyWage guard + result summary）:** 時給未設定時に 0 円 completed していた事象を修正。`executeMonthlyPayroll` run 作成前 server guard。`getPayrollCandidates` は `wageMissingStaff` で candidate warning + execute disabled。result tab は §4-1（全 staffResults 合算）と §4-2（grossPay=0 card 非表示）を分離。**実機 PASS。**  
修正要否: **解消済み**（CLN-L1）  
再確認: **不要**

---

### QA-01 SystemSettings 露出（中は押さない）

| 項目 | 内容 |
|------|------|
| ID | QA-01 |
| necessity | C |
| action | DO_NOT_EXECUTE |
| client | Flutter Terminal |
| destructive | HIGH |
| source | `02` |
| portfolio | 除外 |
| 撮影候補 | no |

**操作:** 歯車で SystemSettings を開く。タイル（リセット・デモ・移管等）が並ぶことを記録する。**いずれの実行ボタンも押さない。**

**期待結果:** 到達できる（既知）。営業デモで踏みやすいなら発見事項に書く。誤実行しなければ PASS。

判定: PASS（DO_NOT_EXECUTE・実行系は未実施）  
発見事項: Flutter Terminal の歯車から SystemSettings へ到達。リセット／デモ／移管等のタイル表示を確認。画面表示に問題なし。raw error なし。実行系ボタンは押していない。誤実行なし。  
修正要否: cleanup 時（隔離／権限。既存候補）  
再確認: 不要（露出観測は完了。中の実行はしない）  

**2026-08-18 CLN-H1:** Terminal AppBar の歯車／`SystemSettingsPage` 遷移を削除。page class は残置。実機: Terminal ホームに SystemSettings 入口なし。**解消済み。**  

---

### QA-02 LogOps グリッド（emit しない）

| 項目 | 内容 |
|------|------|
| ID | QA-02 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter Admin |
| destructive | LOW |
| source | `02` |
| portfolio | 除外 |
| 撮影候補 | no |

**操作:** Admin に「logOpsError 代表サンプル」があるか見る。**中の送信はしない。**

**期待結果:** 有無を記録。cleanup 対象。

判定: PASS（OBSERVE_ONLY 完了・**emit 未実施**）  
発見事項: Flutter Admin に `logOpsError` 代表サンプル導線が表示されている。emit／送信操作は実施していない。営業 UI に QA／debug 用途の導線が残っていることを確認。cleanup 対象。現時点では削除しない。cleanup 時に非表示化／QA 専用導線への隔離／完全削除を判断する。  
修正要否: cleanup 時（非表示／QA 隔離／削除）  
再確認: **CLN-H2 解消済み。** 実機: Admin ホームから `logOpsError 代表サンプル` 消失。他 Admin 項目維持。automated test PASS。

**CLN-H2 再実機（実装後）:** PASS（Admin ホームにサンプルなし。他 Admin 項目維持）。

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| H2-1 | Admin ホームを開く | `logOpsError 代表サンプル` が表示されない | PASS |
| H2-2 | 同画面 | 詳細設定・デバイス管理など正式 Admin 項目がこれまでどおり表示。画面崩れ/raw error なし | PASS |  

---

### QA-03 Firestore size 入口

| 項目 | 内容 |
|------|------|
| ID | QA-03 |
| necessity | C |
| action | OBSERVE_ONLY |
| client | Flutter Terminal |
| destructive | LOW |
| source | `02` |
| portfolio | 除外 |
| 撮影候補 | no |

**操作:** 「Firestoreサイズ計算」を開く。タイトルと初期表示を見る。**全スキャン実行はしない**（開始ボタンがあれば押さない）。

**期待結果:** 開ける。raw / path が出ない。重い処理を走らせない。

判定: PASS（OBSERVE_ONLY・全スキャン未実施）  
発見事項: Flutter Terminal から「Firestoreサイズ計算」を開けた。タイトル／初期表示に問題なし。raw error なし。Firestore 内部 path 等の不自然な露出なし。全スキャン／重い処理は実行していない。開始ボタン等は押していない。  
修正要否: cleanup 時（営業 UI に残す必要性を確認。保守用途なら正式な保守導線への隔離も検討）  
再確認: **CLN-H3 解消済み。** Terminal 営業ホームから入口削除。page 本体は残置。scan 実機は不要。

**CLN-H3 再実機（実装後）:**

| ID | 操作 | 期待 | 判定 |
|----|------|------|------|
| H3-1 | Terminal ホームを開く | Firestoreサイズ計算が表示されない。SystemSettings 歯車も引き続き表示されない | **PASS** |
| H3-2 | 同画面 | 注文・入退店・会計・Tournament・SideGame 等の正式項目が残る。layout 崩れ/raw error なし | **PASS** |  

---

### QA-06 一時卓タイル（作らない）

| 項目 | 内容 |
|------|------|
| ID | QA-06 |
| necessity | C |
| action | DO_NOT_EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | — |
| 撮影候補 | no |

**操作:** SystemSettings に「一時テーブル作成」があるか見る。**作成画面で保存しない。**

**期待結果:** 到達の有無を記録。卓は増やさない。

判定: PASS（DO_NOT_EXECUTE・保存未実施）  
発見事項: SystemSettings に「一時テーブル作成」導線が存在し到達可能。作成画面で保存はしていない。実際の卓は増やしていない。raw error なし。誤実行なし。cleanup 時に、正式な営業機能として残すか、SystemSettings 上の保守／QA 導線として残すか、別の正式業務 UI へ移すかを再判断する。**今回は削除しない。**  
修正要否: cleanup 時（移設／残置／削除の判断）  
再確認: cleanup 後に必要なら  

**2026-08-18 CLN-H4:** 仕様確定は **詳細設定 → 卓管理 → 一時テーブル作成**。既存 `CreateTemporaryTablePage` を再利用。SystemSettings 側 tile は削除。実機: 詳細設定 → 卓管理 → 一時テーブル作成 PASS（保存なし・崩れ/raw error なし）。**解消済み。**  

---

## 17. Block 11 Close/Open

> **警告:** このブロックは **テスト営業日のみ**。通常営業データでは実施しない。  
> 順序: CLS-01 → CLS-02 → CLS-04 → CLS-03。CLS-06 は Settings を押さない確認のみ。  
> 強制閉店（CLS-05）は D。通常閉店がゲートで進めないとき以外はやらない。

**Block 11 サマリ（2026-08-18）:** PASS 5 / HOLD 0 / FAIL 0。**実機確認完了**（cleanup は別途未完了）。**CLS-02 → CLS-04 → CLS-03 の正式な一連フロー（正式閉店 → 閉店後 LINE 確認 → 開店）が実機で成功。** CLS-05 は通常閉店成功のため意図的未実施（D／自動確認代替）。FAIL/HOLD には含めない。

### CLS-01 閉店前確認

| 項目 | 内容 |
|------|------|
| ID | CLS-01 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | NONE |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** storeManagement または Admin。テスト日。未退勤・未閉 T を意図的に残して一覧を見てもよい。

**操作:** 開閉店 → 閉店処理を開始 → 閉店前確認まで。**ここではまだ閉店確定しない。**

**期待結果:** 未会計・未退勤・未閉 T が一覧される。タップで T に飛べる。raw なし。

判定: PASS  
発見事項: Flutter から開閉店 → 閉店処理を開始し、閉店前確認画面まで到達。未会計／未退勤／未終了トーナメント等の確認表示に問題なし。必要な対象一覧が正常に表示。raw error なし。この時点では閉店確定していない。  
**CLN-I4（2026-08-22）:** 未終了TN / 未会計 / 未退勤を同一パターン（カード→詳細dialog→正式画面）へ。I4-1〜I4-3 実機 **PASS**（§19）。  
修正要否: なし（I4 解消済み）  
再確認: 不要  

---

### CLS-02 正式閉店

| 項目 | 内容 |
|------|------|
| ID | CLS-02 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | HIGH |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes（必須ではない。破壊的操作のため任意） |

**事前条件:** **テスト営業日。** CLS-01 済み。本番データ禁止。

**操作:** 「確認して閉店する」で `closeStoreTerminal`。強制閉店は使わない。

**期待結果:** 閉店完了。失敗時は安全文言＋再試行。SystemSettings の単体 reset は使わない。

判定: PASS  
発見事項: テスト営業日で正式閉店を実施。「確認して閉店する」から `closeStoreTerminal` 経路を使用。強制閉店は使用していない。閉店完了。raw error なし。loading 残留なし。SystemSettings の単体 reset は使用していない。正式閉店フローとして問題なし。  
修正要否: なし（`closeStoreTerminal` を正本の閉店経路として維持。個別 reset を正式閉店の代替にしない）  
再確認: 不要  

---

### CLS-04 閉店後 LINE

| 項目 | 内容 |
|------|------|
| ID | CLS-04 |
| necessity | A |
| action | EXECUTE |
| client | LINE user (+ staff 任意) |
| destructive | HIGH（閉店後状態） |
| source | `02` |
| portfolio | 低 |
| 撮影候補 | no |

**事前条件:** CLS-02 直後。USR-03 の user。

**操作:** LINE user ホームの在店を見る。注文ボタン。必要なら履歴。

**期待結果:** 未入店／注文不可など閉店後として自然。誤って入店中のままに見えない。raw なし。

判定: PASS  
発見事項: CLS-02 直後に LINE user 側を確認。閉店後として自然な状態に更新されている。誤って入店中のままに見えない。注文不可等の状態も問題なし。raw error なし。閉店による user 側状態反映は正常。  
修正要否: なし（閉店後の在店／注文状態反映の現仕様を退行させない）  
再確認: 不要  

---

### CLS-03 開店

| 項目 | 内容 |
|------|------|
| ID | CLS-03 |
| necessity | A |
| action | EXECUTE |
| client | Flutter |
| destructive | MEDIUM |
| source | `02` |
| portfolio | 中 |
| 撮影候補 | yes |

**事前条件:** CLS-02 済みのテスト日。

**操作:** 開店処理を開始する。日付調整が必要ならパスワード経路のみ。

**期待結果:** 開店できる。翌営業日として破綻しない。raw なし。

判定: PASS  
発見事項: CLS-02 後の状態から Flutter で開店処理を実施。正常に開店できた。翌営業日として状態破綻なし。raw error なし。loading 残留なし。正式な開店フローとして問題なし。  
修正要否: なし  
再確認: 不要  

---

### CLS-06 Settings 単体 reset しない

| 項目 | 内容 |
|------|------|
| ID | CLS-06 |
| necessity | C |
| action | DO_NOT_EXECUTE |
| client | Flutter |
| destructive | HIGH |
| source | `02` |
| portfolio | 除外 |
| 撮影候補 | no |

**操作:** QA-01 で見た「全テーブルリセット」「全SGリセット」「閉店クリーンアップ」「未会計移管」を **押さない。** 正式閉店は CLS-02 のみ。

**期待結果:** 単体 reset を使わずに Block 11 を終える。誤押下は FAIL。

判定: PASS（DO_NOT_EXECUTE・単体 reset 系は意図的に未実行）  
発見事項: Block 11 では SystemSettings 内の全テーブルリセット／全 SG リセット／閉店クリーンアップ／未会計移管／その他、正式閉店を代替する単体 reset 系を使用していない。正式閉店は CLS-02 のみ。誤操作なし。  
修正要否: cleanup 時（単体 reset 系を QA／保守用途として残すなら隔離／権限制御。営業デモから誤操作しにくい構造にする）  
再確認: 不要（未実行の観測完了）  

**2026-08-18 CLN-I1:** H1 により当該 reset 系は営業 Terminal から非到達。Flutter UI 入口は SystemSettings のみ（詳細設定へ未移設）。callable は残置。正式閉店は `closeStoreTerminal`（Block 11 PASS）。destructive 単体実行は未実施のままで正しい。**解消済み。追加実機不要。**  

---

## 18. 自動確認で代替済み

本体では実施しない（D）。偶然観測したら発見事項へ。

| ID | 内容 | 根拠 |
|----|------|------|
| ACC-05 | start rollback 注入 | Emulator startAccounting* |
| TRN-05 | T 登録 unknown | ORD-03 と同型 + public 134 |
| SG-02 | 預入後・退席失敗 | Flutter widget |
| ATT-07 | 修正 unknown | 同上 |
| SHF-03 | シフト unknown | 同上 |
| CLS-05 | 強制閉店 | 通常閉店で cover。**2026-08-18: CLS-02 正式閉店が成功したため実機未実施**（実行条件なし。自動確認／既存テスト根拠） |
| UI-03 | 旧削除画面入口 | git + 主要回帰 |
| AUTO-01 | Functions helpers / logging map | tsc / Jest |

---

## 19. 実機後cleanup候補

cleanup の current 判定・件数・最終結果の正本は `docs/綺麗化/00_営業向け綺麗化_統合管理.md`。  
本節は第一巡時点の索引。実装計画をここに増やさない。旧 `04_実機後cleanup計画.md` の CLN フル表は git history 参照。

第一巡は FAIL 0 で完了。cleanup 実装は当時の `04` に沿って進行し、その後 Final Cleanup まで CLOSED。**現在件数の正本は統合管理00（全件 54 / DONE 50 / DEFERRED 4（E1〜E4）/ remaining 0。F6 = PASS / RESOLVED）**。下記索引表の件数は第一巡棚卸し時点のまま残し、最新件数は統合管理00を見る。

| 残 | 件数（第一巡索引・参考） |
|----|------|
| P0 営業前必須 | 7 |
| P1 できれば | 17 |
| P2 仕様判断 | 9 |
| P3 再実機のみ | 7 |
| P4 今回対象外 | 4 |

### P0（営業前必須）

| ID | 対象 | 状態 |
|----|------|------|
| CLN-B3 | 退席後も操作カードが残る | **解消済み。** 実機で通常退席 / 預入+退席 / 退席状態整合 PASS |
| CLN-B4 | SG debug / `debugSideGame` | **解消済み。** Callable/UI 削除 + live Function 削除。異常時実機は困難のため automated/static 代替 |
| CLN-C2 | okibake 来店なし入金 | **解消済み。** ①実機 PASS。③既存 bill `hwlRXiI4Ly3hTwEBPCiV` read-only。②④⑤ automated。backfill しない |
| CLN-E1 | 1 businessDate = 1 attendance | **延期**（営業向け対象外。案件化後。実装済みではない） |
| CLN-E2 | 再入店データモデル | **延期**（workSessions。E1 とセット） |
| CLN-E3 | downstream の 1 日 1 件前提 | **延期**（E1〜E3 実装後。ATT-03 は E4 で再確認） |
| CLN-E4 | ATT-03 等の勤怠実機確認 | **延期**（E1〜E3 実装後。独立 HOLD ではない） |
| CLN-H1 | Terminal から SystemSettings（reset/demo） | **解消済み。** 歯車入口削除。実機で Terminal ホームに入口なし |

### 仕様判断が先（P2）

会計「後で」の再開 UX / 名称整理（ACC-02）は将来判断。**CLN-C1（A/B/C）は解消済み**（cleanup 正本は統合管理00。詳細履歴は git の旧 `04`）。旧 CLN-C3 は C1-C へ吸収。`createStaffByApp`（**CLN-D3 解消済み**）/ `confirmShiftRequest` / 募集通知宛先 / admin→staff 配布 / 「本日・今後1週間」 / 強制閉店 / 一時卓は **判断または解消済み**。

### cleanup 再実機記録（2026-08-22）

| ID | 結果 | 記録 |
|----|------|------|
| CLN-I4 | **PASS** | I4-1 / I4-2 / I4-3。カード→詳細dialog→正式画面（未会計→AccountingPage / 未退勤→StaffAttendancePage）。status 表示削除済み。**解消済み** |
| CLN-J1 | **PASS** | J1-1〜J1-4。最終UIは Flutter/LINE とも **本日 / 今後1週間**。**解消済み** |
| CLN-F2 | **PASS** | LINE staff 通常シフト画面を開ける。申請期間外のためカレンダー/申請操作は非実施（既存仕様・F6 と別）。旧 requestId deep-link は削除済みのため再現不要。**解消済み** |
| CLN-C2 | **解消済み** | ①実機 PASS。③既存 bill `hwlRXiI4Ly3hTwEBPCiV` read-only。②④⑤ automated。backfill しない |
| CLN-C1 | **解消済み** | C1-A/B/C。**C1-B 実機 PASS（2026-08-25）。** C1-C 実機 PASS（2026-08-23）。会計日基準 UI・same-day restore・claim===received 含む。旧 C3 は C1-C 吸収。cleanup 正本: 統合管理00 |
| CLN-C3 | **C1-C へ吸収** | 独立残件ではない |
| CLN-L1 | **PASS** | PAY-01 実機 PASS（calc target / carry-over / hourlyWage guard / result summary）。**解消済み（2026-08-30）** |

### 再実機のみ（P3・コード待ちではない）

ATT-03 / ATT-05 フル（**延期**。E1〜E3 実装後。単独 HOLD ではない）。**CLN-F6 = PASS / RESOLVED**（cleanup 正本: 統合管理00）。PAY-01 **PASS**（CLN-L1 解消）。SHF-05 は CLN-K6 解消で PASS。ATT-04 / ATT-05 simple・USR-01/07・ORD-03・CLN-C2・CLN-E6 は解消済み。

### 再確認（今はコード修正しない）

該当なし。CLN-D5 は LINE Developers チャネル名修正 + D5-1/D5-2 実機 PASS で **解消済み**（repo 変更なし）。

### 解消済み / 現状維持（退行させない）

QA-04 冪等再送の Terminal 隔離、ACC-04 overlay、TRN-03 loading、16〜22 対象月フィルタ、live の LINE native dialog→AppDialogs、device archive 導線、閉店後 LINE、`closeStoreTerminal` を正式閉店の正とする、CLN-B4 `debugSideGame` 削除、CLN-H1 SystemSettings 営業入口削除、CLN-H4 一時卓を詳細設定へ移設、CLN-I1 単体 reset 営業 UI 隔離、CLN-B3 退席後操作カード残留、CLN-B2 withdraw 文言、CLN-G3 last-admin 保護（automated）、CLN-D5 staff↔user 切替、**CLN-D2/D3**、**CLN-I2/I4**、**CLN-J1（本日/今後1週間）**、**CLN-F2/F3/F4**、CLN-F5 は今回対象外（staff 配布新設なし）。

### 今回やらない

Tournament カードの参加費表示、Blind 柔軟化、ATT-08 logical delete、`00` §9.4/§10 の将来課題、**E1〜E4 勤怠再設計の実装と実装後実機確認**（仕様は確定。営業向けcleanupでは延期）。

---

## 20. FAIL / 保留一覧

実施中に追記する。FAIL は現時点 **0 件**（**再実機待ちは SHF-02 のみ** — CLN-F6 営業前残件。SHF-01 PASS 済）。ATT-03 / ATT-05 フルは **延期**（E1〜E4 延期群。単独 HOLD ではない）。PAY-01 **PASS**（CLN-L1 解消）。SHF-05 は CLN-K6 解消で PASS。ATT-04 / ATT-05 simple・USR-01/07・ORD-03・CLN-C2・CLN-E6 は 2026-08-23 に解消。SG-03 は保留（reachability 不明）→ 静的確認後 PASS（履歴行を維持）。TRN-03 は一次 FAIL → 修正後再実機で PASS（履歴行を維持）。

**Block 6:** PASS 7 / FAIL 0 / HOLD 0（**確認完了 + cleanup完了**）— 2026-08-17。  
- ACC-04: loading 修正後の再実機 PASS  
- QA-04: Terminal 隔離後の再実機 PASS  
- ACC-03: backend 確認済・追加修正なし  
- ACC-02: 導線仕様（「後で」残否／settling 再開 UX／名称整理）は **実機後 cleanup 時の仕様判断**として残す（Block 6 ブロッカーではない）

**Block 7:** PASS 6 / FAIL 0 / HOLD 0（**2026-08-30 更新: ATT-03 は延期群へ移行**）— 2026-08-17 第一巡 + 2026-08-30 更新。  
- PASS: STF-01, STF-02, STF-03, ATT-01, ATT-02, ATT-06  
- ATT-03: **延期**（E1〜E4 延期群。勤怠再設計実装後に再確認。FAIL/HOLD ではない）  
- ATT-04 / ATT-05: **PASS**（simple edit。CLN-E6 解消）。ATT-05 フル（却下後）は E1〜E4 延期群  
- ATT-02 は PASS（open 二重は backend 代替、break 中退勤は仕様どおり）。同日再出勤・1 businessDate = 1 attendance は cleanup 課題  
- STF cleanup: 電話番号 placeholder（D1 解消済み）、LINE/Flutter schema（**D2/D3 解消済み**。正式作成は LINE のみ）、操作結果フィードバックUI統一（CLN-D4 **解消済み**）、staff↔user 切替文言（CLN-D5 **解消済み**。表示元は LINE Developers チャネル名。両チャネル名変更 + D5-1/D5-2 PASS）

**Block 8:** PASS（SHF-01/02/05/06 等）。**CLN-F6 = PASS / RESOLVED**（cleanup 正本: 統合管理00。旧「再実機待ち / OPEN」は stale）。  
- PASS: **SHF-01**, **SHF-02**, SHF-06, **SHF-05**（CLN-K6）  
- OBSERVE_ONLY 完了: SHF-04  
- cleanup: **CLN-F2/F4/K6/L1 解消済み**。**CLN-F5 今回対象外**。不足日募集: 現状把握完了・営業後延期。詳細操作記録は本ファイル各 SHF 節に残す

**Block 9:** PASS 3 / HOLD 0 / FAIL 0（**実機確認完了・cleanup未完了**）— 2026-08-17。  
- PASS: DEV-01, DEV-02, DEV-03  
- HOLD: なし  
- DEV-02: last-admin 最後の1台降格は意図的に実機未実施（backend／自動テスト根拠）  
- DEV-03: archive 本体は DO_NOT_EXECUTE。UI 導線観測のみで PASS  
- cleanup: Admin「オプション編集」要否は CLN-G1 **解消済み**。archive 方針維持。last-admin は CLN-G3 **解消済み**（automated。危険実機なし）

**Block 10:** PASS 4 / OBSERVE完了 1 / HOLD 0 / FAIL 0（**2026-08-30 更新**）— 2026-08-17 第一巡 + 2026-08-30 更新。  
- PASS: QA-01, QA-03, QA-06, **PAY-01**（CLN-L1 解消。いずれも実行系操作は意図的に未実施 — PAY-01 を除く）  
- OBSERVE_ONLY 完了: QA-02（`logOpsError` 代表サンプル表示あり・emit 未実施・cleanup 対象）  
- HOLD: なし  
- cleanup: logOpsError 代表サンプル、Firestore サイズ計算の営業 UI 残否、一時テーブル作成の移設／残置／削除 — **CLN-L1 解消済み**

**Block 11:** PASS 5 / HOLD 0 / FAIL 0（**実機確認完了・cleanup未完了**）— 2026-08-18。  
- PASS: CLS-01, CLS-02, CLS-04, CLS-03, CLS-06  
- HOLD: なし  
- 一連: **CLS-02 正式閉店 → CLS-04 閉店後 LINE → CLS-03 開店** が実機で成功  
- CLS-05: 通常閉店成功のため意図的未実施（D／自動確認代替。FAIL/HOLD に含めない）  
- CLS-06: 単体 reset 系は意図的未実行  
- cleanup: Settings 単体 reset の隔離、`closeStoreTerminal` 正本維持、**CLN-I2 強制閉店は現状維持で解消**、閉店後 LINE の退行防止、**CLN-I4 復旧導線 PASS で解消**

| ID | status | 理由 | next action | 修正要否 | 再確認 |
|----|--------|------|-------------|----------|--------|
| USR-01 | PASS | 未登録 LINE 本登録〜再起動後ホームまで PASS。入店表示懸念は実状態問題なし。新規 CLN 不要（CLN-R1 解消） | 不要 | なし | 不要 |
| USR-07 | PASS | 捨丸→まんじゅうや実移行 PASS。historical ended `unlinked` 過剰 block は guard 修正 + deploy 後に解消（CLN-R2） | 不要 | 実施済み | 不要 |
| ORD-03 | PASS（automated） | 通常注文実機済み。通信断 unknown は timing 依存で未再現。classify / orderAllItems 静的 / placeOrderByUser idempotency の automated で代替 PASS（2026-08-23） | 不要 | なし | 不要 |
| ATT-03 | 延期（E1〜E3 実装後） | 同日複数 attendance により LINE 表示対象と Admin 対象が不一致。**E1〜E3 仕様確定・営業向け実装保留。** CLN-E4 延期群。単独 HOLD ではない | E1〜E3 実装後に ATT-03 再確認（CLN-E4） | 営業向けでは実装しない | **E1〜E3 実装後** |
| ATT-04 | PASS | create / timezone / loading / 論理削除後一覧非表示 PASS（CLN-E6 解消） | 不要 | なし | 不要 |
| ATT-05 | 単純 PASS / フル延期 | 単純 edit PASS。フル（却下後直接修正）は E1〜E4 延期群 | フルは E1〜E3 実装後 | 単純なし | フルのみ E1〜E3 実装後 |
| SHF-01 | PASS | **2026-09-01** ①提出期間でカレンダー正式フロー・`submitShiftRequests`・pending 作成・feedback・loading/lock・raw error なしを実機 PASS | 不要 | なし | 不要 |
| SHF-02 | REOPEN / 追加 UI 修正反映済 / 再実機待ち | #4 LINE calendar 確定済み青✓ を追加修正。#1–3 に続き Hosting working tree。§23.6 通し再実機 | §23.6 A〜E + 緑/青✓凡例 | (1)(2) deploy 済 / (3)(4) working tree | **必要** |
| SHF-05 | PASS | legacy 一括提出 DOM 削除済み（CLN-K6）。非表示確認 PASS | — | 不要 | **不要** |
| SHF-04 | OBSERVE完了（PASS） | 正式 requestId URL 生成/配布経路なし。募集作成は別系統。実機実行なし。営業必須項目にしない | **CLN-F2 解消済み**（confirm / deep-link / decline 削除 + production 反映 + 通常シフト画面 PASS） | 実装・実機済み | 不要 |
| CLS-05（注記） | 実機未実施（D／自動確認代替） | 通常の正式閉店 CLS-02 が成功したため実行条件なし。強制閉店は通常閉店がゲートで進めない場合のみ想定。FAIL/HOLD に含めない | **CLN-I2 解消済み**（emergency として現状維持。追加実装なし） | 方針維持 | 不要 |

| DEV-02（注記） | PASS（注記あり） | last-admin 最後の1台降格は意図的に実機未実施。複数 Admin 時の降格・復元と self 危険操作非表示は実機 PASS。保護は backend／自動テスト根拠 | CLN-G3 **解消済み**（automated。危険実機未実施・不要） | なし | 不要 |
| DEV-03（注記） | PASS（OBSERVEのみ） | archive 本体は DO_NOT_EXECUTE・未実行。他端末向け削除/archive ボタン表示の観測のみ。誤操作なし | 統合実機では実行しない方針を維持。正式導線として残す | なし（方針維持） | 不要 |
| PAY-01 | PASS | calc target / carry-over / hourlyWage guard / result summary 実機 PASS（2026-08-30） | CLN-L1 **解消済み** | 解消済み | 不要 |
| QA-02 | OBSERVE完了（PASS） | Admin に `logOpsError` 代表サンプル導線あり。emit 未実施。営業 UI の QA/debug 露出 | CLN-H2: 営業入口削除。page は **判定 B** で残置（QA emit）。削除しない | 隔離済み | 不要 |
| QA-01（注記） | PASS（DO_NOT_EXECUTE） | Terminal 歯車から SystemSettings 到達・タイル表示確認。実行系未実施・誤実行なし | CLN-H1: 歯車入口削除。実機で Terminal ホームに入口なし。**解消済み** | 実装・実機済み | 不要 |
| QA-03（注記） | PASS（OBSERVEのみ） | Firestoreサイズ計算の入口・初期表示確認。全スキャン／開始ボタン未実施。内部 path 露出なし | CLN-H3: 営業入口削除。page は **判定 B** で残置（maintenance read）。削除しない | 隔離済み | 不要 |
| QA-06（注記） | PASS（DO_NOT_EXECUTE） | 一時テーブル作成導線到達。保存未実施・卓未増加。誤実行なし | CLN-H4: 詳細設定 → 卓管理へ移設。実機入口 PASS（保存なし）。**解消済み** | 実装・実機済み | 不要 |
| CLS-06（注記） | PASS（DO_NOT_EXECUTE） | 単体 reset 系（全テーブル／全 SG／閉店クリーンアップ／未会計移管等）は意図的未実行。正式閉店は CLS-02 のみ。誤操作なし | CLN-I1: H1 により営業 UI 非到達。正式閉店は Block 11 PASS。destructive 未実行のままで正しい。**解消済み。追加実機不要** | 隔離判定済み | 不要 |
| ACC-04 | 解消済み（PASS） | （一次）減額時ボタン内 spinner。全面 overlay へ修正後、減額＋代表操作の再実機 PASS（2026-08-17） | なし（Block 6 cleanup 完了） | 修正実施済み | 完了 |
| QA-04 | 解消済み（PASS） | （一次）正式 Terminal から冪等再送 QA 到達可能。entry 隔離後、再実機で入口なし・業務導線から到達不可（2026-08-17） | なし（Block 6 cleanup 完了） | 隔離実施済み | 完了 |
| ACC-02 | PASS（実機後 cleanup 時仕様判断） | 3導線は役割が異なる。調査済・コード未変更。「後で」は現時点削除しない | 実機後 cleanup 時に「後で」残否／settling 再開 UX／名称整理を判断 | cleanup 時判断 | 仕様変更時のみ |
| ACC-03 | 解消済み（PASS） | UI は二重 tap 不成立。backend は txn＋status＋idempotency で十分。Functions 変更なし | なし。必要なら後続で Emulator 同時 start test 検討 | なし | 不要 |
| OKIBAKE-RP settle | **解消済み（2026-08-25）** | 来店なし入金は **claim===received 必須**（C1-B と同契約。legacy claim≠received は migration しない）。ByCategory/onSettle 維持。会計後調整が差額の正式ルート。関数単位 deploy 済 | `resolveOkibakePendingReviewWithRemotePayment` exact-match + UI | 不要 | 完了 |
| SG-03 | 解消済み（PASS） | （一次保留）通常 Terminal で debug UI 未確認・reachability 不明。静的確認で条件付き reachable（Terminal default ON、error/未作成時のみ UI、table は常時 OFF）。確認項目は PASS。**実装の最終許容ではない** | CLN-B4: 判定 A。source 削除 + live Function 削除済み。異常時実機は困難のため automated/static 代替。**完了** | 解消済み | 不要 |
| TRN-03 | 解消済み（PASS） | （一次 FAIL）(1) 個別参加者ダイアログ Addon 永続 loading。(2) 順位確定→終了の二重 loading。2026-08-16 修正後再実機で両方解消・Tournament 終了まで正常完了 → PASS | なし（Block 4 完了） | 修正実施済み | 完了 |
| TRN-02 / TRN-04（横断） | PASS（営業品質改善あり） | LINE native dialog（alert/confirm）で Hosting domain（`amuse-app-template.web.app`）が露出。TRN 固有ではなく LINE user/staff 横断候補 | LINE native dialog 横断修正後に再確認 | あり（横断） | 必要 |

---

## 21. 実機確認完了条件

- A 全件 PASS（skip した場合は理由が残っている）
- B は PASS、または根拠付き代替／保留解消
- C 全件観測済み（DO_NOT_EXECUTE は誤操作なし）
- FAIL 0
- 再確認待ち 0
- destructive の誤操作なし
- raw error 露出なし
- 新規 P0/P1 なし
- 主要業務フロー完走
- Flutter↔LINE 統合完走（USR-03, ORD-01, TRN-02, ATT-01, ATT-03, CLS-04）
- §4 営業品質を主要画面で確認済み

---

## 22. 完了後の次工程

```text
03 第一巡完了（HOLD は条件待ち）
↓
04 棚卸し・実装バッチ
↓
最終監査（2026-08-31 完了。`00` §14）
↓
2026-09-01: F6 deploy 反映（`interimConfirmRequests` + Staff Hosting）← SHF-02 再実機待ち
2026-09-01: F6 実機 SHF-01 PASS ← 完了
2026-09-01: F6 実機 SHF-02 通し再実機 ← **最終クローズ条件（未実施）**
↓
PASS なら inventory 50+4+0=54・正本更新・「営業向け綺麗化 完了」
↓
cleanup 最終成果物 commit（除外: hosting cache / flutter_open_error_paths_inventory.tsv）
↓
cleanUp push
↓
営業用ポートフォリオ作成
```

---

## 23. F6 クローズ結果（最終）

cleanup current 正本は `docs/綺麗化/00_営業向け綺麗化_統合管理.md`。本節は実機実行観点の要約。旧 `00` §14 / 旧 `04` §11 の詳細は git history。

### 23.1 方針・結果

**CLN-F6 = PASS / RESOLVED。** SHF-01 / SHF-02 とも PASS。営業前 remaining は **0**。

現時点 inventory（cleanup 正本と一致）: DONE **50** / DEFERRED **4**（E1〜E4）/ remaining **0** / total **54**

### 23.2 F6 実機確認項目

| ID | 依存 | 確認内容 | 最終 |
|----|------|----------|------|
| **SHF-01** | — | LINE staff・カレンダー・`submitShiftRequests`・pending・feedback・loading/lock・raw error なし | **PASS** |
| **SHF-02** | SHF-01 | pending 編集 → Admin draft → interim → final（通し） | **PASS / RESOLVED** |
| **SHF-05** | — | legacy 一括提出 DOM 削除（CLN-K6） | **PASS** |

**F6 現状:** **PASS / RESOLVED**

**F6 クローズ条件に含めない:** 不足日募集フロー（営業後延期）

### 23.3 inventory（確定）

| 区分 | 件数 |
|------|------|
| DONE / resolved | **50**（F6 含む） |
| DEFERRED | **4**（E1〜E4） |
| remaining | **0** |
| **合計** | **54**（**50 + 4 + 0 = 54**） |

**注意:** E1〜E4・別 track 技術負債・不足日募集は残る。**cleanup 完了 ≠ repository 全体 technical debt ゼロ。** Final Cleanup Batch も別枠で CLOSED（統合管理00 §7）。

### 23.4 最終監査サマリ（2026-08-31・当時）

当時 commit 前に実施。詳細は git history の旧 `00` §14。

| 監査 | 結論 |
|------|------|
| A. cleanup 目的達成 | **PASS WITH KNOWN LIMITATIONS**（blocker / reopen **なし**） |
| B. repository 全体横断 | **PASS WITH KNOWN LIMITATIONS**（P0/P1 **なし**） |
| C. `.cursor/rules` | **MOSTLY COMPLIANT**（cleanup 差分の新規違反 **なし**） |
| 回帰 | Flutter 871/871 PASS / Functions cleanup 重要 suite ALL PASS |
| `functions/lib` | clean build 後 stale JS **0** → deploy artifact stale **RESOLVED** |

### 23.5 commit / push（確定）

Final Cleanup 成果物は commit `1d53760`（`Finalize final cleanup batch`）で push 済。current cleanup 正本は統合管理00。
### 23.6 SHF-02 再実機チェックリスト（2026-09-01 deploy 後）

**前提:** SHF-01 PASS 済。Flutter は **§5 working tree ビルド**（Admin 端末）。Functions `interimConfirmRequests` + Staff Hosting（`https://amuse-app-template.web.app/staff/`）は **2026-09-01 deploy 済**。

#### A. LINE pending 再編集

- pending 申請済み日を 1 件選択 → **即 selected 色**
- 2 件目選択 → それぞれ即反映
- 1 件解除 → 即解除
- ✓ 色と selected 状態が一致
- pending 申請を編集して再提出

**カレンダー状態表示（2026-09-03 修正後）:**

- pending 日 → **緑✓**（申請済み・修正可能）
- interim_confirmed / final_confirmed 日 → **青✓**（確定済み・修正不可）
- 未申請日 → チェックなし
- 上部凡例に緑/青の意味が表示されること
- 青✓日をタップ → 選択されず「すでに確定されているため変更できません」相当
- selection sync（緑✓の即時色反映）が退行していないこと

#### B. Admin draft

staff 最新申請例: 19:00–22:00

| 項目 | Admin 初期表示 |
|------|----------------|
| 申請時間 | 19:00–22:00 |
| 割当時間 | 19:00–22:00 |

Admin が割当を 19:00–21:00 に変更した場合:

| 項目 | 期待 |
|------|------|
| 申請時間 | 19:00–22:00 のまま |
| 割当時間 | 19:00–21:00 |

最新申請範囲外へ allocation できないこと。

**cross-day selection（2026-09-01 修正後）:**

- 10/2 で check → 10/1 タブへ移動 → selection **0 件**（ボタン disabled）
- 10/1 のみ check → 成功
- 10/1 で Staff A + B を check → 「2件を中間確定」→ 成功
- 他日 requestId が state に残っても current date 以外は count / confirm 対象外

#### C. interim

- 中間確定成功
- 可能なら Firestore 確認:
  - `shiftRequests.startMinute` / `endMinute` = staff 最新申請
  - `assignments.startMinute` / `endMinute` = Admin allocation
  - `status`: `interim_confirmed`

#### D. final

- final confirm 成功
- staff 側で `final_confirmed` 後は編集不可

#### E. UX

一連の操作で:

- loading 異常なし
- 二重操作なし
- success feedback あり
- raw error なし
- native alert/prompt 等の退行なし

**PASS 判定:** A〜E を同一セッションで問題なし → SHF-02 PASS → F6 解消（resolved 50）
