# カスタム支払い：サーバー検証 + roundingUnits（実装済み 2026-05-30）

## 概要

| # | 内容 | 実装 |
|---|------|------|
| 1 | カスタム支払いのサーバー検証 | `verifyCustomPayment` callable + `startAccounting` 内 `validateAndNormalizeCustomPayment` |
| 2 | roundingUnits（100枚・1000円単位） | Flutter `payment_rounding.dart` + `CategoryPaymentMethodDialog` + 確認ダイアログ |

## デプロイ対象 Functions

- `verifyCustomPayment`（新規）
- `startAccounting`（検証統合）
- `verifyPaymentSplit`（roundingUnits を storeConfig から適用）

## 関連テスト

- `functions/__tests__/services/paymentRounding.spec.ts`
- `functions/__tests__/services/customPaymentValidator.spec.ts`
