# やること整理

## 01 からの引き継ぎ事項（2026-05-29）

`01_事後イベント会計後調整本番化` の完了にともない、以下の未完了項目を引き継ぎます。

| # | 引き継ぎ事項 | 優先度 | 内容 |
|---|---|---|---|
| C-1 | 旧経路廃止 | ✅ 完了（2026-05-29） | `postEvent*` 系 repos / `updateAccounting` / `processRefund` / `billsEventsOnCreate` を `functions/src/unused_function_lib` へ移動。旧 UI（`postAccounting*Dialog`、`refundProcessingDialog`、`postAccountingAdjustmentsPage`）を `lib/to_be_deleted` へ移動。リモートには既に存在せず。 |
| C-2 | `paymentMethodsByCategory` 自動推論による `categoryPaymentMatrix` 充填 | ✅ 完了（2026-05-30） | `billsOnSettle.ts` に `inferPaymentMethodsByCategory` 実装・デプロイ済み。`paymentSplitCalculator` の `roundingUnits` 対応も統合。`paymentMethodsInference.spec.ts` PASS。実機確認済み。 |
| C-2.5 | ポイント・チップでの追加徴収・返金対応 | ✅ 完了（2026-05-30） | `recordPostSettlementCashAction.ts` でポイント/sideGameChip の残高確認・差し引き・返金バリデーション（`paymentTotals + collected - refunded` 上限）実装・デプロイ済み。Flutter `postSettlementCollectionDialog` / `postSettlementRefundDialog` に選択肢・残高表示・上限バリデーション追加済み。`recordPostSettlementCashAction.specialMethods.spec.ts`（16ケース）PASS。実機確認（bill `6214aa42`）済み。 |
| C-3 | 月次整合性チェック整備 | ✅ 完了（2026-06-01） | `analyticsDailyCheck` / `analyticsMonthlyCheck` / `reportingDailyCheck` / `reportingMonthlyCheck` の 4 callable 実装・デプロイ済み。`batchJobLogs` コレクション基盤構築済み。Flutter UI（admin_detail_settings_page）への手動起動ボタン追加済み。詳細は `10_仕様_C3_整合性チェック.md` 参照。Phase 2（スケジューラー統合）は `07(+08)_schedulerとCloudTasks確認` で管理。 |

---

## ✅ 完了済み

- C-1: 旧経路廃止（2026-05-29）
- C-2: paymentMethodsByCategory 自動推論（2026-05-30）
- C-2.5: ポイント/チップ 追加徴収・返金（2026-05-30）
- B-3-a（updateActiveBill chipRate 修正）（2026-05-30）
- カスタム支払いサーバー検証 + roundingUnits（2026-05-30）
- UI修正 Step 2/3/4（2026-05-30）
- B-1: draftAccountingInput 残存 → 仕様として確定（対応不要）（2026-05-30）
- B-2: lastResettledAt 初回 settle 書き込みバグ → 修正・デプロイ済み（2026-05-30）
- B-3: paymentTotals.sideGameChip 単位 → 仕様として確定（円額、対応不要）（2026-05-30）
- B-4: 旧互換フィールド整理（postEvents / paymentsSummary / receiptNumber 等 11フィールド削除）→ 完了（2026-05-30）
- **C-3: 整合性チェック 4 callable 実装・デプロイ・実機確認完了（2026-06-01）**

## 残件（次に取り組む順）

なし。全タスク完了。

---

### ~~月次整合性チェックの整備~~（→ C-3 として完了）

~~`analyticsMonthly` および `reportingMonthly`（今後実装）について、月次で集計値の整合性を確認する仕組みを整備します。~~

**対応済み（2026-06-01）:**
- `analyticsDailyCheck` / `analyticsMonthlyCheck` / `reportingDailyCheck` / `reportingMonthlyCheck` の 4 callable 実装
- `batchJobLogs/{jobKey}/executions` スキーマで構造化ログ記録
- Phase 2（スケジューラー統合）は `07(+08)_schedulerとCloudTasks確認` フォルダで管理

---

### 実機確認で見つかった要確認事項（2026-05-11）

以下は `07_実機による動作確認/03_後続用メモ.md` に記録された要確認事項です。

| # | 現象 | 結果 |
|---|---|---|
| B-1 | `draftAccountingInput` が cancel / settle 後も parent に残存する | ✅ 仕様（2026-05-30）。reopen 後の再会計引き継ぎ用に意図的に保持。対応不要。 |
| B-2 | `reopenSummary.lastResettledAt` が初回 settle 時にも更新される | ✅ 修正済み（2026-05-30）。`cycle > 1` のときのみ書き込む条件に変更・デプロイ済み。 |
| B-3 | `paymentTotals.sideGameChip` の単位が円額でなく枚数寄りに見える | ✅ 仕様（2026-05-30）。円額で保持が正しい仕様。実機確認済み。対応不要。 |
| B-4 | parent root 直下に旧互換 field（`amounts`, `postEvents`, `itemsSnapshot` 等）が残存する | ✅ 完了（2026-05-30）。`postEvents` / `paymentsSummary` / `receiptNumber` 等 11フィールドを削除。詳細は `08_削除計画` / `09_進捗管理` 参照。 |

## 完了の定義

- 細かな残件が1か所にまとまっている ✅
- blocking / 非 blocking が分かれている ✅
- 本番前に残せないものに優先順位が付いている ✅
- 「これは今回やらない」が明文化されている ✅
