# Step 01: 型定義と設定 — 作業履歴と申し送り

## 作業日

2026-05-28

## 作業内容

1. `functions/src/domains/reporting/types.ts` を新規作成
   - `ReportingEntry`, `ReportingMonthly`, `TaxReportingBehavior`, `ReportingGroupConfig` 等
2. `functions/src/domains/reporting/config/defaults.ts` を新規作成
   - `DEFAULT_TAX_REPORTING_BEHAVIOR` 定数
3. `functions/src/domains/reporting/config/taxReportingBehaviorLoader.ts` を新規作成
   - `loadTaxReportingBehavior()` + 内部 `mergeWithDefaults()`
4. `functions/src/shared/config/types.ts` を変更
   - `features.reportingAggregatorEnabled` 追加
5. `functions/src/shared/config/defaults.ts` を変更
   - `DEFAULT_REPORTING_AGGREGATOR_ENABLED = false` 追加
6. `functions/src/shared/config/configLoader.ts` を変更
   - `buildFromDefaults()`, `mergeWithDefaults()`, `mergeConfigForUpsert()` の 3 箇所に追加

## TypeScript チェック

`npx tsc --noEmit` → 通過

## 申し送り

- `taxReportingBehaviorLoader` は Step 04 以降で bills.onSettle トリガーから呼び出される予定。
- `reportingAggregatorEnabled` フラグは、Step 04 で bills.onSettle 統合時にガードに使用する。
- テストは Step 01 単体では不要（型定義・デフォルト値のみ。ローダーのテストは統合時に追加可能）。
