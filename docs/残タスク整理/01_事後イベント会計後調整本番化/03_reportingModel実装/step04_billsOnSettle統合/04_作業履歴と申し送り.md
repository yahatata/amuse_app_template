# Step 04: billsOnSettle 統合 — 作業履歴と申し送り

## 作業日

2026-05-28

## 実施内容

- `billsOnSettle.ts` に reporting 統合を追加
- bill 再読み込みを analytics ブロック外に移動し、reporting と共有
- `categoryBreakdown` の `sideGameChips` → `sideGameChip` キー変換を実装
- `resettle` / `settle` の判定は `currentSettlementCycle > 1` で判定
- `tsc --noEmit` でコンパイルエラーなしを確認
- 既存テストの破損なしを確認

## 申し送り

- `paymentMethodsByCategory` は `draftAccountingInput` → `meta` の優先順で取得
- `settledAt` には batch commit 前に生成した `now` (Timestamp) を使用
- reporting entry の書き込みは batch commit + cleanup + analytics enqueue 完了後に実行される
- 実機確認は `step04_billsOnSettle統合` 後に `実機確認と要確認事項/02_実機確認_settle.md` で実施
