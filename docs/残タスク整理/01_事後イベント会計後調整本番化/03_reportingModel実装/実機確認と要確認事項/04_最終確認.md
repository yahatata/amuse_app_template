# 最終確認チェックリスト（本番環境での実機確認）

## 前提条件

- 本番環境（amuse-app-template）にデプロイ済み（Functions / rules / indexes）✅
- `storeMeta/taxReportingBehavior` / `storeMeta/reportingGroupConfig` / `storeMeta/config` 設定済み ✅
- `storeMeta/config` で `reportingAggregatorEnabled: true` に設定済み ✅

---

## 全 9 シナリオ

### シナリオ 1: 通常会計 ✅ 確認済み（2026-05-29）

> 確認 bill: `d17343e1`, `6cd950c4`, `6604a1ea`, `ErdnFAQ1sTWPV7aQILcW`, `9zGBrYSE4d3RGdUHgNpi` 等複数

- [x] テーブルに顧客を着席させ、注文を追加して会計を完了する
- [x] Firestore で `reportingEntries/{billId}_settle_1` が作成されていることを確認
- [x] `entryType` が `settle` であること
- [x] `categoryBreakdown` / `paymentBreakdown` が伝票の金額と一致すること
- [ ] `categoryPaymentMatrix` が伝票の金額と一致すること（⚠️ `paymentMethodsByCategory` データなし → 別タスク）
- [x] `reportingMonthly/{yyyyMM}` が更新されていること
- [x] `aggregationMarkers` にエントリが存在すること

### シナリオ 2: carryover（翌日以降会計）⚠️ 未確認

> 明示的な carryover テストは未実施。`dateRule.settle = 'settledAt'` の設定であるため、originBusinessDate と reportingMonth の乖離ケースは今後の営業で自然に発生する。

- [ ] 営業日をまたいで会計を完了する（入店日と会計日が異なる）
- [ ] `reportingEntries` の `originBusinessDate` が**入店日**であること
- [ ] `reportingMonth` が**会計した月**（`settledAt` ベース）であること
- [ ] `reportingMonthly` が会計月で更新されていること

### シナリオ 3: 即時返金 adjustment + cashAction ✅ 確認済み（2026-05-29）

> 確認 bill: `6cd950c4-f5aa-4488-8a64-39973d9ac8a4`（返金 -3000円 / cash）

- [x] 会計後の伝票に対して事後イベント（返金）を作成する
- [x] cashAction（返金実行）を記録する
- [x] `reportingEntries/{billId}_cashAction_{cashActionId}` が作成されること
- [x] 金額が負値であること（返金）
- [x] `reportingMonthly` の合計が減少していること

### シナリオ 4: 後続徴収 cashAction ✅ 確認済み（2026-05-29）

> 確認 bill: `6604a1ea-3e77-4c62-bc36-6d001324cc1c`（追加徴収 +1300円 / ピザ+タコス）  
> ※ 初回テスト時に `recordPostSettlementCollection` の旧バージョン問題を検出・再デプロイ済み

- [x] 会計後の伝票に対して追加徴収の事後イベントを作成する
- [x] cashAction（徴収実行）を記録する
- [x] `reportingEntries/{billId}_cashAction_{cashActionId}` が作成されること
- [x] 金額が正値であること（徴収）
- [x] `reportingMonthly` の合計が増加していること

### シナリオ 5: reopen ✅ 確認済み（2026-05-29）

> 確認 bill: `6604a1ea-3e77-4c62-bc36-6d001324cc1c`（reopen_1 / extraCost: 3000 → -3000）

- [x] 会計済み伝票を reopen する
- [x] `reportingEntries/{billId}_reopen_{cycleNo}` が作成されること
- [x] 全金額が**負値**（元の settle の逆符号）であること
- [x] `reportingMonthly` の合計が元の settle 分だけ減少していること
- [x] `reportingMonth` が元の settle と**同じ月**であること（`reverseInOriginalMonth` ポリシー）

### シナリオ 6: resettle（再会計）✅ 確認済み（2026-05-29）

> 確認 bill: `d17343e1-9fa7-4d21-90dd-38457820b488`（cycle 2, 3 まで確認）

- [x] reopen した伝票を再度会計する
- [x] `reportingEntries/{billId}_resettle_{cycleNo}` が作成されること（`cycleNo` >= 2）
- [x] 再会計時の金額で `categoryBreakdown` 等が記録されること
- [x] `reportingMonthly` が再会計の金額分だけ更新されること

### シナリオ 7: `reportingMonthly` 整合性 ✅ 確認済み（2026-05-29）

> `rebuildReportingMonthly` 手動実行で 202605 の全 13 件を再集計。totalAmountIncl = 34,000円（rebuild後）

- [x] `totalAmountIncl` が全 `reportingEntries` の合算と一致すること
- [x] `categoryBreakdown` の各キーの値が、全エントリの同キー合算と一致すること
- [x] `paymentMethodBreakdown` の各キーの値が、全エントリの同キー合算と一致すること
- [ ] `categoryPaymentMatrix` の各キー（⚠️ `paymentMethodsByCategory` 未実装のため空 → 別タスク）

### シナリオ 8: 冪等性 ✅ 確認済み（2026-05-29）

> 全ラウンドを通じて `aggregationMarkers` の重複なしを確認。`entryWriter.ts` の `create` → gRPC code 6 ガードはコードレビューで確認。

- [x] `reportingEntries` が重複しないこと（`entryWriter` の `create` が ALREADY_EXISTS で `{ written: false }` を返す）
- [x] `reportingMonthly` の `aggregationMarkers` により、同一エントリの二重加算がないこと

### シナリオ 9: `analyticsMonthly` 共存 ✅ 確認済み（2026-05-29）

> 複数の bill で `analyticsMonthly` markers の存在と独立性を確認

- [x] 上記のシナリオ実行後、`analyticsMonthly` の動作が変わっていないことを確認
- [x] `analyticsMonthly` の `aggregationMarkers` と `reportingMonthly` の `aggregationMarkers` が独立していること

---

## 追加確認

### `rebuildReportingMonthly` callable ✅ 確認済み（2026-05-29）

> 実機確認セッション中に 202605 を複数回 rebuild。全件正常に再集計されることを確認。

- [x] 指定月の `reportingMonthly` が再集計されること
- [x] 再集計後の値が、`reportingEntries` の合算と一致すること
- [x] `aggregationMarkers` が全件再作成されていること

### `initReportingConfig` スクリプト ✅ 確認済み（2026-05-29）

- [x] `storeMeta/taxReportingBehavior` が正しいデフォルト値で作成されていること
- [x] `storeMeta/reportingGroupConfig` が作成されていること
- [ ] 2 回目の実行で既存ドキュメントがスキップされること（未検証・コード上は `merge: false` + 既存チェックあり）

### feature flag 確認 ✅ 確認済み（2026-05-29）

> `02_実機確認_settle.md` 確認3 で実施（bill: `011964eb`）

- [x] `reportingAggregatorEnabled: false` の状態で会計しても `reportingEntries` が作成されないこと
- [x] `reportingAggregatorEnabled: true` に変更後、会計すると `reportingEntries` が作成されること
