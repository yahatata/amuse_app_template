# Step 02: entryBuilder — 作業履歴と申し送り

## 作業日

2026-05-28

## 作業内容

1. `functions/src/domains/reporting/services/entryBuilder.ts` を新規作成
   - 3 つのビルダー関数 + ヘルパー関数群
2. `functions/__tests__/reporting/entryBuilder.spec.ts` を新規作成
   - 14 テストケース（settle 7件、cashAction 3件、reopen 2件、helper 2件）

## TypeScript チェック

`npx tsc --noEmit` → 通過（未使用 import `ReportingEntryType` を削除して解消）

## テスト結果

14 テスト全件 PASS

## 設計判断

- `buildCashActionEntry` の `categoryPaymentMatrix` 按分はカテゴリ金額比に基づく `Math.round` で丸め。端数差は許容（按分結果は参考値であり、正本は paymentBreakdown と categoryBreakdown）。
- `mapTargetCategory` は静的マッピングテーブル。新しい targetCategory が追加された場合はフォールスルーで元の名前をそのまま使用する。

## 申し送り

- Step 04（billsOnSettle 統合）で `buildSettleEntry` を bills.onSettle トリガーから呼び出す。
- Step 05（即時 cashAction 統合）で `buildCashActionEntry` を呼び出す。
- Step 07（reopen 統合）で `buildReopenRollbackEntry` を呼び出す。
