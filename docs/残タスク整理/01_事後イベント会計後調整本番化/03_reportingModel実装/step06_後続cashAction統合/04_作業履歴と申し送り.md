# Step 06: 後続 cashAction 統合 — 作業履歴と申し送り

## 作業日

2026-05-28

## 実施内容

- `recordPostSettlementCashAction.ts` に reporting 統合を追加
- `ReportingCaptureFromTx` インターフェースを追加し、transaction 内で adjustment lines を capture
- `CashActionDoc.executedAt` の `unknown` → `Timestamp` キャストを追加
- `tsc --noEmit` でコンパイルエラーなしを確認
- 既存テストの破損なしを確認

## 申し送り

- `allocationAdjustmentSnaps` は transaction 内で既に read されているため、reporting 用の追加 read は発生しない
- `linkedAdjustmentId` は allocation が 1 件の場合のみ設定。複数 allocation の場合は null（reporting 側で特定不可のため）
