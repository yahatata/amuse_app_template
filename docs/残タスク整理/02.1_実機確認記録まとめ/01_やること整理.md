# やること整理

## このタスクの現状（2026-06-01 更新）

**01・02 の作業を通じて大半の実機確認が代替確認済みとなり、台帳の更新が完了しました。**

`docs/事後イベント（会計後調整系）本番化/07_実機による動作確認/02_確認結果集約.md` を更新済み。  
全体判定は「**条件付き OK**」でクローズ。

---

## 残っている未確認シナリオ（3件）

### 未確認 1: シナリオ 12-F — carryover bill の会計再開

**内容:** 翌日以降会計（carryover）状態の bill を `要対応の会計` 画面から会計再開し、  
通常会計に引き継いで完了できることを確認する。

**確認すること:**
- `AccountingPage` へ遷移できること
- carryover bill を通常会計の続きとして操作完了できること
- 完了後 `closeSummary.unresolved = false` になること
- `要対応の会計` 一覧から消えること

**いつやるか:** **実運用で翌日以降会計が自然発生したタイミング**  
（明示的なテストは不要。`01_やること整理.md` にも「実運用で自然確認」と明記済み）  
発生した際は結果を `12_要対応一覧と後続徴収返金確認.md` のシナリオ F 実施結果欄に記録すること。

---

### 未確認 2: シナリオ 12-I — リアルタイム更新と排他表示

**内容:** `要対応の会計` 画面を開いたまま別端末から bill 状態を更新し、  
stream 反映とカード種別の排他表示を確認する。

**確認すること:**
- Firestore stream の変更がリアルタイムに画面反映されること
- 同一 bill が 未会計 / 追加徴収 / 要返金 に二重表示されないこと

**いつやるか:** **優先度低・実運用で自然に通過**  
日常業務での複数端末運用中に問題が出た場合に優先確認する。  
明示的な単体テストは現時点では不要と判断。

---

### 未確認 3: reportingDailyCheck / reportingMonthlyCheck の定常動作確認

**内容:** デプロイ済みの整合性チェック callable が、実際の運用データで安定して  
`"ok"` 判定を返すことを確認する。

**背景:** 2026-06-01 時点で以下の状況:
- `analyticsDailyCheck` / `analyticsMonthlyCheck`: OK 判定済み
- `reportingDailyCheck`: `billsOnSettle` 旧バージョンによるデータ不整合で NG（同時開発エンジニア側の問題。対応方針は別途判断）
- `reportingMonthlyCheck`: 対象月のデータが少なく warning（正常動作）

**確認すること:**
- `billsOnSettle` が最新バージョンで稼働するようになってから数日後、`reportingDailyCheck` が `"ok"` に戻ること
- 月が変わった後（毎月2日 JST 05:10 以降）に `reportingMonthlyCheck` が `"ok"` を返すこと

**いつやるか:** 以下の2段階で確認する
1. **近日（`billsOnSettle` 更新後 1〜3 営業日以内）**: `admin_detail_settings_page` から `reportingDailyCheck` を手動実行し、`"ok"` になることを確認
2. **来月2日以降**: `reportingMonthlyCheck` を手動実行し、`"ok"` になることを確認

確認できたら `02_確認結果集約.md` の備考欄を更新すること。

---

## 完了の定義（更新）

- [x] `02_確認結果集約.md` の全5ファイルの結果が記録されている
- [x] 全体判定が「条件付き OK」に更新されている
- [ ] 未確認シナリオ 1〜3 が実運用・経過観察で順次クローズされること
- [ ] 未確認 3 について `reportingDailyCheck` が `"ok"` 安定したことが確認できた時点でこのタスクを「完了」に更新する
