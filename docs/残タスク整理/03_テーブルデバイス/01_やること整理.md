# やること整理

## 仕様として確定済みの事項

- ✅ `tableDevice` は店舗従業員（ディーラー）が操作する卓専用端末として扱う
- ✅ `role: table` を独立 role とする
- ✅ 卓状態の単一参照先は `tables/{tableId}` とし、`tournamentDetail` を追加する
- ✅ サイドゲーム時の `tables.status` は固定値 `sideGame` ではなく、`sideGameTypes` に含まれる**ゲーム名**を使う
- ✅ `tableDevice` はデバイス登録画面でも登録可能にする
- ✅ 同一卓に複数の `tableDevice` を紐付けてよい
- ✅ `tableId` 未設定の `table` 端末は、管理者対応を促す専用メッセージを表示する
- ✅ `tableDeviceRegistrationEnabled` と `forceClearPasscode` は `storeMeta/config` で扱う

## 必須実装タスク（すべて完了）

1. ✅ `role: table` 追加に伴う Flutter / Functions / デバイス登録フロー
2. ✅ `devices.optionParams.table_device_table` を使った卓紐付け UI と取得処理
3. ✅ `tables.tournamentDetail` 追加と、既存 tournament callable の更新
4. ✅ `scheduledTournaments/{id}/tablesSeat/{tableId}.isEnabled` による論理削除
5. ✅ `TableDedicatedHomePage` / `TableDeviceDrawer`
6. ✅ `TableDeviceTableDetailPage` / `TableDeviceSideGamePage`
7. ✅ `registerTableToTournament` / `unregisterTableFromTournament`
8. ✅ `registerTableToSideGame` / `unregisterTableFromSideGame`
9. ✅ `storeMeta/config` の卓デバイス設定（registration / forceClearPasscode / 着席・履歴フラグ）
10. ✅ Firestore rules 本番化（`tables` / `sideGame`）と SG 操作の CF 化
11. ✅ 卓ページ TN/SG/置きバケ操作の `role: table` 権限

## 残タスク（本機能の外または運用フェーズ）

- 運用時資料への `tableDevice.*` 追記（`tobe_spec.md` §16）
- 店舗投入時の config 調整（`tournamentSeatAssignmentEnabled` 等）
- ディーラー向け操作マニュアル（カテゴリ F）

## 完了の定義（達成済み）

- ✅ `role / option / tableId / config` の扱いがコードに反映されている
- ✅ 登録から運用までの画面遷移が実装されている
- ✅ 卓端末が何をしてよくて何ができないかが UI と callable の両方で制御されている
- ✅ `tables` / `tablesSeat` / `sideGame` の状態遷移が仕様どおりに揃っている
- ✅ 実機確認計画の実施（A-1、2026-06-25）
