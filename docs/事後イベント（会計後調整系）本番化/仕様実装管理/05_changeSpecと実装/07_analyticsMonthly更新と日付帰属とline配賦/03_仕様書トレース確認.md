# 03_仕様書トレース確認

仕様書 [04_仕様書/07_analyticsMonthly更新と日付帰属とline配賦.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/04_仕様書/07_analyticsMonthly更新と日付帰属とline配賦.md) と上流文書（12 / 15 / 18 / blockC）の各項目を、[02_changeSpec.md](./02_changeSpec.md) と実装で漏れなくカバーしているかをトレースする。

Step07 完了時点で、current-scope の全項目を「完了」に更新済み。テスト結果と実装サマリは [05_実装サマリ.md](./05_実装サマリ.md) / [06_確認結果サマリ.md](./06_確認結果サマリ.md) を参照。

## 1. 仕様書 §5 役割

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §5.1 運用ダッシュボード read model | 既存挙動維持。Step07 では SoT 化しない（changeSpec §1 / §6） | 完了 |
| §5.2 SoT は bills / settlementCycles / baselineSnapshot / adjustments / cashActions | analyticsMonthly schema 変更なし、SoT は touch しない（changeSpec §4.1 / §7.1） | 完了 |
| §5.3 主用途（売上傾向 / 日別推移 / category 別 / user 別 / tournament template 別） | 既存 5 sub-collection を維持 | 完了 |

## 2. 仕様書 §6 3 つの日付軸

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §6.1 売上帰属日 = `bill.businessDate` | adjustment 反映で `bill.businessDate.substring(0, 7)` で月キー（changeSpec §5.2.1 / §5.2.3） | 完了 |
| §6.2 adjustment 確定日 = `adjustment.createdAt` | adjustment doc の `createdAt` field（既存 Step03 で実装済） | 完了 |
| §6.3 cashflow 実行日 = `cashAction.executedAt` / `cashflowBusinessDate` | cashAction doc の field（既存 Step04 で実装済）。Step07 では analytics 反映の月キーには使わない | 完了 |

## 3. 仕様書 §7 analyticsMonthly が使う日付

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §7.1 売上系は `bill.businessDate` ベース | adjustment / cashAction 反映で `bill.businessDate` を月キー / 日キーに使用（changeSpec §5.2.3 / §5.2.4） | 完了 |
| §7.1 補足: `adjustment.createdAt` / `cashAction.executedAt` は audit 軸、`days` 更新キーには使わない | `applyAdjustmentToAnalytics` / `applyCashActionToAnalytics` は `bill.businessDate` のみ受け取る | 完了 |
| §7.2 cashflow 系は cashActions 起点の別責務 | Step07 では `analyticsMonthly` の `cashflow.refundsByMethod` 等は touch しない | 完了 |

## 4. 仕様書 §8 paymentTotals

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §8.1 売上帰属月ベースの受領方法配分 | `applyCashActionToAnalytics` は `bill.businessDate.substring(0, 7)` を monthKey に使用 | 完了 |
| §8.2 「実際にその月に動いた現金」ではない / card 後日入金ではない | Step07 では card 後日入金混入なし | 完了 |
| §8.3 後日回収で売上帰属月の paymentTotals が後から増えることを許容 | collection cashAction が後日実行されても、`bill.businessDate.substring(0, 7)` を monthKey とするため売上帰属月に反映される（changeSpec §3） | 完了 |
| §8.4 refund で paymentTotals を直接減らさない | `applyCashActionToAnalytics` は `cashActionType === 'refund'` で early return（changeSpec §5.2.4 step 1） | 完了 |

## 5. 仕様書 §9 支払手段 source

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §9.1 更新時 source 優先順位 (`/payments` > `meta.paymentMethodsByAmount` > `meta.paymentMethodsByCategory + categoryBreakdown`) | settle 経路の `calculatePaymentTotals` で既に実装済（既存）。Step07 では cashAction `methodBreakdown[]` を直接使う | 完了 |
| §9.2 settle 後 / 参照時 source 優先順位 | Step07 では参照しない（cashAction `methodBreakdown` がそのまま source） | 完了 |

## 6. 仕様書 §10 更新責務

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §10.1 settle / resettle で baseline 売上を analyticsMonthly に反映 | settle marker を `{billId}_cycle{cycleNo}_settle` に変更（changeSpec §4.2 / §5.3.4 / §5.3.5）。reopen 後の resettle で再反映可能化 | 完了 |
| §10.2 adjustment 作成時に売上差分を反映 | `processAdjustmentAnalyticsAtomically` を `createPostSettlementAdjustment` から呼ぶ（changeSpec §5.3.1） | 完了 |
| §10.3 cashAction 実行時に実入出金を反映、collection 系で paymentTotals を増やす | `processCashActionAnalyticsAtomically` を `recordPostSettlementCashAction` から呼ぶ、collection のみ（changeSpec §5.3.2） | 完了 |

## 7. 仕様書 §11 4 パターン matrix

| 仕様書パターン | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| `減額 + 返金済` (`decrease_refunded`): adjustment 売上差分する / 同時 cashflow する / 後続なし / paymentTotals 触らず | adjustment 反映で sales -、refund cashAction 即時 → `applyCashActionToAnalytics` で early return（refund のため）（changeSpec §3） | 完了 |
| `減額 + 返金前` (`decrease_refund_pending`): adjustment 売上差分する / 同時 cashflow しない / 後続 refund で cashflow / paymentTotals 触らず | adjustment 反映で sales -、後続 refund cashAction も `applyCashActionToAnalytics` で early return（changeSpec §3） | 完了 |
| `増額 + 追加徴収済` (`increase_collected`): adjustment 売上差分する / 同時 cashflow する / 後続なし / paymentTotals 即時増 | adjustment 反映で sales +、collection cashAction 即時 → `applyCashActionToAnalytics` で paymentTotals +（changeSpec §3 / §5.3.1） | 完了 |
| `増額 + 追加徴収前` (`increase_collection_pending`): adjustment 売上差分する / 同時 cashflow しない / 後続 collection で cashflow / 回収完了時 paymentTotals 増 | adjustment 反映で sales +、後続 collection cashAction で paymentTotals +（changeSpec §3 / §5.3.2） | 完了 |

## 8. 仕様書 §12 lines[] 粒度

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §12.1 lines[] 必須 | adjustment service `validateLines` で既に保証（既存 Step03） | 完了 |
| §12.2 targetCategory: item / extra / tournament / sideGameChip | adjustment service `LineTargetCategory` で既に保証 | 完了 |
| §12.3 tournament の追加粒度（template / operationType） | adjustment service `validateLines` で targetId と operationType の整合を保証 | 完了 |

## 9. 仕様書 §13 byCategory 配賦

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §13 `item → items` / `extra → extraCost` / `sideGameChip → sideGameChip` / `tournament → tournaments` | `buildAdjustmentAnalyticsDelta` の `byCategory` 集計（changeSpec §5.2.1）、`processAdjustmentAnalyticsAtomically` の `byCategory/summary.totals` 更新（§5.2.3） | 完了 |

## 10. 仕様書 §14 top-level / days 配賦

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §14.1 top-level: `grossSales = 全 line 合計` / 各 category sales = 同 category line 合計 | `buildAdjustmentAnalyticsDelta` で `grossSales` / `byCategory` を集計（changeSpec §5.2.1） | 完了 |
| §14.2 days bucket は `bill.businessDate` 軸、`adjustment.createdAt` ベースの別 day bucket は作らない | `applyAdjustmentToAnalytics` は `bill.businessDate` のみ受け取り、`adjustment.createdAt` は使わない（changeSpec §5.2.3） | 完了 |

## 11. 仕様書 §15 byUser 配賦

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §15-1 `bill.party.userId` 有時のみ更新 | `buildAdjustmentAnalyticsDelta` の `userId` field、`applyAdjustmentToAnalytics` で `byUser` 更新は userId 有時のみ（changeSpec §5.2.1 / §5.2.3） | 完了 |
| §15-2 line ごとに user は変えない | bill の所有 user に category 差分を配賦（changeSpec §5.2.1） | 完了 |
| §15-3 userId が無い bill は byUser 更新しない | 上記実装で保証 | 完了 |

## 12. 仕様書 §16 byTemplateTournaments 配賦

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §16-1 `targetCategory = tournament` のみ対象 | `buildAdjustmentAnalyticsDelta` で tournament line のみ抽出（changeSpec §5.2.1） | 完了 |
| §16-2 `targetId` を template key として使う | `AnalyticsTournamentTemplateAggregate.templateKey` を targetId から（changeSpec §5.2.1） | 完了 |
| §16-3 `operationType` ごとに `entryCount/entrySales` / `reentryCount/reentrySales` / `addonCount/addonSales` 更新 | `buildAdjustmentAnalyticsDelta` で operationType 別集計（changeSpec §5.2.1） | 完了 |
| §16-4 `totalTournamentSales` は tournament line 合計 | `AnalyticsTournamentTemplateAggregate.totalSales` で集計（changeSpec §5.2.1） | 完了 |

## 13. 仕様書 §17 future 機能

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §17 reportingEntries / reportingMonthly / cashflowMonthly / strict 税務 / card 後日入金 / point treatment / product-level analytics / advisor review | Step07 ではすべて touch しない（changeSpec §4.1 / §7.1） | 完了 |

## 14. 仕様書 §18 整合条件

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §18-1 analyticsMonthly は SoT ではない | bills 側 SoT は touch しない（changeSpec §1） | 完了 |
| §18-2 売上系日付軸は `bill.businessDate` を正 | 全 entry point で `bill.businessDate` 経由（changeSpec §5.2.3 / §5.2.4） | 完了 |
| §18-3 `sum(lines[].amountInclDelta) = adjustment.adjustmentAmountIncl` | adjustment service `validateLines` で既に保証 | 完了 |
| §18-4 tournament line は template / operationType を持つ | adjustment service `validateLines` で既に保証 | 完了 |
| §18-5 支払手段 source は現行 bills 実装から派生 | settle 経路は既存ヘルパ、cashAction は `methodBreakdown` 直接 | 完了 |

## 15. 仕様書 §19 不可条件

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §19 analyticsMonthly を current state 正本にしない | bills 側 SoT は touch しない | 完了 |
| §19 card 後日入金 / fee を current-scope に混ぜない | card 後日入金関連 field は新規追加なし | 完了 |
| §19 product-level analytics を今回追加しない | `byCategory/summary.itemSales.{menuItemId}` は touch しない | 完了 |
| §19 generic line だけで category を特定できない adjustment を許可しない | adjustment service `validateLines` で `targetCategory` 必須を既に保証 | 完了 |

## 16. 仕様書 §20 テスト観点

| 仕様書テスト観点 | 実装でのテスト | 状態 |
|---|---|---|
| §20.1 settle 時に baseline 売上が `analyticsMonthly` に反映される | 既存 settle 経路の regression（`__tests__/triggers/billsOnSettle.spec.ts`）+ marker 変更後の挙動確認 | 完了 |
| §20.2 adjustment line に応じて category / days / user / tournament が更新される | `__tests__/analytics/adjustmentDelta.spec.ts` + `__tests__/analytics/applyAdjustmentToAnalytics.spec.ts` + 既存 `createPostSettlementAdjustment.spec.ts` 拡張 | 完了 |
| §20.3 refund で `paymentTotals` を直接減らさない | `__tests__/analytics/cashActionDelta.spec.ts` + `__tests__/analytics/applyCashActionToAnalytics.spec.ts` で refund early return を検証 | 完了 |
| §20.4 collection 完了で `paymentTotals` が増えるパターンが正しく動く | 上記 + 既存 `recordPostSettlementCashAction.spec.ts` 拡張 | 完了 |
| §20.5 複数支払手段入力から受領方法配分を復元できる | settle 経路の既存挙動（`calculatePaymentTotals`）の regression のみ | 完了 |

## 17. 上流 [12_analyticsMonthlyと入出金データの役割分担.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.11_未決論点の再決定/12_analyticsMonthlyと入出金データの役割分担.md)

| 上流条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §1 スコープ（運用ダッシュボード read model 維持 / cashActions 別責務 / 現行 source / bills 派生） | Step07 は仕様書 §1〜§20 と同じく Step07 changeSpec で実装 | 完了 |
| §3 既存 top-level field 維持 | analyticsMonthly schema 変更なし | 完了 |
| §4.1 paymentTotals が後から動くケース | 5.3.1 / 5.3.2 で実装 | 完了 |
| §4.2 paymentTotals 直接減らさず | 5.3.2 で `cashActionType === 'refund'` early return | 完了 |
| §6 実入出金は別責務 | cashflow 系 fields は touch しない | 完了 |
| §8 baseline 入力 / 売上差分入力 / 実入出金入力 の役割分担 | settle / adjustment / cashAction の各経路で実装 | 完了 |
| §9 4 パターンごとの想定 | §11 matrix と同じ | 完了 |
| §10 currentSettlementCycle と latestSettledCycle | settle marker key を `{billId}_cycle{cycleNo}_settle` に変更（changeSpec §4.2） | 完了 |
| §11 同一 cycle 内 sequence | adjustment / cashAction の `sequenceNo` は既存実装で保証 | 完了 |
| §13 運用 read model | analyticsMonthly は分析用 read model として維持 | 完了 |

## 18. 上流 [15_売上日入出金日営業日の帰属ルール.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.11_未決論点の再決定/15_売上日入出金日営業日の帰属ルール.md)

| 上流条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §3 売上発生日 = `bill.businessDate` | 全 entry point で `bill.businessDate` 経由 | 完了 |
| §5 adjustment 確定日 = `adjustment.createdAt` | adjustment doc の `createdAt`（既存 Step03） | 完了 |
| §6 cashflow 実行日 = `cashAction.executedAt` / `cashflowBusinessDate` | cashAction doc の field（既存 Step04） | 完了 |
| §7.1 analyticsMonthly 売上系は `bill.businessDate` ベース | 5.3.1 で `bill.businessDate.substring(0, 7)` を月キーに | 完了 |
| §7.2 実入出金は別責務 | analyticsMonthly の cashflow 系 fields は touch しない | 完了 |
| §8 4 パターンと日付軸 | adjustment 確定日と売上帰属日を分離（売上帰属で analytics 更新） | 完了 |
| §9 reopen と日付軸 | reopen 自体は `bill.businessDate` を変更しない、analytics rollback は旧 cycle 寄与のみ | 完了 |

## 19. 上流 [18_売上差分明細の粒度と配賦ルール.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.11_未決論点の再決定/18_売上差分明細の粒度と配賦ルール.md)

| 上流条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §1 current-scope の analytics 軸（top-level / days / byCategory / byUser / byTemplateTournaments） | 5 sub-collection で実装、product-level は対象外 | 完了 |
| §2 lines[] 必須 | adjustment service で既に保証 | 完了 |
| §3 line 基本 field | adjustment service で既に保証 | 完了 |
| §4 `sum(lines[].amountInclDelta) = adjustmentAmountIncl` | adjustment service `validateLines` | 完了 |
| §5 targetCategory: item / extra / tournament / sideGameChip | adjustment service で既に保証 | 完了 |
| §6 category ごとの粒度（item / extra / sideGameChip / tournament template+operationType） | adjustment service で既に保証 | 完了 |
| §8 byCategory 配賦 | 5.2.3 で実装 | 完了 |
| §9 monthly index / daily summary 配賦 | 5.2.3 で実装 | 完了 |
| §10 byUser 配賦 | 5.2.3 で実装、userId 有時のみ | 完了 |
| §11 byTemplateTournaments 配賦 | 5.2.3 で実装、tournament line のみ、operationType 別 | 完了 |
| §12 line 不足は許可しない | adjustment service `validateLines` で既に保証 | 完了 |

## 20. 上流 [01_決定事項総覧.md (blockC)](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.12_全体整合性確認/blockC_集計と日付軸/01_決定事項総覧.md)

| 上流条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| 集計と日付軸の整合 | settle / adjustment / cashAction を `bill.businessDate` ベースで統一 | 完了 |

## 21. リスクと後方互換性

| 項目 | 確認 | 状態 |
|---|---|---|
| settle marker key 変更で既存挙動と衝突しないこと | legacy `{billId}` marker と新 `{billId}_cycle{cycleNo}_settle` marker の共存（changeSpec §4.3） | 完了 |
| flag = false 環境では analytics 更新を完全 skip | changeSpec §5.6 / §7.2 | 完了 |
| 既存 callable signature 変更なし | response shape 維持（changeSpec §9） | 完了 |
| Flutter 側変更なし | UI 影響なし | 完了 |
