# 04_確認観点と確認方法

## 1. 自動テスト観点

| 観点 | 確認方法 | 実行コマンド | 期待結果 | 現時点結果 |
|---|---|---|---|
| bill 作成時に target 初期 field が入る | helper / repo test 更新 | `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/helpers/billsApi/createBillWithActiveStay.spec.ts'` | `status=open` と parent 初期 summary が入る | PASS |
| 会計開始で `ops.accountingStarted*` と draft 支払入力が更新される | helper / callable test 更新 | `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/callables/accounting.spec.ts'` | `status=settling` と該当 field 更新 | PASS |
| 会計取消で `ops.accountingCanceled*` と `status=open` が更新される | callable test 更新 | `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/callables/cancelAccounting.spec.ts'` | target field が正しく戻る | PASS |
| settle 後に `settlementSnapshot/currentSummary/postSettlementState` が生成される | trigger test 更新 | `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/triggers/bills.onSettle.spec.ts'` | root parent summary が target shape になる | PASS |
| close 処理で `closeSummary.*` が付与される | close_process test 更新 | `npm --prefix functions test -- __tests__/close_process/step3.spec.ts --runInBand` | `closeSummary.unresolved=true` 等を確認 | PASS |

## 2. 手動確認観点

| 観点 | 操作手順 | 期待結果 | 現時点結果 |
|---|---|---|
| 未会計一覧が `closeSummary` ベースで読める | 閉店未会計 bill を作り UI を開く | 対象 card が正しく表示される | Step06で確認 |
| 会計開始後の bill 詳細 / 一覧表示 | 通常会計開始を行う | `settling` と必要 summary が UI で破綻しない | Step06で確認 |
| 会計完了後の表示 | 会計完了後に一覧・履歴を開く | target parent summary と整合した表示になる | Step06で確認 |

## 3. 目視レビュー観点

| 観点 | 確認対象 | 期待結果 | 現時点結果 |
|---|---|---|
| 旧 `postEvents` 参照の残存 | `functions/src/domains/bills`, `lib/Accounting`, `lib/Home` | Step01 範囲で置換対象と非対象が整理されている | 暫定共存で許容。段階削除はStep03-07 |
| 旧 `closeSnapshot` 参照の残存 | close process / UI | 後続 step へ回す箇所が明示されている | close process は互換維持、UI切替はStep06 |
| `status` 旧値の残存 | bills domain / UI | current-scope 契約と矛盾する残存箇所が把握できている | bills domain は概ね整備済み、UIはStep06対象 |

## 4. テストファイル作成 / 更新方針

- 新規テスト追加よりも、まず既存 bills / close_process / trigger tests を更新する方針が自然
- 主更新対象:
  - `functions/__tests__/helpers/billsApi/createBillWithActiveStay.spec.ts`
  - `functions/__tests__/callables/accounting.spec.ts`
  - `functions/__tests__/callables/cancelAccounting.spec.ts`
  - `functions/__tests__/triggers/bills.onSettle.spec.ts`
  - `functions/__tests__/close_process/step3.spec.ts`
- UI はこのステップでは自動テストより手動確認寄りになる可能性が高い

## 5. 実行前チェック

- [x] 自動テスト観点を定義した
- [x] 手動確認観点を定義した
- [x] 目視確認観点を定義した
- [x] 実装対象ファイルに応じたテスト更新順を確定した
- [x] 実行環境（Firestore Emulator の要否）を最終確認した

## 6. 実行メモ

- `npm --prefix functions run build`: 成功
- `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/callables/accounting.spec.ts'`: 成功
- `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/callables/cancelAccounting.spec.ts'`: 成功
- `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/triggers/bills.onSettle.spec.ts'`: 成功
- `firebase emulators:exec --only firestore 'cd functions && npm test -- --runInBand __tests__/helpers/billsApi/createBillWithActiveStay.spec.ts'`: 成功
- `npm --prefix functions test -- __tests__/close_process/step3.spec.ts --runInBand`: 成功
- `ts-jest` の `globals` 非推奨 warning は出るが、今回の失敗原因ではない
