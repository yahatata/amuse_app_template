# 03_仕様書トレース確認

## 1. 対応対象

- [03_adjustments管理.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/04_仕様書/03_adjustments管理.md)

## 2. トレース表

| 仕様項目（仕様書の節） | 実装箇所（予定 / 実施） | 確認方法 | 状態 |
|---|---|---|---|
| §6 保存先 `bills/{billId}/settlementCycles/{cycleNo}/adjustments/{adjustmentId}` | `repos/createPostSettlementAdjustment.ts`, `services/adjustments.ts` (`buildAdjustmentDoc`) | unit `adjustments.spec.ts`, Emulator `createPostSettlementAdjustment.spec.ts`, 実機確認 1 | 完了 |
| §7 必須 field（`sequenceNo` / `adjustmentType` / `adjustmentDirection` / `adjustmentAmountIncl` / `cashActionTypeAtCreation` / `cashActionHandledAtCreation` / `adjustmentState` / `requiredActionRemainingIncl` / `createdAt` / `createdBy` / `note` / `lines[]` / `supersededByAdjustmentId`） | `services/adjustments.ts` (`buildAdjustmentDoc`) | 同上 + field 検証 assertion | 完了 |
| §8 `adjustmentType` 4 値 | `services/adjustments.ts` (`validateAdjustmentInput`) | unit テスト（4 値 / 不正値） | 完了 |
| §9 `adjustmentDirection` 2 値 | 同上 | 同上 | 完了 |
| §10 `cashActionTypeAtCreation` 3 値 | 同上 | 同上 | 完了 |
| §11 `adjustmentState` 4 値 | `services/adjustments.ts` (`buildAdjustmentDoc` で初期 `effective`、offset で `completed_by_offset`、immediate で `completed_by_cash_action`) | unit + Emulator | 完了 |
| §11 `cancelled_by_reopen` への遷移 | Step05 主責務 | Step05 で確認 | 後続 |
| §12 `requiredActionRemainingIncl` 初期値ルール（pending=adjustmentAmountIncl, immediate=最終 0） | `services/adjustments.ts` (`buildAdjustmentDoc`) + immediate cashAction 適用 | unit + Emulator 4 パターン | 完了 |
| §13 `lines[]` 必須 + 必須 field | `services/adjustments.ts` (`validateLines`) | unit テスト（不足 / 符号不一致 / 合計不一致） | 完了 |
| §13.4 tournament line の `targetId / operationType` 必須 | `services/adjustments.ts` (`validateLines`) | unit テスト | 完了 |
| §13.5 `sum(lines[].amountInclDelta) = adjustmentAmountIncl` / 符号一致 | 同上 | 同上 | 完了 |
| §14.1 `減額 + 返金前` 保存ルール | `services/adjustments.ts` + repo | Emulator 統合 | 完了 |
| §14.2 `減額 + 返金済` （adjustment + 同一トランザクション cashAction） | repo + `services/cashActions.ts` (`buildImmediateCashActionDoc`) | Emulator 統合 | 完了 |
| §14.3 `増額 + 追加徴収前` 保存ルール | 同上 | 同上 | 完了 |
| §14.4 `増額 + 追加徴収済` （adjustment + 同一トランザクション cashAction） | 同上 | 同上 | 完了 |
| §15 opposite-direction 内部相殺（sequenceNo 順 / `completed_by_offset`） | `services/adjustments.ts` (`applyOppositeDirectionOffset`) | unit + Emulator 統合 | 完了 |
| §15.4 具体例（refund 1000 + collection 1500 → collection 残 500） | 同上 | unit + Emulator | 完了 |
| §16.1 parent 反映（`currentSummary.claimTotalIncl/netSalesIncl` / `postSettlementState.totalAdjustmentsIncl/requiredActionType/requiredActionIncl` / `lastRecordType`）+ status 遷移 | `services/parentSummary.ts` (`buildPostSettlementStateAfterAdjustment` / `buildCurrentSummaryAfterAdjustment` / `deriveStatusAfterAdjustment`) + repo | unit + Emulator | 完了 |
| §16.2 parent 要対応再計算（remaining 合計から派生） | `services/adjustments.ts` (`summarizeRemainingByDirection`) + parentSummary helper | unit + Emulator | 完了 |
| §16.3 同時両残りを許可しない不変則 | repo 内で `summarizeRemainingByDirection` 後に assertion | unit + Emulator（不正系を弾く） | 完了 |
| §16.4 parent には差額だけ持たせる | parentSummary helper 経由で 1 方向のみ書く | Emulator | 完了 |
| §17 `sequenceNo` 採番 (`nextSequenceNo` から取得) | `repos/createPostSettlementAdjustment.ts` 内 transaction | unit + Emulator | 完了 |
| §18.1 line-less 不可 | `validateLines` | unit | 完了 |
| §18.2 amount 不一致不可 | `validateLines` | unit | 完了 |
| §18.3 tournament line の field 不足不可 | `validateLines` | unit | 完了 |
| §18.4 両残額の同時 parent 反映不可 | `summarizeRemainingByDirection` + parentSummary helper | unit + Emulator | 完了 |
| §19.1 4 パターンごとの `adjustmentType` と初期 remaining | Emulator | 4 ケース別 it() | 完了 |
| §19.2 immediate で remaining=0 | Emulator | 同上 | 完了 |
| §19.3 line 合計検証 | unit | unit | 完了 |
| §19.4 opposite-direction 相殺で差額のみ残る | unit + Emulator | 同上 | 完了 |
| §19.5 parent `requiredActionType / requiredActionIncl` が差額に一致 | unit + Emulator | 同上 | 完了 |
| §19.6 tournament line が `byTemplateTournaments` 更新に必要な情報を持つ | unit（line 構造の field 検証）+ Emulator（tournament line を含む adjustment 保存テスト） | line 構造検証 | 完了（analytics 反映本接続は Step07） |

## 3. Step03 で意図的に扱っていないもの

- `cancelled_by_reopen` への遷移（Step05 主責務）
- cashAction の later パターン（`cashActions` 単独作成）と field 拡張・full validation（Step04 主責務）
- `cashflowBusinessDate` の `calcBusinessDate` AMBIGUOUS / NONE 処理（Step04 主責務）
- `analyticsMonthly` への line 配賦本接続（Step07 主責務）
- UI 接続 / 旧 callable からの差し替え（Step06 主責務）
- `supersededByAdjustmentId` の運用本実装（adjustment 訂正フロー、後続 step）
- 旧 `bills/{billId}/events` 経路の改修・廃止
- migration / backfill

これらは仕様書 §3 と Step02 handoff の境界に従い Step03 完了条件には含めない。

## 4. 完了判定の前提

- 上記トレース表で「着手中」と書かれた項目は、実装・テスト完了時点で「完了」へ更新する
- 「後続」項目は本ステップでは確認対象外とする
- 仕様書 §19 のテスト観点は全て unit / Emulator のいずれかで覆われていることを `06_確認結果サマリ.md` で再確認する
