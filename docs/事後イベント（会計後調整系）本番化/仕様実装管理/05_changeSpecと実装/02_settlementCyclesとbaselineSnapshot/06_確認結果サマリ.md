# 06_確認結果サマリ

## 1. 実行した確認

### 1.1 build

- コマンド: `npm --prefix functions run build`
- 結果: 成功

### 1.2 createBillWithActiveStay

- コマンド:

```bash
firebase emulators:exec --only firestore "npm --prefix functions test -- __tests__/helpers/billsApi/createBillWithActiveStay.spec.ts --runInBand"
```

- 結果: 成功

### 1.3 bills.onSettle

- コマンド:

```bash
firebase emulators:exec --only firestore "npm --prefix functions test -- __tests__/triggers/bills.onSettle.spec.ts --runInBand"
```

- 結果: 成功

### 1.4 エミュレータ再試行確認

- 直接 `npx jest ...` を使った初回実行では、workspace 外の Jest 解決によりバージョン不一致エラーが発生した
- `npm --prefix functions test -- ...` に切り替えて Firestore Emulator 下で再実行し、正常終了を確認した
- 判定: エミュレータ再試行ルール込みで確認完了

## 2. 補足

- `ts-jest` の deprecation warning は出るが、今回の Step02 判定を妨げるものではない
- `Jest did not exit one second after the test run has completed` の警告が出る場合があるため、後続 step でも open handle の有無は継続監視する

## 3. Step02 の判定

- create / settle パスにおける current-scope は完了
- Step02 単体の残タスクはなし
- `reopen` 実行系は仕様どおり Step05 へ送る
