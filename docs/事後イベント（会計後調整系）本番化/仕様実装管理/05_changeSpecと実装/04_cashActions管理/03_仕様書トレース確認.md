# 03_仕様書トレース確認

## 1. 対応対象

- [04_cashActions管理.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/04_仕様書/04_cashActions管理.md)

## 2. トレース表

| 仕様項目（仕様書の節） | 実装箇所（予定 / 実施） | 確認方法 | 状態 |
|---|---|---|---|
| §5.2 保存先 `bills/{billId}/settlementCycles/{cycleNo}/cashActions/{cashActionId}` | `repos/recordPostSettlementCashAction.ts`、`services/cashActions.ts` (`buildCashActionDoc`)、`repos/createPostSettlementAdjustment.ts`（immediate） | unit `cashActions.spec.ts`、Emulator `recordPostSettlementRefund.spec.ts` / `recordPostSettlementCollection.spec.ts` / `createPostSettlementAdjustment.spec.ts`、実機確認 | 完了 |
| §5.3 cycle 配下に置く（adjustment 配下に置かない） | 同上、`recordPostSettlementCashAction` で `cycleRef.collection('cashActions')` を参照 | Emulator | 完了 |
| §6 必須 field（`sequenceNo` / `cashActionType` / `amountIncl` / `executedAt` / `executedBy` / `cashflowBusinessDate` / `methodBreakdown[]` / `allocations[]` / `note`） | `services/cashActions.ts` (`buildCashActionDoc`) | unit + Emulator | 完了 |
| §7 `cashActionType` 値域 (`refund` / `collection`) | `buildCashActionDoc` の type guard、callable 名で固定 | unit + Emulator | 完了 |
| §8 `methodBreakdown[]` 役割 / 必須 field (`method` / `amountIncl`) | `services/cashActions.ts` (`validateMethodBreakdown`) | unit テスト（multi method / sum 不一致 / method 文字列不正） | 完了 |
| §8.3 `method` の値は現行実装と整合する範囲で使う | callable レベルでの enum 制約は付けず、string 受け入れ。strict 検査は future。Step04 では `cash` / `credit_card` / `electronic_money` / `qr` / `bank_transfer` 等の現行実装値を unit で確認 | unit | 完了 |
| §9.1 `allocations[]` 役割（cashAction が adjustment をどれだけ解消したか） | `services/cashActions.ts` (`buildCashActionDoc`) + `applyAllocationsToAdjustments` | unit + Emulator | 完了 |
| §9.2 `allocations[]` 必須化 | `validateAllocations` の `length >= 1` | unit | 完了 |
| §9.3 `allocations[]` 必須 field (`adjustmentId` / `amountIncl`) | `buildCashActionDoc` 内検証 + `validateAllocations` | unit | 完了 |
| §9.4.1 `allocations` 1 件以上必須 | `validateAllocations` | unit | 完了 |
| §9.4.2 `cashAction.amountIncl = sum(allocations[].amountIncl)` | 同上 | unit | 完了 |
| §9.4.3 allocation 先 adjustment は同一 cycle | `validateAllocations` の `expectedCycleNo` チェック + repo 内で current cycle 配下の adjustment のみ read | unit + Emulator | 完了 |
| §9.4.4 allocation 先 adjustment は `requiredActionRemainingIncl > 0` を持つ | `validateAllocations` で `effective` && `remaining > 0` チェック | unit + Emulator | 完了 |
| §9.4.5 over-allocation 禁止 | `validateAllocations` で `amountIncl <= remaining` チェック | unit + Emulator | 完了 |
| §10 adjustment 残額更新ルール（remaining -= allocation.amountIncl、0 なら `completed_by_cash_action`） | `services/cashActions.ts` (`applyAllocationsToAdjustments`) + repo の transaction patch | unit + Emulator | 完了 |
| §10.2 具体例（adj_2 collection 残 500 → cashAction 500 で完全解消） | `recordPostSettlementCollection.spec.ts` の happy path | Emulator | 完了 |
| §11.1 immediate patterns（adjustment と同一 transaction） | `repos/createPostSettlementAdjustment.ts` の immediate 経路（Step04 で統合） | Emulator `createPostSettlementAdjustment.spec.ts` | 完了 |
| §11.2 later patterns（後続操作で cashAction を作る） | `repos/recordPostSettlementCashAction.ts` + `callables/recordPostSettlementRefund` / `recordPostSettlementCollection` | Emulator | 完了 |
| §12.1 refund: `currentSummary.refundedTotalIncl` 増 / `postSettlementState.totalRefundedIncl` 増 / `requiredActionType / requiredActionIncl` 再計算 | `services/parentSummary.ts` (`buildPostSettlementStateAfterCashAction` / `buildCurrentSummaryAfterCashAction`) + repo | unit + Emulator | 完了 |
| §12.2 collection: `currentSummary.receivedTotalIncl` 増 / `postSettlementState.totalCollectedIncl` 増 | 同上 | unit + Emulator | 完了 |
| §12.3 `lastRecordType = cash_action` / `lastRecordAt = executedAt` / `lastRecordId = cashActionId` / 未解消なしで `status = settled` | `buildPostSettlementStateAfterCashAction` + `deriveStatusAfterCashAction` + repo の bill update + Step03 immediate 経路の Step04 統合 | unit + Emulator | 完了 |
| §13 `analyticsMonthly` との接続 | Step07 主責務 | Step07 で確認 | 後続 |
| §14 `sequenceNo` 採番（adjustment と同一カウンタ） | `repos/recordPostSettlementCashAction.ts` 内 transaction で `cycle.nextSequenceNo` を消費 | Emulator | 完了 |
| §15.1 `allocations[]` なしの cashAction を作らない | `validateAllocations` | unit | 完了 |
| §15.2 `cashAction.amountIncl` と allocation 合計がズレたまま保存しない | 同上 | unit | 完了 |
| §15.3 異なる cycle の adjustment を 1 つの cashAction に混在させない | 同上、repo 内で current cycle 配下のみ read | unit + Emulator | 完了 |
| §15.4 remaining を超える over-allocation を許可しない | `validateAllocations` | unit + Emulator | 完了 |
| §16.1 immediate cashAction 作成時に adjustment remaining が 0 になる | `createPostSettlementAdjustment.ts` の immediate 経路で `requiredActionRemainingIncl = 0` / `adjustmentState = completed_by_cash_action` | Emulator | 完了 |
| §16.2 later cashAction 作成時に allocated adjustment の remaining が正しく減る | `applyAllocationsToAdjustments` + repo の patch 適用 | Emulator | 完了 |
| §16.3 allocation 合計と amount が一致しない場合は弾かれる | `validateAllocations` | unit + Emulator | 完了 |
| §16.4 multi-allocation cashAction が作成できる | `recordPostSettlementRefund.spec.ts` の multi allocation ケース | Emulator | 完了 |
| §16.5 parent summary が refund / collection で正しく更新される | parent 派生 helper + repo | unit + Emulator | 完了 |

## 3. Step04 で意図的に扱っていないもの

- `analyticsMonthly` への `paymentTotals` / `cashflow` 反映（Step07 主責務）
- `getRefundHistory` の本実装（Step06 主責務）
- 旧経路（`postEventRefund` / `processRefund` / `updateAccounting`）の改修・廃止
- UI 接続（旧 callable から新 callable への差し替え。Step06 主責務）
- `cancelled_by_reopen` 遷移時の cashAction 扱い（Step05 主責務）
- card 後日入金 / fee の厳密管理（future）
- 自社 / 他社 point treatment の厳密判定（future）
- migration / backfill

これらは仕様書 §3 と Step03 handoff の境界に従い Step04 完了条件には含めない。

## 4. 完了判定の前提

- 上記トレース表で当初「着手中」と置いた項目は、実装・テスト完了時点で「完了」へ更新済み（本ファイルにて反映済）
- 「後続」項目は本ステップでは確認対象外とする
- 仕様書 §16 のテスト観点は全て unit / Emulator のいずれかで覆われていることを `06_確認結果サマリ.md` で再確認する
- Step03 で書いた `createPostSettlementAdjustment.spec.ts` の immediate ケースで `lastRecordType` 期待値を `'cash_action'` に更新したことを確認する
