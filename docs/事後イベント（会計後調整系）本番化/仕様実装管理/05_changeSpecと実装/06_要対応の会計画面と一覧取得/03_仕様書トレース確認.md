# 03_仕様書トレース確認

仕様書 [04_仕様書/06_要対応の会計画面と一覧取得.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/04_仕様書/06_要対応の会計画面と一覧取得.md) と上流文書の各項目を、[02_changeSpec.md](./02_changeSpec.md) と実装で漏れなくカバーしているかをトレースする。

## 1. 仕様書 §6 入口

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §6.1 メニュー名 `要対応の会計` | `terminalHomePage.dart` L1490 label 変更（changeSpec §3.1.3 / §5.6.1） | 完了 |

## 2. 仕様書 §7 画面構造

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §7.1 表示軸 `日付ごと` / `ユーザー別` | タブ 2 つ（changeSpec §5.3.3 / §5.3.4 / §5.3.5） | 完了 |
| §7.2 フィルタ `すべて` / `未会計` / `追加徴収` / `要返金`、デフォルト `すべて` | 上部フィルタ chips（changeSpec §5.3.2） | 完了 |

## 3. 仕様書 §8 内部カード種別

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §8 内部カード種別 3 種 | `BillCardType` enum 3 値（changeSpec §5.1.1） | 完了 |
| §8.1 画面ラベル `未会計` / `追加徴収` / `要返金` | `computeDisplayLabel`（changeSpec §5.1.6） | 完了 |

## 4. 仕様書 §9 判定条件

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §9.1 `carryover_unsettled` = `status=open` + `closeSummary.unresolved=true` | query 1（changeSpec §5.2.1）+ `classifyBill`（§5.1.2） | 完了 |
| §9.2 `post_settlement_collection_pending` = `status=post_settlement_pending` + `requiredActionType=collection` + `requiredActionIncl > 0` | query 2 + `classifyBill` で `incl > 0` filter | 完了 |
| §9.3 `post_settlement_refund_pending` = `status=post_settlement_pending` + `requiredActionType=refund` + `requiredActionIncl > 0` | query 3 + `classifyBill` で `incl > 0` filter | 完了 |
| §9.4 `すべて` = 3 種の和集合 | フィルタ `すべて` 選択時に 3 streams 全件マージ | 完了 |

## 5. 仕様書 §10 日付ごと表示

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §10.1 カテゴリフィルタ適用後の bill を日付単位で一覧 | `requireSpecialAttentionPage.dart` タブ1 で grouping by `sortDate` | 完了 |
| §10.2 日付キー: `carryover_unsettled` は `closeSummary.closedBusinessDate` 優先 / 取れない場合 `bill.businessDate` fallback / 会計後要対応は `bill.businessDate` | `computeSortDate`（changeSpec §5.1.3） | 完了 |
| §10.3 `sortDate` は §10.2 を使う | view model 構築時に `computeSortDate` を呼ぶ | 完了 |
| §10.4 例（`日付ごと + すべて` / `日付ごと + 未会計` 等） | フィルタ + 表示軸の組合せ（changeSpec §5.3.4） | 完了 |

## 6. 仕様書 §11 ユーザー別表示

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §11.1 カテゴリフィルタ適用後のユーザー一覧 | タブ2 ユーザー別 grouping（changeSpec §5.3.5） | 完了 |
| §11.2 ユーザーカード必須件数 `総件数` / `未会計 x件` / `追加徴収 x件` / `要返金 x件` | `UserAttentionCounts.from` で集計表示（changeSpec §5.5） | 完了 |
| §11.3 フィルタ適用中でも要対応件数内訳をカード表示 | カード本体は filter 適用前の全件、tap 後の bill 一覧は filter 適用後（changeSpec §5.5） | 完了 |

## 7. 仕様書 §12 共通 view model

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §12 11 fields (`billId` / `cardType` / `displayLabel` / `businessDate` / `displayTitle` / `displayAmountIncl` / `primaryActionType` / `sortDate` / `badgeText` / `userId` / `userDisplayName`) | `BillRequireAttentionViewModel` dataclass（changeSpec §5.1.1） | 完了 |
| §12.1 `displayAmountIncl`: `carryover` は `closeSummary.displayAmountAtMark` / `collection`/`refund` は `requiredActionIncl` | `computeDisplayAmountIncl`（changeSpec §5.1.4） | 完了 |
| §12.2 `primaryActionType`: 各 cardType 対応 | `computePrimaryAction`（changeSpec §5.1.5） | 完了 |
| §12.3 `businessDate` は `bill.businessDate`、`sortDate` は §10.2 | dataclass field と `computeSortDate` の使い分け | 完了 |

## 8. 仕様書 §13 一覧取得方式

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §13 種別ごとに別 query / 近い条件 1 query 許容 | 種別ごとに 3 query + client merge（changeSpec §5.2） | 完了 |
| §13 判定条件は仕様書を正とする | `classifyBill` は §9 を厳格に実装 | 完了 |

## 9. 仕様書 §14 整合条件

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §14.1 `未会計` は carryover unsettled のみ | `classifyBill` で status=`open` + `closeSummary.unresolved` のみ判定 | 完了 |
| §14.2 `追加徴収` は parent `requiredActionType=collection` のみ | `classifyBill` で type=`collection` のみ判定 | 完了 |
| §14.3 `要返金` は parent `requiredActionType=refund` のみ | `classifyBill` で type=`refund` のみ判定 | 完了 |
| §14.4 同じ bill が複数 cardType に分類されない | `classifyBill` の if/return 順序で保証（status で先に分岐、`refund` か `collection` は同 status の中で type 排他） | 完了 |

## 10. 仕様書 §15 不可条件

| 仕様書条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §15 要対応一覧都合で新しい共通正本 collection 作らない | 新規 collection なし（changeSpec §7.1） | 完了 |
| §15 旧 `partially_paid` 前提に判定しない | `partially_paid` は判定式に含まない | 完了 |
| §15 持ち越し未会計と会計後要対応を同一 lifecycle にしない | 内部 `BillCardType` で分離、保存構造は touch しない | 完了 |

## 11. 仕様書 §16 テスト観点

| 仕様書テスト観点 | 実装でのテスト | 状態 |
|---|---|---|
| §16.1 `status=open + closeSummary.unresolved=true` が `未会計` に出る | view model unit test + 画面 widget test（`bill_require_attention_view_model_test.dart` + `require_special_attention_page_basic_test.dart`） | 完了（テスト実装済） |
| §16.2 `post_settlement_pending + collection` が `追加徴収` に出る | 同上 | 完了（テスト実装済） |
| §16.3 `post_settlement_pending + refund` が `要返金` に出る | 同上 | 完了（テスト実装済） |
| §16.4 ユーザー別カードに件数内訳が出る | `UserAttentionCounts` unit test + widget test（`user_attention_counts_test.dart` + `require_special_attention_page_basic_test.dart §16.4`） | 完了（テスト実装済） |
| §16.5 各カードから正しい導線に遷移する | view model `primaryActionType` の widget test（`require_special_attention_page_basic_test.dart §16.5`）+ dialog コードレベル確認（`06_確認結果サマリ.md §3` の代替検証） | 完了（テスト実装済 + コードレビュー） |

## 12. 上流 [16_未会計一部未徴収会計後イベントの接続方針.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.11_未決論点の再決定/16_未会計一部未徴収会計後イベントの接続方針.md) §1〜§14

| 上流条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §1 対象 2 系統（持ち越し未会計 + 会計後要対応） | cardType 3 種で対応（carryover + collection + refund） | 完了 |
| §2 保存構造は分離維持 | 新 collection / 新 status 追加なし | 完了 |
| §3 `bills` と整合性を保つ | 既存 parent doc field のみで判定（changeSpec §5.1.2） | 完了 |
| §4 同じ画面で扱う | `RequireSpecialAttentionPage` 1 画面 | 完了 |
| §5 表示軸 + フィルタ | changeSpec §5.3.2 / §5.3.3 | 完了 |
| §6 内部カード種別 3 種 + 判定条件 | changeSpec §5.1.2 | 完了 |
| §7 日付ごと表示 | changeSpec §5.3.4 | 完了 |
| §8 ユーザー別表示 | changeSpec §5.3.5 | 完了 |
| §9 ユーザーカード件数内訳 | changeSpec §5.5 | 完了 |
| §10 共通画面の読み取りモデル 11 fields + §10.1〜§10.3 | `BillRequireAttentionViewModel` + 各 compute 関数 | 完了 |
| §11 一覧取得は分離してよい | 3 query + client merge | 完了 |
| §12 詳細画面・導線は分ける | primary action 3 種 + 各 dialog（changeSpec §5.4） | 完了 |
| §13 current-scope で追加しないもの（共通 collection / 共通 status / `attentionType` 親 field / 同一 lifecycle） | すべて回避（changeSpec §7.1 / §10） | 完了 |

## 13. 上流 [01_決定事項総覧.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.12_全体整合性確認/blockA_業務と画面/01_決定事項総覧.md)

| 上流条件 | changeSpec / 実装での対応 | 状態 |
|---|---|---|
| §2.3〜§2.6 画面構成と判定 | changeSpec § 5 全体で実装 | 完了 |
| §2.7 同じ画面で扱うが導線は分ける | primary action 3 種別の dialog 切替 | 完了 |
| §2.8 ユーザー別件数要件 | `UserAttentionCounts` | 完了 |

## 14. 上流 [03_代表シナリオ.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03.1_前提再設計/step3.12_全体整合性確認/blockA_業務と画面/03_代表シナリオ.md)

| シナリオ | 期待動作 | 実装での対応 | 状態 |
|---|---|---|---|
| シナリオA: `日付ごと + すべて` | 日付の要対応 bill 全件、各カードに `未会計` / `追加徴収` / `要返金` ラベル | view model + フィルタ + 日付タブ | 完了 |
| シナリオB: `日付ごと + 未会計` | 持ち越し未会計のみ、`会計を再開する` ボタン | フィルタ `未会計` + `resumeAccounting` | 完了 |
| シナリオC: `ユーザー別 + 追加徴収` | 追加徴収対象ユーザー一覧、件数内訳 | フィルタ + ユーザー別タブ + `UserAttentionCounts` | 完了 |
| シナリオD: `ユーザー別 + 要返金` | 要返金 bill ユーザー一覧、件数内訳 | 同上 | 完了 |
| シナリオE: reopen 後 | reopen 後の bill は `status=open` で `closeSummary.unresolved` が無い → 要対応一覧に出ない、通常未会計導線へ | `classifyBill` の判定条件で自然に除外（reopen 後は `closeSummary.unresolved=true` にならない） | 完了 |

## 15. リスク条件のトレース

| changeSpec §9 リスク | 対策 | 状態 |
|---|---|---|
| §9.1 import 切れ | rename と import 更新を同 commit | 完了 |
| §9.2 firestore.indexes.json deploy | equality 2 条件のみ + orderBy / 不等式なしのため新規 composite index 不要と判定（`05_実装サマリ.md §2.4`）。実機で `failed-precondition: query requires an index` が出た場合は `08_実機確認手順.md §9` の対処に従い追加 deploy | 完了 |
| §9.3 stream merge race | `Map<String, BillVM>` で merge | 完了 |
| §9.4 dialog callable エラー UI | try/catch + finally + SnackBar、`flutter-loading-display.mdc` 準拠 | 完了 |
| §9.5 旧画面 / 旧 callable 混在 | 影響範囲明確化、`07_後続ステップへの伝達事項.md` で future 廃止記載 | 完了 |

## 16. 後方互換性チェック

| changeSpec §10 後方互換性 | 確認 | 状態 |
|---|---|---|
| 旧 callable 未変更 | `git diff` で `functions/src/**` に Step06 起因の変更がないことを確認（`06_確認結果サマリ.md §6`） | 完了（実装後確認済） |
| 既存 dialog 未変更 | `git diff` で `lib/Accounting/refundProcessingDialog.dart` 等の旧 dialog に変更がないことを確認（`06_確認結果サマリ.md §6`） | 完了（実装後確認済） |
| `users.unsettledBillsCount` counter 未変更 | applyCloseSnapshot.ts / finalizeUnsettledBillAfterAccounting.ts に touch しない | 完了 |
| `bills` 親 doc field 未変更 | functions/src 配下に変更なし（読み取りのみ） | 完了 |
| `closeSnapshot` dual-write 維持 | applyCloseSnapshot.ts に touch しない | 完了 |
