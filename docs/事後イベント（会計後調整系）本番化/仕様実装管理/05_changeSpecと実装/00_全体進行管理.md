# 00_全体進行管理

## 1. このファイルの役割

このファイルは、`05_changeSpecと実装/` 全体の進行管理を行うためのハブとする。

## 2. ステップ進捗一覧

| ステップ | 対応仕様書 | 状態 | 依存先 | 最新更新 | 備考 |
|---|---|---|---|---|---|
| 01 | `04_仕様書/01_bills親docとcurrent_state管理.md` | 完了 | なし | 2026-05-09 | parent target contract の実装とテスト更新は実施済み。build 成功、`createBill / accounting / cancelAccounting / billsOnSettle / close_process` は Emulator 下で確認済み |
| 02 | `04_仕様書/02_settlementCyclesとbaselineSnapshot.md` | 完了 | 01 | 2026-05-09 | `settlementCycles/1` 初期生成、settle 時の cycle settled 化、`baselineSummary` と `baselineSnapshot/snapshot` 保存、Emulator 下の `createBillWithActiveStay / bills.onSettle` 確認まで完了 |
| 03 | `04_仕様書/03_adjustments管理.md` | 完了 | 01, 02 | 2026-05-09 | 新 callable `createPostSettlementAdjustment` を追加し、4 パターン (`decrease_refund_pending` / `decrease_refunded` / `increase_collection_pending` / `increase_collected`) と opposite-direction offset、parent `postSettlementState` / `currentSummary` / `status` 反映、idempotency replay まで Emulator 下で確認済み。即時 cashAction は最小 shape で先行実装し、本実装は Step04 で拡張。UI 切替は Step06、analytics 反映は Step07 |
| 04 | `04_仕様書/04_cashActions管理.md` | 完了 | 01, 02, 03 | 2026-05-09 | 新 callable `recordPostSettlementRefund` / `recordPostSettlementCollection` と共通 repo `recordPostSettlementCashAction` を追加。`services/cashActions.ts` を拡張し `buildCashActionDoc` / `validateAllocations` / `applyAllocationsToAdjustments` / `resolveCashflowBusinessDate` を導入。`services/parentSummary.ts` に cashAction 派生 helper を追加し、Step03 immediate 経路で `lastRecordType = 'cash_action'` 仕様完全準拠化。`cashflowBusinessDate` は input → calcBusinessDate → bill.businessDate borrow の優先順位で必ず string 値を保証。Emulator 統合 28 tests / unit 98 tests / 旧経路リグレッション 76 tests すべて pass。UI 切替は Step06、analytics 反映は Step07 |
| 05 | `04_仕様書/05_reopenと再会計.md` | 完了 | 01, 02, 03, 04 | 2026-05-09 | 新 callable `reopenAccountedBill` と repo `reopenAccountedBill` を追加。`services/parentSummary.ts` に `buildReopenSummaryAfterReopen` / `buildParentDocPatchForReopen`、`services/settlementCycles.ts` に `buildReopenedCycleDocPatch`、`services/adjustments.ts` に `buildAdjustmentCancelledByReopenPatch` を追加。当日営業日制約 (`bill.businessDate === storeMeta.currentBusinessDateKey`) を hard precondition として実装。idempotency / 完了済 adjustment 不変 / 親 doc spec_only reset / 旧 cycle baselineSnapshot・cashActions 不変 のすべての不変則を保持。Emulator 統合 13 tests / unit 116 tests / Step01-04 リグレッション 71 tests / 旧経路リグレッション 76 tests **すべて pass**。UI 切替は Step06、analytics 反映は Step07 |
| 06 | `04_仕様書/06_要対応の会計画面と一覧取得.md` | 完了 | 01, 03, 04, 05 | 2026-05-09 | 既存 `lib/Accounting/unsettledAccountingPage.dart` を `lib/Accounting/requireSpecialAttentionPage.dart` に rename + 全面刷新。共通 view model `BillRequireAttentionViewModel`（`classifyBill` / `computeSortDate` / `computeDisplayAmountIncl` / `computePrimaryAction` / `computeDisplayLabel`）と `UserAttentionCounts` を実装。3 stream（`carryover_unsettled` / `post_settlement_collection_pending` / `post_settlement_refund_pending`）を client-side merge し、`日付ごと` / `ユーザー別` の 2 タブと `すべて` / `未会計` / `追加徴収` / `要返金` の 4 chip フィルタを実装。新 dialog 2 つ（`PostSettlementCollectionDialog` / `PostSettlementRefundDialog`）から `recordPostSettlementCollection` / `recordPostSettlementRefund` callable を呼ぶ primary action 導線を新設。`terminalHomePage.dart` のメニュー entry を `未会計の会計` → `要対応の会計` に改名。`closeSummary` のみ read（`closeSnapshot` は touch しない）、`carryover_unsettled` は `status='open'` のみ、`userDisplayName` は `bills.party.pokerName` source。Flutter unit / widget 110 tests **すべて pass**（既存 56 + Step06 新規 54）、Functions コードは未 touch のため Step01〜05 のリグレッション影響なし。実機確認は後続作業 |
| 07 | `04_仕様書/07_analyticsMonthly更新と日付帰属とline配賦.md` | 完了 | 01, 02, 03, 04, 05 | 2026-05-10 | post-settlement adjustment / cashAction の analytics 反映と reopen 時 rollback を実装。pure delta builder（`adjustmentDelta.ts` / `cashActionDelta.ts`）と atomic applier（`applyAdjustmentToAnalytics.ts` / `applyCashActionToAnalytics.ts` / `applyReopenRollbackToAnalytics.ts`）を新規追加。settle marker docId を `{billId}_cycle{cycleNo}_settle` に cycle 化し、`processBillAnalyticsAtomically` / `aggregator/index.ts` / `aggregator/types.ts` / `billsOnSettle.ts` で `cycleNo` を伝搬（legacy `{billId}` marker は後方互換）。`createPostSettlementAdjustment` / `recordPostSettlementCashAction` / `reopenAccountedBill` の各 repo で main transaction 後の **separate-tx** で analytics applier を呼び、feature flag `storeConfig.features?.settlementAggregatorEnabled` で制御。analytics 失敗時は `logOpsError` のみで callable は成功（仕様書 §10.5 の nightly 再計算前提）。pure unit + emulator integration + callable spec 拡張で **comprehensive テスト戦略**を採用。`paymentTotals` は仕様書 §8.4 通り refund では直接減らさず、`bill.businessDate.substring(0, 7)` の売上帰属月に集約。Functions build / lint 0 errors、Step07 で touch した spec / src は全 pass、既存 9 suites failing は develop baseline 由来（Step05 / Step06 完了時点と同じセット、Step07 起因なし）。Flutter コードは未 touch、`firestore.indexes.json` / `firestore.rules` も未 touch。実機確認は後続作業 |

状態の目安:
- `未着手`
- `現状確認中`
- `changeSpec作成中`
- `確認観点整理済み / 実装未着手`
- `実装中`
- `確認中`
- `完了`
- `保留`

## 3. 横断的な注意事項

- 仕様書と実装差分を見つけた場合は、先に `04_仕様書/` を確認する。
- 仕様書だけでは判断しづらい論点は `03.1_前提再設計/` に戻って確認する。
- future 機能は、このファイルでは TODO として残し、current-scope に持ち込まない。
- 実装着手前に、各ステップで `01_現状確認と影響範囲.md` と `02_changeSpec.md` の整合を確認する。
- 実装後は、`03_仕様書トレース確認.md` の状態と `04_確認観点と確認方法.md` の実行結果を更新してから次ステップへ進む。

## 4. 共通の未解決 / 要監視事項

- Step01 では親 doc の target 契約を先に導入し、旧 `postEvents` / `closeSnapshot` / root summary の整理は後続 step で段階的に進める。
- Step02 では `settlementCycles` と `baselineSnapshot` を導入したが、actual reopen runtime は Step05 で接続する。
- `android/build/` は未追跡生成物であり、このフェーズの docs / code 変更に混ぜない。
- Emulator 依存テストは、失敗時にまず再起動を試し、その後に実装不備と環境不備を切り分ける。

## 5. 共有メモ

- Step03 は `settlementCycles/{cycleNo}.nextSequenceNo` と `baselineSnapshot/snapshot` を前提に changeSpec を起こす。
- Step04 は Step03 が最小 shape で先行実装した cashAction を土台に、later パターン / multi method / multi allocation / `cashflowBusinessDate` 厳密化を本実装した。新 callable `recordPostSettlementRefund` / `recordPostSettlementCollection`、共通 repo `recordPostSettlementCashAction`、`services/cashActions.ts` 拡張（`buildCashActionDoc` / `validateAllocations` / `applyAllocationsToAdjustments` / `resolveCashflowBusinessDate`）、`services/parentSummary.ts` cashAction 派生 helper、`createPostSettlementAdjustment` immediate 経路の Step04 仕様統合（`lastRecordType = 'cash_action'`）。後続 step は次の点に注意:
  - Step05: `cashAction` 自体は reopen 時に `cancelled_by_reopen` に遷移しない。allocation 先 adjustment が `cancelled_by_reopen` になるが cashAction の `allocations[]` / `methodBreakdown[]` は保持。
  - Step06: UI を旧 `processRefund` から新 `recordPostSettlementRefund` / `recordPostSettlementCollection` に切替。`getRefundHistory` の本実装も Step06 で行う。method 文字列 enum の strict 化は UI 側を allow list (`cash` / `credit_card` / `electronic_money` / `qr` / `bank_transfer` / `other`) に揃える。
  - Step07（確定済の実装方針）: 仕様書 `07` / `changeSpec §5.2.4` 通り、事後 cashAction の **`paymentTotals` は売上帰属月 (`bill.businessDate.substring(0, 7)`）** に集約し、`cashAction.methodBreakdown[]` は `processCashActionAnalyticsAtomically` へ入力。以前メモしていた `analyticsMonthly.delta.cashflow.*` や **`cashflowBusinessDate` を月キーとする経路は採らない**。`analyticsMonthly.delta.cashflow.*` は current-scope で touch しない。
- Step05 は Step02 が作った cycle 基盤の上に reopen runtime を乗せた。Step03 で定義のみした `adjustmentState = cancelled_by_reopen` への一括遷移を実装。さらに上流 §7 の「当日営業日のみ reopen 可」を hard precondition として `BILLS_REOPEN_NOT_TODAY` で実装。後続 step は次の点に注意:
  - Step06: UI 側で当日営業日でない bill には reopen ボタンを disable / hidden 表示するロジックを実装（precondition は backend が hard reject するが UX 観点で UI 側での判定も推奨）。Step06 では旧 dialog (`postAccountingReopenDialog` 等) は touch していないため、旧経路から `postEventReopen` を呼ぶ既存 UI は併存。新 callable `reopenAccountedBill` への UI 切替は後続 step で行う。
  - Step07: reopen 時の analyticsMonthly rollback / resettle 反映を実装。旧 cycle 寄与の rollback と新 cycle baseline の forward で paymentTotals / baseline / line 配賦の整合をとる。
  - Step05 では `requireSpecialAttention` / `closeSummary` / `amounts` / `itemsSnapshot` 等の補助 fields は touch していない（仕様書記述外）。これらの reset / forward が必要な場合は Step06 / Step07 で扱う。
- Step06 は Step01〜Step05 で整備した `bills` 親 doc field（`closeSummary.unresolved` / `closeSummary.closedBusinessDate` / `closeSummary.displayAmountAtMark` / `postSettlementState.requiredActionType` / `postSettlementState.requiredActionIncl`）から要対応 bill を判定する読み取りモデルを実装した。Functions 側コードは未 touch（`bills` 親 doc に新 field を追加していない）。旧 dialog / 旧 callable は併存維持。後続 step は次の点に注意:
  - Step07: 要対応一覧の数値は `bills` 直接読みで完結しているため、analytics 集計（`analyticsMonthly`）が遷移しても要対応一覧の表示挙動は影響を受けない。事後 adjustment / cashAction の analytics 配賦と、reopen 時の analytics rollback を Step07 で実装する際、要対応一覧側は read-only で独立。
  - 後続 step（旧経路統廃合）: `AccountingPage` 内の事後操作（`refundProcessingDialog` 等）も新 dialog に切り替え、旧 events 経路 dialog 群（`postAccountingRefundDialog` / `postAccountingAdjustmentDialog` / `postAccountingReopenDialog` / `postAccountingCancelDialog` 等）と旧 callable（`processRefund` / `postEventAdjustment` / `postEventRefund` / `postEventCollect` 系 / `postEventReopen` / `postEventCancel`）を段階的に廃止する。`closeSnapshot` field の段階的廃止も同タイミングで検討（grep で全 read 箇所を洗い出す）。
- Step07 は `baselineSummary` と `baselineSnapshot` を analytics 側の入力として参照できる。Step03 が必須化した line 構造 (`targetCategory` / `targetId` / `operationType` / `qtyDelta` / `amountInclDelta` / tournament line の templateKey) は Step07 の `analyticsMonthly` 更新にそのまま使える。
- Step07 の主要決定（[02_changeSpec.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/05_changeSpecと実装/07_analyticsMonthly更新と日付帰属とline配賦/02_changeSpec.md)）:
  - reopen 時 analytics 反映: **rollback-and-resettle**（baseline + adjustments + collection cashActions の負号適用 + 新 cycle で再 settle）
  - transaction 戦略: **separate-tx**（main 後 / capture pattern / 失敗時は ops error log のみ）
  - feature flag: 既存 `storeConfig.features?.settlementAggregatorEnabled` を流用（settle 経路と一貫）
  - helper 戦略: 新規 helper を追加（既存 `addTo*` 群は touch なし）
  - テスト戦略: comprehensive（pure unit + emulator integration + callable spec 拡張）
- 後続 step（旧経路統廃合）: 旧 events 経路（`enqueueEvent` / `buildEventDelta` / `appendEventLog` / 旧 `bills/{billId}/events` trigger）の analytics 反映停止、legacy settle marker `{billId}` の deprecation、`analyticsMonthly` の field schema 統一（`entrySalesIncl` vs `entrySales` 等の suffix 差）、nightly 再集計バッチの実装は **後続 step** で扱う。
