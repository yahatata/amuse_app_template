# 03_確定対象の評価結果

このファイルでは、仕様候補ごとに、`step3.11` で確定したか、後続ステップへ回したかを一覧化する。

なお、今回の `step3.11` では、**現行実装から派生でき、`bills` との整合性を保ったまま実装可能な範囲**を優先して確定した。  
税務・会計 read model の厳密化は、必要なものを [/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/05_今後検討_税務会計read_model拡張.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/05_今後検討_税務会計read_model拡張.md) に切り出して継続管理する。

| 候補ID | 仕様候補 | `step3.11` で確定するか | 判断理由 | 確定内容の所在 | `step3.12` への引き渡し事項 |
|---|---|---|---|---|---|
| C01 | 事後イベントとして用意する機能 / 業務パターン | 確定する | 金銭系 4 パターン、`reopen` の位置づけ、`会計後キャンセル` 廃止方針が決まらないと、後続の `analyticsMonthly`、保存モデル、current-state 設計、未会計との接続の整合性確認ができないため | [11_事後イベントの機能と業務パターン.md](./11_事後イベントの機能と業務パターン.md) | `reopen` の rollback 実装詳細、cycle と analytics の結び付け方、未会計 cancel との接続は後続で詰める |
| C02 | `analyticsMonthly` / 入出金系データ / 運用 read model の役割分担 | 確定する | `analyticsMonthly` の既存役割、`paymentTotals` の意味、実入出金の持ち場所、運用 SoT を先に決めないと、後続の整合性確認と詳細仕様分割ができないため。今回は現行実装から読める source に限定して合意するため | [12_analyticsMonthlyと入出金データの役割分担.md](./12_analyticsMonthlyと入出金データの役割分担.md) | `/payments` 正式 ledger 化、税務・会計用 read model の厳密 schema、クレカ後日入金やポイント treatment は future doc で継続検討する |
| C03 | `bills` の SoT と保存モデル | 確定する | 親 doc、`settlementCycles`、`baselineSnapshot`、`adjustments`、`cashActions` の役割分担が決まらないと、14 の current-state 設計や 12 で決めた `analyticsMonthly` 非 SoT 方針が成立しないため | [13_billsのSoTと保存モデル.md](./13_billsのSoTと保存モデル.md) | 親 doc の正式 field 名、`baselineSnapshot` / `adjustments` / `cashActions` の正式 schema、`requiredActionRemainingIncl` と `allocations` の厳密更新ルール、親 doc 更新境界を後続で詰める |
| C04 | 親 doc の status / summary / pending 管理 | 確定する | 親 doc で current state をどう表すか、current cycle と old cycle をどう切り分けるか、未徴収 / 未返金 / 持ち越し未会計をどう軽く読めるようにするかが決まらないと、運用画面・一覧・保存モデル・`analyticsMonthly` 非 SoT 方針がつながらないため | [14_status_summary_pending管理.md](./14_status_summary_pending管理.md) | summary field 名の最終固定、`lastRecordType` の具体 enum、`currentStateMeta` の採否などは詳細仕様で詰める |
| C05 | 売上日 / 入出金日 / 営業日の帰属ルール | 確定する（current-scope） | 売上発生日 / adjustment 確定日 / cashflow 実行日の 3 軸が曖昧だと、`analyticsMonthly` と `bills` 保存モデルの整合が取れないため。今回は現行 `bills` 実装から派生できる範囲に絞って帰属ルールを定義する | [15_売上日入出金日営業日の帰属ルール.md](./15_売上日入出金日営業日の帰属ルール.md) | クレカ後日入金、ポイント treatment、月締め / 年締め、厳密な税務・会計 read model は future doc で後続管理する |
| C06 | 未会計 / 一部未徴収 / 会計後イベントの接続方針 | 確定する（current-scope） | 閉店持ち越し未会計と会計後要対応を同じ画面で扱うか、保存構造を統一するか、カード種別をどう分けるかが決まらないと、Step4 の一覧取得条件と UI 導線が定まらないため。今回は `bills` 親 doc から導出する共通 view model までを current-scope で確定する | [16_未会計一部未徴収会計後イベントの接続方針.md](./16_未会計一部未徴収会計後イベントの接続方針.md) | 保存構造は分けたままとし、同一画面ではカード種別を明示する。query の一本化要否や最終画面名、表示項目の詳細は Step4 で詰める |
| C07 | 既存データ互換 / 移行方針 | 確定する（移行不要） | 現在は未リリースの開発期間中であり、既存 `analyticsMonthly` や既存 `bills` を今回の改修に適合させる migration / backfill を前提にしなくてよいため | [17_既存データ互換移行方針.md](./17_既存データ互換移行方針.md) | migration 章は必須にしない。もし将来追加設定が必要なら初期化時生成で扱い、リリース後の移行は future に回す |
| C08 | 売上差分明細の粒度・配賦ルール | 確定する（current-scope） | `adjustments.lines[]` の粒度と category / tournament 配賦ルールが決まらないと、`analyticsMonthly` の top-level / `byCategory` / `byUser` / `byTemplateTournaments` 更新責務が確定しないため | [18_売上差分明細の粒度と配賦ルール.md](./18_売上差分明細の粒度と配賦ルール.md) | product-level analytics など current-scope 外の軸は future に残し、Step4 では current 集計軸に必要な line 必須ルールとして扱う |
