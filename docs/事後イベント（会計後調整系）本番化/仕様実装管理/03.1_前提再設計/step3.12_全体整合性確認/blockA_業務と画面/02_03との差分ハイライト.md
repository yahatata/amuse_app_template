# Block A 02_03との差分ハイライト

## 1. このファイルの役割

ここでは、`03_ToBe意思決定` のうち、このブロックに関係するファイルに残っている旧前提をハイライトし、どこをどう修正検討すべきかを整理する。

## 2. 主な同期対象

- [/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03_ToBe意思決定/I2_伝票状態・表示データ・一覧取得の正規化_ToBe意思決定.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03_ToBe意思決定/I2_伝票状態・表示データ・一覧取得の正規化_ToBe意思決定.md)
- [/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03_ToBe意思決定/I3_会計管理UI導線と営業状態別アクセス設計_ToBe意思決定.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03_ToBe意思決定/I3_会計管理UI導線と営業状態別アクセス設計_ToBe意思決定.md)

## 3. `I2` に残っている旧前提

### 3.1 旧 status 名

残っている主な旧 status:

- `partially_paid`
- `partially_refunded`
- `refunded`
- `voided`
- `in_progress`

### 3.2 旧 summary 名

- `postEvents`

### 3.3 旧一覧の考え方

- `会計完了` / `会計後管理` の旧一覧を別立てで考えている
- `要対応の会計` という入口がない
- `日付ごと / ユーザー別` の軸に、今回決まった 4 フィルタが乗っていない

### 3.4 3.11 の確定版

`step3.11` での確定版は次。

- `terminalHome` の入口は `要対応の会計`
- 画面軸は
  - `日付ごと`
  - `ユーザー別`
- フィルタは
  - `すべて`
  - `未会計`
  - `追加徴収`
  - `要返金`
- 親 doc status は
  - `open`
  - `settling`
  - `settled`
  - `post_settlement_pending`
- 会計後差分は `postSettlementState` で読む
- `reopen` 後は `open`
- `voided` は current-scope の標準 status に採らない

### 3.5 修正検討の方向

`I2` は次の方向で同期修正するのが自然。

1. old status 群前提を current-scope status 群へ置換
2. `postEvents` 参照を `postSettlementState` へ置換
3. 一覧入口を `要対応の会計` 前提へ置換
4. `日付ごと / ユーザー別` + 4 フィルタ前提へ置換
5. 未収残あり / 未返金ありの判定を status ではなく
   - `status = post_settlement_pending`
   - `requiredActionType`
   - `requiredActionIncl`
   へ寄せる
6. `reopen` 後の扱いを `in_progress` ではなく `open` へ置換
7. `voided` を標準 UI 状態として扱う前提を外す

## 4. `I3` に残っている旧前提

### 4.1 旧 status / 旧画面前提

残っている主な旧前提:

- `会計後管理` を中心にした旧一覧導線
- `in_progress`
- `voided`
- old status 群を前提にした post-settlement 管理一覧

### 4.2 3.11 の確定版

- `terminalHome` の入口は `要対応の会計`
- `reopen` 後 status は `open`
- 会計後要対応は `post_settlement_pending`
- 持ち越し未会計と会計後要対応は同じ画面に出せる
- ただし導線は bill 種類ごとに分ける
- 画面フィルタラベルは `未会計 / 追加徴収 / 要返金`

### 4.3 修正検討の方向

`I3` は次の方向で同期修正するのが自然。

1. `terminalHome` の入口名称を `要対応の会計` に置換
2. `要対応の会計` 画面の構造を
   - `日付ごと / ユーザー別`
   - `すべて / 未会計 / 追加徴収 / 要返金`
   として明記
3. `reopen` 後の status 記述を `open` に置換
4. `voided` を current-scope の標準 UI 状態として扱う前提を外す
5. 同じ画面に出すカード種別と分岐導線を明記する

## 5. このブロックで利用者に判断してもらったこと

2026-05-08 時点で、次は利用者判断済みである。

1. 同じ画面で扱う方針を維持する
2. フィルタは
   - `すべて`
   - `未会計`
   - `追加徴収`
   - `要返金`
   とする
3. `reopen` 後は `open` へ統一する
4. 導線は bill 種類ごとに分ける

## 6. GO が出たのでどこをどう直すか

### 修正対象

- `I2`
- `I3`

### 主な修正内容

- old status 群を current-scope status 群へ置換
- `postEvents` を `postSettlementState` へ置換
- `in_progress` を `open` へ置換
- `voided` を current-scope の標準状態から外す
- 一覧条件を「同じ画面 / 2 軸 / 4 フィルタ」前提へ整理
- `terminalHome` の入口名称を `要対応の会計` に置換
