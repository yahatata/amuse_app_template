# Block A 04_確認チェックリスト

## GO 判断のための確認項目

### A-1 業務パターン

- 4 パターン + `reopen` という分け方で理解できる
- `reopen` は金銭系ではなく再会計入口として扱う方が自然だと思える

### A-2 `要対応の会計` 画面

- `terminalHome` の入口名を `要対応の会計` にしてよい
- 持ち越し未会計と会計後要対応を同じ画面で扱う方針に納得できる
- ただしカード種別は分ける方がよいと思える

### A-3 表示軸とフィルタ

- `日付ごと / ユーザー別` の 2 軸を残してよい
- `すべて / 未会計 / 追加徴収 / 要返金` の 4 フィルタでよい
- デフォルトを `すべて` にしてよい

### A-4 判定方法

- `carryover_unsettled = status + closeSummary`
- `post_settlement_* = status + postSettlementState`

で十分判定できると考えられる

### A-5 導線分岐

- `未会計` は通常会計再開導線
- `追加徴収` は徴収導線
- `要返金` は返金導線

に分けてよい

### A-6 03 の上書き方針

- `I2` / `I3` の旧 status 前提を 3.11 確定版で置き換えてよい
- `reopen` 後を `open` に統一してよい
- `voided` を current-scope の標準 UI 状態から外してよい

## 2026-05-08 時点の判断結果メモ

- GO / 保留 / 修正希望: GO
- 確定事項:
  - 同じ画面で扱う
  - 入口名は `要対応の会計`
  - フィルタは `すべて / 未会計 / 追加徴収 / 要返金`
  - `追加徴収` を画面ラベルとして採用する
  - 導線は bill 種類ごとに分ける
