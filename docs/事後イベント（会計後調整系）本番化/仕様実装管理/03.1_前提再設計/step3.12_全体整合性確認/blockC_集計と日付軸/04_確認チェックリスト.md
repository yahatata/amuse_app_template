# Block C 04_確認チェックリスト

## GO 判断のための確認項目

### C-1 `analyticsMonthly`

- `analyticsMonthly` を運用ダッシュボード用 read model として割り切る方針に納得できる
- SoT ではないという整理で理解できる

### C-2 日付軸

- 売上帰属日 / adjustment 確定日 / cashflow 実行日を分ける方針に納得できる

### C-3 source の範囲

- current-scope では現行実装から派生できる source に限定する方針でよいと考えられる

### C-4 line 粒度

- `lines[]` 必須
- category は 4 種固定
- tournament は template + operationType まで持つ

で current-scope として十分だと考えられる

### C-5 future への切り出し

- strict な税務・会計 read model は future に回してよい
- card 後日入金や point treatment も future でよい

## このブロックの判断結果メモ

- GO / 保留 / 修正希望: GO
- 確定事項:
  - `analyticsMonthly` は運用ダッシュボード用 read model
  - 日付軸は 3 本で分ける
  - 支払手段 source は現行 `bills` 実装から派生できる範囲に限定
  - `adjustments.lines[]` は必須
  - strict な税務・会計 read model は future 機能
