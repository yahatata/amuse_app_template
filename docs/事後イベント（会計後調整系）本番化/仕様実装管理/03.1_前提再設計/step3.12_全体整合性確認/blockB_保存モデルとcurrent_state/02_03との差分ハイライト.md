# Block B 02_03との差分ハイライト

## 1. 主な同期対象

- [/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03_ToBe意思決定/I1_事後イベント業務ルールとデータ契約_ToBe意思決定.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/03_ToBe意思決定/I1_事後イベント業務ルールとデータ契約_ToBe意思決定.md)
- [/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/README.md](/Users/yahatayuusei/Documents/GitHub/amuse_app_template/docs/事後イベント（会計後調整系）本番化/仕様実装管理/README.md)

## 2. `I1` に残っている旧前提

### 2.1 旧 collection 名

- `/postSettlementCollections`

### 2.2 旧 status 前提

- `partially_paid`
- `refunded`
- `partially_refunded`

### 2.3 旧 migration 前提

- リリース前に既存 event を正規化 / migration する前提

### 2.4 3.11 の確定版

- 実入出金は `settlementCycles/{cycleNo}/cashActions/{cashActionId}`
- `cashActions` は cycle 配下に置く
- `allocations[]` は必須
- 親 doc status は `post_settlement_pending` を使う
- required action は `postSettlementState.requiredActionType / requiredActionIncl` で読む
- unreleased 前提なので migration / backfill は不要

### 2.5 修正検討の方向

`I1` は次の方向で同期修正するのが自然。

1. `/postSettlementCollections` を `cashActions` 前提へ置換
2. old status 群を current-scope status 群へ置換
3. pending の読み方を status ではなく parent summary へ寄せる
4. `allocations[]` 必須と `requiredActionRemainingIncl` 前提を明記する
5. リリース前 migration 前提を current-scope 非対象へ置換する

## 3. `仕様実装管理/README.md` に残る旧前提

### 3.1 残っているもの

- `pending subcollection`
- parent / pending の関係を旧モデルで書いている記述

### 3.2 3.11 の確定版

- pending 独立 subcollection は採らない
- `baselineSnapshot / adjustments / cashActions` の 3 層で current state を作る
- `requiredActionRemainingIncl` と `allocations` を中心に current state を作る

### 3.3 修正検討の方向

README の step3.12 / step3.14 付近の説明を、新しい保存モデルに同期する。

## 4. このブロックで利用者に判断してもらったこと

2026-05-09 時点で、次は利用者判断済みである。

1. `baselineSnapshot` は 1 doc で持つ
2. `cashActions` は cycle 配下に置く
3. `allocations[]` は必須にする
4. `requiredActionRemainingIncl` は adjustment 単位で持つ
5. migration は current-scope では不要とする

## 5. GO が出たのでどこをどう直すか

### 修正対象

- `I1`
- `仕様実装管理/README.md`
- blockB 配下の判断資料

### 主な修正内容

- `baselineSnapshot / adjustments / cashActions` 前提へ置換
- `/postSettlementCollections` を `cashActions` へ置換
- old status 群を `post_settlement_pending` + parent summary へ置換
- `allocations[]` / `requiredActionRemainingIncl` のルールを明記
- migration 不要前提へ置換
