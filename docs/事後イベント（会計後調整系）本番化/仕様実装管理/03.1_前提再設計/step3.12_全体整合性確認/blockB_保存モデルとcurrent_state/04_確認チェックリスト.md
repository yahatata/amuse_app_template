# Block B 04_確認チェックリスト

## GO 判断のための確認項目

### B-1 保存構造

- 親 doc / cycle / baseline / adjustments / cashActions の分離で理解できる
- `baselineSnapshot` は full snapshot として 1 doc でよいと感じる

### B-2 cycle と順番

- cycle は baseline の版だと理解できる
- cycle を進めるのは `reopen` の時だけでよいと思える
- 同一 cycle 内の順番は `sequenceNo` で十分だと思える

### B-3 adjustment と cashAction

- adjustment は売上差分
- cashAction は実入出金

という分け方で自然だと思える

### B-4 allocations と残額

- `allocations[]` で複数 adjustment をまたぐ設計に納得できる
- `requiredActionRemainingIncl` を adjustment 単位で持つ方針に納得できる

### B-5 migration

- current-scope では移行不要でよいと考えられる

## 2026-05-09 時点の判断結果メモ

- GO / 保留 / 修正希望: GO
- 確定事項:
  - `baselineSnapshot` は 1 doc で持つ
  - `cashActions` は cycle 配下に置く
  - `allocations[]` は必須
  - `requiredActionRemainingIncl` は adjustment 単位で持つ
  - migration は current-scope では不要
