# 05_動作保証範囲

## 1. このファイルの見方

ここでは、動作保証範囲を次の 3 つに分けて整理する。

1. テストで確認済み
2. 実機確認済み
3. 未確認 / 条件付き

重要なのは、**「手順がある」ことと「実施済みである」ことは別**として扱うことである。

## 2. テストで確認済みの範囲

### 2.1 Functions build

- この確認作業中に `npm --prefix functions run build` を再実行
- 成功

### 2.2 Step01〜Step05 の Functions 側

`05_changeSpecと実装/00_全体進行管理.md` と各 step の `06_確認結果サマリ.md` から、次が確認済みとして記録されている。

- Step01
  - `createBill`
  - `accounting`
  - `cancelAccounting`
  - `billsOnSettle`
  - `close_process`
- Step02
  - `settlementCycles/1` 初期生成
  - `baselineSummary` / `baselineSnapshot` 保存
- Step03
  - adjustment 4 パターン
  - opposite-direction offset
  - immediate cashAction 最小 shape
- Step04
  - later refund / collection
  - allocation / methodBreakdown / parent summary 反映
- Step05
  - reopen / resettle runtime
  - `cancelled_by_reopen`
  - cycle 進行

### 2.3 Step06 Flutter 側

この確認作業中に `flutter test test/Accounting` を再実行し、成功した。

再確認できたもの:

- `requireSpecialAttentionPage` の分類
- フィルタ
- grouping
- primaryActionType
- user attention counts

### 2.4 Step07 analytics 側

docs 上では次が確認済みとして整理されている。

- adjustment delta
- cashAction delta
- analytics applier
- reopen rollback applier
- settle marker cycle 化
- Step07 で touch した spec は pass
- ただし overall functions suite には develop baseline 由来の失敗が 9 suites 残る

## 3. 実機確認済みの範囲

### 3.1 現時点の判定

**実機確認済みとして結果まで残っている範囲は、今回確認した docs からは十分に確認できなかった。**

理由:

- Step03〜07 の `08_実機確認手順.md` は存在する
- ただし `実施日 / 実施者 / 結果` が `TODO` のままのものがある
- Step06 / Step07 の `06_確認結果サマリ.md` でも、実機確認は後続作業と明記されている

### 3.2 実務上の扱い

したがって、現時点では

- **実機確認手順は整備済み**
- **実機確認の実施結果は未記録**

として扱うのが正確である。

## 4. 未確認の範囲

### 4.1 end-to-end 実機フロー

未確認として扱うのが安全なもの:

- `terminalHome` から `要対応の会計` へ入る一連の実機導線
- 実際の collection / refund dialog 実行
- reopen 後 resettle の実機シナリオ
- analytics rollback の実機シナリオ

### 4.2 セキュリティルール / index 周辺

- `要対応の会計` 画面で使う `bills` 複合条件 query 用 composite index は追加・deploy 済み
- current-scope の新 UI / Firestore read path は、既存 `firestore.rules` の開発用 read 許可で到達可能であることを確認済み
- ただし、本番向け read 制限の再設計は未着手

### 4.3 overall functions suite の完全通過

- Step07 docs によると、Step07 で touch した spec は pass
- ただし repository 全体では develop baseline 由来の failing suites が残っている
- そのため「repo 全体テスト green」とはまだ言わない方が正確である

## 5. 現時点の保証表現

現時点の動作保証範囲を一言で言うなら次になる。

**「Cloud Functions の主要 current-scope と Flutter の要対応 UI については、自動テストと build でかなり広く確認されている。加えて、実機確認前提となる Firestore index は deploy 済みで、current-scope の read path も既存 rules で到達可能である。一方、実機 end-to-end の結果記録と、本番向け rules 再設計はまだ残っている」**

## 6. 次にやると保証が強くなること

1. Step06 と Step07 の `08_実機確認手順.md` を実際に実施する
2. 結果を各 step の `06_確認結果サマリ.md` または運用記録へ残す
3. 必要なら本番向け rules 制限方針を別 docs に切り出す
4. overall functions suite の baseline fail を、今回改修と切り離して別管理する
