# 04_改修対象外と後続事項

## 1. 今回あえて触っていないもの

### 1.1 strict な税務・会計 read model

次は今回の current-scope には入っていない。

- `reportingEntries`
- `reportingMonthly`
- `cashflowMonthly`
- advisor review / period close

### 1.2 Firestore rules / indexes

- `firestore.rules`
- `firestore.indexes.json`

については、初回サマリ作成時点では差分上で確認できなかった。

その後の確認で、

- rules: 今回の current-scope 新 UI / 新 subcollection read path は既存の開発用 read 許可で到達可能
- indexes: `要対応の会計` 画面で使う `bills` の複合条件 query 用 composite index を追加

まで対応済みである。

### 1.3 旧経路の全面撤去

旧 `events` 系経路や旧 dialog / 旧 callable の全面削除は、今回まだ完了していない。

例:

- 旧 `postEvent*` 系
- 旧 `processRefund`
- 旧 `postAccounting*Dialog`
- 旧 `refundProcessingDialog`

### 1.4 analytics の厳密運用周辺

- nightly 再集計バッチの本実装
- analytics failure の運用リカバリ導線
- marker の長期運用整理

## 2. 後続フェーズに回したもの

### 2.1 実機確認の結果記録

手順は整備されているが、実施結果の記録は未完了である。

### 2.2 セキュリティルールの再確認

current-scope の新 UI / Firestore read path は、既存 `firestore.rules` で到達できることを確認済みである。

ただし、現行 rules は開発用の広い read 許可を含むため、本番向けの read 制限強化は別フェーズで再設計してよい。

### 2.3 旧経路の整理・廃止

新経路導入後に、旧経路をいつ止めるかは別判断が必要である。

### 2.4 追加の運用確認

- reopen 後 resettle の実環境確認
- analytics rollback の実環境確認
- `要対応の会計` 画面の end-to-end 確認

## 3. 今後やるとよいこと

1. 実機確認を実施して、各 `08_実機確認手順.md` の結果欄を埋める
2. 本番向け `firestore.rules` の read 制限再設計要否を判断する
3. 旧経路を残す期間と撤去順を決める
4. 未コミット差分を整理して commit / push する
