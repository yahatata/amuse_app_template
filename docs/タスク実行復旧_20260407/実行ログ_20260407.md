# 実行ログ（2026-04-07）

## 1. 目的

- タスク復旧作業の実施内容と確認結果を、再現可能な形で残す
- 新規 Firebase プロジェクト導入時に同じ確認を流用できるようにする

## 2. 事前確認（Phase0）

実施:

- Queue 一覧確認（`asia-northeast1`）
- Scheduler 一覧確認（`asia-northeast1`）
- `business-date-assessment-queue` のタスク一覧確認

結果:

- `business-date-assessment-queue` に以下 5 件の 400 再試行タスクを確認
  - `open_assessment_2026-04-04`
  - `open_assessment_2026-04-05`
  - `close_assessment_2026-04-03`
  - `close_assessment_2026-04-04`
  - `close_assessment_2026-04-05`

## 3. malformed タスク削除（Phase1）

実施:

- 上記 5 件を個別削除

結果:

- 削除後 `business-date-assessment-queue` は 0 件

## 4. SA 統一 + ログ書き込み修正（Phase2/3）

コード変更:

- `functions/src/shared/config/cloudTasksConfig.ts`
  - `OPENCLOSE_INVOKER_SA_PREFIX` を `tasks-invoker` に変更
- `functions/src/domains/scheduler/supervisor/schedulerLogs.ts`
  - Firestore 書き込み前に undefined field を除外

確認:

- `openclose-tasks-invoker@...` は存在しない
- `tasks-invoker@...` は存在する
- `npm --prefix functions run build` 成功
- `npm --prefix functions run lint` 成功

## 5. 限定デプロイ（復旧対象）

デプロイ関数:

- `schedulerSupervisor`
- `scheduled-job-weekly-planner`
- `scheduled-job-enqueue-tournament-tasks-by-scheduler`
- `scheduled-job-generate-recurring-tournaments-by-scheduler`
- `scheduled-job-scheduled-cleanup`
- `scheduled-job-schedule-generate-next-year-business-hours`
- `scheduled-job-payroll-notification-scheduler`
- `continueBusinessTerminal`
- `closeStoreTerminal`
- `temporaryUnlockAlreadyRunningDifferentDateTerminal`

結果:

- すべて更新成功（Deploy complete）

## 6. 復旧確認（assessment/scheduler）

### 6.1 weeklyPlanner 手動投入

実施:

- `scheduled-job-weekly-planner` queue に manual task を投入

結果:

- task は消化済み（キュー残 0）
- `business-date-assessment-queue` に open/close task が再生成された
- 生成 task の内容を確認:
  - body あり
  - `Content-Type: application/json`
  - OIDC SA: `tasks-invoker@amuse-app-template.iam.gserviceaccount.com`

### 6.2 open/close assessment 実行確認

実施:

- `business-date-assessment-queue` に manual open/close task を投入

結果（Cloud Logging）:

- `openAssessmentTask` 直近実行が `HTTP 200`
- `closeAssessmentTask` 直近実行が `HTTP 200`

### 6.3 scheduler execution log write 失敗の再発確認

実施:

- `schedulerExecutionLogsByCloudTask write failed` をログ検索

結果:

- 修正後時刻帯で該当ログなし

## 7. default-store 段階移行（Phase7 第一段）

コード変更:

- `functions/src/shared/runtime.ts`
  - `isSingleStorePerProjectMode()` を追加（既定 true）
  - 単一店舗モード時は `validateStoreTenantForProduction` の strict reject を回避
- `functions/src/domains/tournament_createTournament/services/generateRecurringTournamentsCore.ts`
  - 単一店舗モード時は `default-store` strict skip を回避

追加デプロイ関数:

- `createScheduledTournament`
- `createTournamentRecurrence`
- `generateRecurringTournaments`
- `enqueueTournamentTasks`
- `scheduled-job-enqueue-tournament-tasks-by-scheduler`
- `scheduled-job-generate-recurring-tournaments-by-scheduler`

結果:

- すべて更新成功（Deploy complete）
- `storeId is required in production. default-store is not allowed.` の再発なし（修正後時刻帯）
- `scheduled-job-enqueue-tournament-tasks-by-scheduler` manual 実行 `HTTP 204`

## 8. payroll task queue 実行確認（重点再検証）

実施:

- 各 queue に manual task を投入
  - `processPayrollNotifications`
  - `processStaffPayroll`
  - `finalizePayrollRun`

結果（Cloud Logging）:

- `processPayrollNotifications` `HTTP 204`
- `processStaffPayroll` `HTTP 204`
- `finalizePayrollRun` `HTTP 204`

## 9. us-central1 残存整理（Phase6）

実施:

- `us-central1` に残っていた `probe*` 関数 5 本を削除

削除対象:

- `probeAuthErrorShape`
- `probeFirestoreErrorShape`
- `probeCloudTasksErrorShape`
- `probeFirestoreErrorShapeInvalidArgument`
- `probeStorageErrorShape`

結果:

- Functions リージョン集計が `asia-northeast1` のみ（178 件）

## 10. 既知の残課題

- `firebase-functions` 依存が古い警告は残る
  - `package.json indicates an outdated version of firebase-functions`
- `default-store` 文字列依存の「完全排除」は未完了
  - 現在は「単一店舗モードで互換許容」の段階
  - 完全排除にはデータ移行と query の整合設計が必要

## 11. 追加実施（2026-04-07 後半）

### 11.1 `default-store` 後段対応（既存データ移行なし）

実施:

- `resolveStoreTenantForWrite` を追加し、単一店舗モードで
  - 欠損/legacy default を `projectId` ベースに正規化
- 適用箇所:
  - `createScheduledTournament`
  - `createTournamentRecurrence`
  - `generateRecurringTournamentsCore`
  - `enqueueTournamentTasksCore`
- 重複判定に legacy default 互換検索を追加（既存データ衝突回避）

確認:

- デプロイ後に scheduled-job-generate-recurring を有効 payload で手動投入
- 生成された `scheduledTournaments` の `storeId/tenantId` を Firestore REST API で確認
  - `GjWJTHM0xKFhITzfatUr`（legacy default の recurrence）由来でも
    新規 `scheduledTournaments` は `amuse-app-template` へ正規化されることを確認

### 11.2 `probe*` 本番除外

実施:

- `functions/src/index.ts` から `debug/errorShapeProbes` export を削除
- クラウド上に存在していた `probe*` 5 関数を削除

確認:

- `gcloud functions list` で `probe*` が 0 件であることを確認

### 11.3 scheduler task queue 実行確認（本番側）

実施:

- `schedulerSupervisor` を Cloud Scheduler から手動実行
- `scheduled-job-enqueue...` / `scheduled-job-generate...` queue に valid payload task を手動投入

確認:

- `schedulerSupervisor` が `HTTP 200` で完走
- `scheduled-job-enqueue...` が `HTTP 204`
- `scheduled-job-generate...` が `HTTP 204`
- 備考: `{}` payload の手動投入は `Request body is missing data` で `HTTP 400`（仕様どおり）
