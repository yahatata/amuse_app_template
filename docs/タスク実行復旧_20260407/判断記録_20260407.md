# 判断記録（2026-04-07）

## 確定済み

- D-01: `1（段階移行）`
  - 条件: 復旧後に `default-store` 依存排除フェーズを必ず実施
- D-02: `1（既存 SA へ統一）`
  - 統一先: `tasks-invoker@...`
- D-03: `1（ピンポイント削除）`
- D-04: `1（段階移行）`
  - 条件: 今回の修正作業内に必ず移行フェーズを含める
- D-05: `2（本番相当フル検証）`

## 補足

- 主要判断はすべて確定済み。
- 2026-04-07 の実装では、D-01 は「段階移行の第一段（strict reject 緩和）」まで完了。
- 完全排除（`default-store` 文字列依存の撤去）は、データ移行設計を伴う次段で実施する。
