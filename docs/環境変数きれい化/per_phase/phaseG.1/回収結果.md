# phaseG.1 リージョン固定化 回収結果

作成日: 2026-04-03

## 1. 実施内容

- 新規追加:
  - `lib/core/utils/functions_client.dart`
    - `FirebaseFunctions.instanceFor(region: 'asia-northeast1')` を共通化
- 一括回収:
  - `FirebaseFunctions.instance` を `FunctionsClient.instance` へ置換
  - 対象59ファイル（抽出78箇所）を全件回収
- 付帯調整:
  - 置換後に未使用化した `cloud_functions` import を、影響範囲内で削除

## 2. 網羅性チェック結果

- 事前抽出（phaseG.1開始時）
  - 78箇所 / 59ファイル
- 事後確認
  - `rg -n "FirebaseFunctions\\.instance" lib -g '*.dart'`
    - 残件は `lib/core/utils/functions_client.dart` の `instanceFor` のみ
  - 事前対象59ファイルに対する機械検証
    - `FunctionsClient.instance` 未反映ファイル: 0
    - `FirebaseFunctions.instance` 残存ファイル: 0

## 3. 検証結果

1. 静的解析
- 実行: `flutter analyze`
- 結果: `1024 issues found`
- 判断: 既知水準（以前と同一）であり、今回改修による増分警告は解消済み

2. テスト
- 実行: `flutter test`
- 結果: `All tests passed!`

## 4. 変更ファイル範囲

- アプリコード:
  - `lib/core/utils/functions_client.dart`
  - 既存のCallable呼び出しファイル 59件（詳細は `git diff --stat` 参照）
- ドキュメント:
  - `docs/環境変数きれい化/per_phase/phaseG.1/README.md`
  - `docs/環境変数きれい化/per_phase/phaseG.1/対象一覧_抽出結果.txt`
  - `docs/環境変数きれい化/per_phase/phaseG.1/対象一覧_ファイルパス.txt`
  - 本ファイル

## 5. 結論

- Dart 側の Functions 呼び出しは、抽出対象について `asia-northeast1` 固定クライアントへ統一完了。
- 既定リージョン（`us-central1`）へ誤到達する経路は、対象範囲内で除去済み。

## 6. 追加監査（LIFF / taskQueue 以外）

- phaseG.1 方針に基づく追加の横断監査結果は、以下に記録。
  - `追加調査_リージョン漏れ再点検.md`
- 要点:
  - コード上の `us-central1` 直書きは LIFF 修正後に解消済み。
  - 一方で、実環境には `us-central1` 残存資産（probe 系関数 5件、Scheduler job 1件）と、22関数の env ドリフト（`CONTROL_HOOK_URL` 等）が存在することを確認。

## 7. custom env ドリフト整理（probe* 除外）

- 実施記録:
  - `追加調査_envドリフト整理結果.md`
- 要点:
  - 本線関数 21 件の legacy custom env を除去し、`OK 21 / NG 0` で完了。
  - 全関数再走査の結果、custom env が残るのは `probe*` 5 関数のみ。
