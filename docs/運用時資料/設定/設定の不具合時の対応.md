# storeMeta/config 設定の不具合時の対応

本ドキュメントには、storeMeta/config に関連する**不具合発生時**の対応手順を**設定ごと**に記載する。  
※ ID は開発用の識別子のため、本ドキュメントには記載しない。

---

## 不具合のケース分類

| ケース | 内容 | 主な対応 |
|--------|------|----------|
| **A. 設定値の誤り** | 管理者が誤った値を設定した | Firestore の値を修正 |
| **B. 運用ミス** | テスト用設定を本番に適用した、設定変更を取り消したい | Firestore の値を修正 |
| **C. コードのバグ** | 特定の設定値で実装にバグがあり不具合が発生 | コード修正・デプロイ、またはロールバック |
| **D. コードと設定の不整合** | デプロイ済みコードと config のスキーマ・想定が合っていない | 設定 or コードのどちらかを修正 |
| **E. Firestore 障害** | Firestore が利用不可で config を読めない | 「取得失敗時の挙動設計」の範疇。本ドキュメントの対象外 |

---

## デフォルトの対応方針（A〜D）

A〜D の不具合については、**統一した処理を基本**とする（例外設定あり得る）。各設定の検証時に、本デフォルトとするか他の仕様とするかをユーザーが指示する。

### 進め方

1. **リトライを必ず行う**
2. **それでもダメな場合はエラーコードを出す**
   - 運用・保守側向け
   - 1 リポジトリから複数アプリ・Firebase プロジェクトをリリース・使用していることを意識した**扱いやすいエラーコード**を適切に出す
3. **一時対応**: 設定によっては、エラーで動かない場合の代替処理を用意する

### エラーコードの形式化

- per_id Task 4 進め方（`per_id_TASK4_PROCEDURE.md`）にエラーコードの形式・命名ルールを記載する
- 複数アプリ・複数 Firebase プロジェクト間で一貫したコード体系とする

---

## 設定ごとの記載

| 設定 | 想定パターン | 対応フロー | 一時対応・備考 |
|------|--------------|------------|----------------|
| **settlementAggregatorEnabled**（決済アグリゲータ有効化） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告「会計等の蓄積処理ができていないため、管理者にご連絡ください。」 | 本設定は boolean のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **dualWriteEnabled**（当日請求デュアルライト有効化） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は boolean のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **payrollStartDay / payrollEndDay**（給与締め日） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は 1〜31 の数値のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **enqueueSchedulerEnabled**（enqueue スケジューラ有効化） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は boolean のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **templateBusinessDateCheck**（テンプレート営業日重複チェック有効化） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は boolean のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **calcBufferMinutes**（営業日境界バッファ） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は数値のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **autoOpenClose**（自動開閉店） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は boolean と数値のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **businessStyles**（storeMeta/businessStyles） | A〜D | 1. リトライ 2. A,B: 設定未完了表示＋エラーコード 3. C,D: 処理スキップまたは設定画面誘導＋エラーコード | 正本 doc 未存在・不正時は fallback しない。initializeStoreConfigCallable で作成 |
| **linePlan**（LINE プラン種別） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は string のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **shift**（submissionStartDay / submissionEndDay / schedulingStartDay） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は 1〜28 の数値のため常にデフォルトで実行可能。スキップは発生しない想定 |
| **billing**（entranceFee / entranceFeeDescription / chargeEntranceFeeOnReentry / sideGameChipRate / paymentPolicy） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は数値・文字列・boolean・オブジェクトのため不正時はデフォルトで実行可能。スキップは発生しない想定 |
| **menuCategories**（メニューカテゴリ） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は文字列配列のため不正時はデフォルトで実行可能。スキップは発生しない想定 |
| **sideGameTypes**（サイドゲーム種別） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は文字列配列のため不正時はデフォルトで実行可能。スキップは発生しない想定 |
| **tournament**（トーナメント） | A〜D | 1. リトライ 2. A,B: デフォルトで実行＋エラーコード 3. C,D: デフォルトで実行可能な場合は実行＋エラーコード。それ以外は処理スキップ＋エラーコード＋画面警告 | 本設定は数値・文字列・オブジェクトのため不正時はデフォルトで実行可能。スキップは発生しない想定 |

---

## 関連ドキュメント

- `取得失敗時の挙動設計.md` … 取得失敗時（読めない・フィールド未存在）の挙動
- `config_migration/phase2/verification/per_id_TASK4_PROCEDURE.md` … エラーコード形式・進め方の詳細
