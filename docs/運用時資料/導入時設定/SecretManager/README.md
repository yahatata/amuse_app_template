# SecretManager（導入時設定）

このフォルダは、初回導入時に実施する Secret Manager 設定をまとめる。  
対象は phaseD で移行した以下 3 secret。

- `line-config`
- `task-endpoints`
- `business-secrets`

資料:

- `初回導入_SecretManager設定手順.md`

補足:

- 日常運用（値更新、確認、障害切り分け）は `docs/運用時資料/設定/SecretManager運用` を参照する。
