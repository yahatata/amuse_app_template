# 業務候補一覧（LINE ミニアプリ・退避用）

**`02.5_基盤業務境界整理.md` で「業務候補」としたものだけを抜き出した。**  
**今回のラウンド（基盤優先）では後回し。**

---

## 業務候補一覧

| 候補名 | Step2 の評価結果 | 業務候補とした理由 | 今回後回しにする理由 | 再開時に最初に見る論点 | 関連領域 / 関連ファイル（例） |
|--------|------------------|-------------------|----------------------|------------------------|------------------------------|
| 業務固有の HTML 組み立て（一覧行・カード・カレンダーセル等） | 保留 | **ドメイン別マークアップ**・イベント付与が中心で、プレースホルダ型より業務差分が濃い | **基盤（フォーマット・操作系 CSS・初回ローディング）を先**にし、テンプレ境界を固めてから | プレースホルダ行との切り分け、`innerHTML` 混在の洗い出し | `public/staff/index.html`, `public/user/index.html`（各 `innerHTML` 生成ブロック） |
| （変更なし）カートモーダルと注文確認フロー（確認→一括注文） | 保留 | **user 専用の注文フロー**一体。staff に同型なし（`01.5`） | 基盤・表示ポリシー（`confirm` 境界）の整理後の方が改修再利用しやすい | モーダル＋`confirm` の三経路整理、一括注文 API 後の UI | `public/user/index.html`, `public/css/user.css` |
| （変更なし）動的カレンダー・シフト・勤怠の大規模 HTML 生成 | 保留 | staff 集中の**大規模業務 HTML**（補足枠）。分割前提 | スコープが広く、**基盤 CSS / 業務 HTML 行**との順序を切る必要がある | 業務 HTML 行との重複、削除・シフト申請フローとの対応 | `public/staff/index.html`（シフト・カレンダー・勤怠の生成部） |

---

## 集計

- **業務候補**: **3 件**（すべて Step2 は **保留**）

---

## 参照

- `02.5_基盤業務境界整理.md`
- `02_共通化価値評価.md`
