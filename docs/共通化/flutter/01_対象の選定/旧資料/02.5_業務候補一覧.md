# 業務候補一覧（Flutter・退避用）

**`02.5_基盤業務境界整理.md` で「業務候補」と再分類したものだけを抜き出した。**  
**今回のラウンド（基盤優先）では後回し。** 再開時にそのまま手掛かりになるよう、論点と関連領域を残す。

**入力**: `02_共通化価値評価.md` ＋ `02.5_基盤業務境界整理.md`  
**注意**: Step2 の **残す / 保留 は再採点していない**。別軸の「今回の進め方」だけ業務側に寄せた。

---

## 業務候補一覧

| 候補名 | Step2 の評価結果 | 業務候補とした理由 | 今回後回しにする理由 | 再開時に最初に見る論点 | 関連領域 / 関連ファイル（例） |
|--------|------------------|-------------------|----------------------|------------------------|------------------------------|
| 会計フロー：支払・方法・金額・カテゴリ等の入力・選択ダイアログ | 保留 | 会計ドメインの入力・バリデーション・例外が厚く、**業務ルールと一体** | **基盤候補（ダイアログの枠・ローディング・formatter）を先に固める**方が依存が少なく、会計はスコープが広い | カテゴリ別支払い・残高・分割の境界、`Accounting` 周辺のダイアログ分割の切り口 | `lib/Accounting/`（例: `accountingPage.dart`, `paymentMethodDialog.dart`, `CategoryPaymentMethodDialog` 等） |
| 会計フロー：調整・返金・再開・事後手続きダイアログ | 保留 | 事後手続きは**権限・会計状態・文言**が業務固有 | 同上。基盤と表示ポリシー（境界）が一段固まってからの方がレビューしやすい | 上記入力系とのペア設計、取消・返金の confirm トーン | `lib/Accounting/` |
| 卓上ポップアップ：金銭・アドオン・チップ購入・入金系 | 保留 | **卓・金銭**の組み合わせで例外が多い | 基盤（ローディング・二重防止）適用後に揃える方が安全 | 金額入力・入金確認・卓種別差 | `lib/user_actions/`（例: `side_game_tip_deposit_popup.dart`, `current_accounting_popup.dart`） |
| 卓上ポップアップ：ゲーム状態・バスト・退出等 | 保留 | **ゲーム状態・卓オペ**に強く依存 | 同上 | 状態遷移・説明文の業務差、卓まわりルール | `lib/user_actions/`, `lib/Home/table_home_page.dart` 等 |
| （変更なし）トーナメント卓操作ダイアログ群 | 残す | Step2 では価値高としたが、**トーナメント運用の文脈**が中心で業務差分が濃い | **今回ラウンドの方針は基盤優先**のため意図的に業務へ再配置（Step2 と矛盾しない） | 運用ダイアログの束の共通部 vs 卓固有、設定系との分離 | `lib/tournament/`, 卓・トーナメント関連 `dialogs/` |
| （変更なし）シフト・勤怠系確認ダイアログ | 残す | **勤怠・シフトオペ**の確認トーン・遷移が業務固有 | 基盤優先。勤怠は Flutter 側でもファイル再編成が進みうるため別タイミングが現実的 | 手動打刻・承認・一覧からの遷移との整合 | `lib/AttendanceManagement/`, `lib/StaffDate/` |
| （変更なし）オーダー編集・注文カード操作 | 残す | **注文フロー固有**の UI。会計との接続あり | 基盤（単純確認・formatter 等）と会計スコープを分けてから | 会計候補との二重実装防止、カード操作の状態管理 | 注文・メニュー周辺（プロジェクト内のオーダー UI） |

---

## 集計

- **業務候補**: **7 件**  
- **Step2 内訳**: 残す 3 ＋ 保留 4  

---

## 参照

- `02.5_基盤業務境界整理.md`
- `02_共通化価値評価.md`
