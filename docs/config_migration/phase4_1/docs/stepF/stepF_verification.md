# 4.1-F: UI 改修、seedAttendancesDemo、monthlyPayrollTrigger — 確認結果

**CHANGESPEC**: [phase_F/CHANGESPEC.md](../phase_F/CHANGESPEC.md)  
**実機確認観点**: [phase_F/REAL_DEVICE_VERIFICATION.md](../phase_F/REAL_DEVICE_VERIFICATION.md)（UI と Functions の切り分け、パターン・期待結果表）  
**本 step**: 4.1-F。Flow2 セクション 5「完了条件」・セクション 6「確認方針」を参照すること。**4.1-F で実機確認を一括実施**（C〜E2 はテスト + エミュレータで確認済み）。

---

## 0. 確認時の共通ルール（全 step で実施）

**目的**: 実装・確認の漏れを防ぐため、以下のタイミングで所定の確認を行う。

| タイミング | 何を | 何のために | 参照先 |
|------------|------|------------|--------|
| **確認開始前** | 本 step の完了条件を確認する | 何をもって完了とするかを把握するため | Flow2 セクション 5「完了条件」の該当 step の行 |
| **確認中** | 完了条件の各項目を満たしているか確認する | 実装が To-Be 仕様を満たしているか検証するため | 上記完了条件をチェックリスト化したもの |
| **確認中** | CHANGESPEC のチェックリスト全項目を実施する | 実装順序・検証ポイントの漏れを防ぐため | 本 step の CHANGESPEC.md セクション 8 |
| **4.1-F の場合** | 実機確認を実施する | C〜E2 の実装を含め、UI・休憩操作・給与計算・LINE 表示等を一括確認 | Flow2 セクション 6。CHANGESPEC の検証ポイントに従い実施 |
| **確認完了時** | 結果を本 VERIFICATION に記録する | 実施証跡を残し、次 step への引継ぎを明確にするため | 本ファイル |
| **確認完了時** | 完了サマリファイルを作成し、本 step フォルダ内に格納する | 依存先として本 step を参照する後続 step が、漏れなく実装内容を確認できるようにするため | 本 step フォルダ（`docs/config_migration/phase4_1/docs/stepF/`）内に `stepF_completion_summary.md` を作成 |

**※ 完了サマリに記載すべき内容**: 変更した点だけでなく、**変更後どうなったか**（どのファイルのどの部分がどのような実装になっているか）を記載する。後続 step が依存先として本 step を参照する際、サマリだけで実装の全体像が把握できることが望ましい。

**※ step ごとの違い**: 完了条件・実機確認要否・CHANGESPEC のチェックリストは step により異なる。**Flow2 セクション 5 で本 step の完了条件を必ず確認し、全項目をチェックすること。**

---

## 実施日・実施者

| 項目 | 内容 |
|------|------|
| 実施日 | YYYY-MM-DD |
| 実施者 | |

---

## 完了条件チェック

| 完了条件（Flow2 セクション 5） | 結果 | 備考 |
|-------------------------------|------|------|
| | ✓ / ✗ | |
| | | |

---

## CHANGESPEC チェックリスト

| 項目 | 結果 |
|------|------|
| | ✓ / ✗ |
| | |

---

## 実機確認結果

| 確認項目 | 結果 | 事象 |
|----------|------|------|
| | 成功 / 失敗 | |
| | | |

---

## 残課題・次段階への引継ぎ

<!-- あれば記載 -->

（なし / 記載する）

---

## 完了サマリ

**作成先**: 本 step フォルダ内（`docs/config_migration/phase4_1/docs/stepF/stepF_completion_summary.md`）

**記載内容**: 変更した点に加え、**変更後どうなったか**（どのファイルのどの部分がどのような実装になっているか）を記載する。後続 step が依存先として本 step を参照する際、サマリだけで実装の全体像が把握できるようにする。

| 対象 | 変更後どうなったか |
|------|-------------------|
| | |
| | |
