# A-3 スタッフ退職処理 実施結果

> 実施日: 2026-07-09

## 実装した内容

Phase 0〜8 を `changeSpec.md` に従い一括実装。

- `staffs.status`（`active` / `retired`）基盤
- `retireStaff` / `reactivateStaffAccount` Callable
- 本人操作 Callable 14件への active guard
- LINE リッチメニュー（`unlinkRichMenu`、active のみ link）
- スタッフ LIFF 再登録画面
- 管理者 Flutter UI（退職手続き導線・一覧フィルタ）
- スタッフ取得条件（出勤一覧・シフト割当・勤怠作成候補）
- Functions 単体テスト追加

## 作成したファイル

### Functions

- `functions/src/domains/staff/types/staffStatus.ts`
- `functions/src/domains/staff/helpers/staffStatus.ts`
- `functions/src/domains/staff/helpers/clearRetiredStaffPii.ts`
- `functions/src/domains/staff/helpers/checkFutureStaffSchedule.ts`
- `functions/src/domains/staff/callables/retireStaff.ts`
- `functions/src/domains/staff/callables/reactivateStaffAccount.ts`
- `functions/scripts/migrateStaffStatusActive.ts`
- `functions/__tests__/staff/*.spec.ts`（8ファイル）

### ドキュメント

- 本ファイル

## 修正したファイル

### Functions

- `functions/src/domains/staff/index.ts`
- `functions/src/domains/staff/callables/createStaffAccount.ts`
- `functions/src/domains/staff/callables/createStaffByApp.ts`
- `functions/src/domains/webhook/services/lineRichMenu.ts`
- `functions/src/domains/webhook/callables/ensureStaffRichMenu.ts`
- `functions/src/domains/webhook/callables/lineWebhook.ts`
- `functions/src/domains/user/callables/generateQRCode.ts`
- `functions/src/domains/attendance/callables/clockIn.ts`
- `functions/src/domains/attendance/callables/clockOut.ts`
- `functions/src/domains/attendance/callables/startBreak.ts`
- `functions/src/domains/attendance/callables/endBreak.ts`
- `functions/src/domains/attendance/callables/createManualClockInRecord.ts`
- `functions/src/domains/attendance/callables/getStaffAttendance.ts`
- `functions/src/domains/attendance/callables/checkExistingCorrectionRequest.ts`
- `functions/src/domains/attendance/callables/createAttendanceCorrectionRequest.ts`
- `functions/src/domains/attendance/callables/getStaffListForAttendance.ts`
- `functions/src/domains/staff/callables/createMultipleShifts.ts`
- `functions/src/domains/staff/callables/updateShiftRequest.ts`
- `functions/src/domains/staff/callables/confirmShiftRequest.ts`
- `functions/src/domains/staff/callables/getShifts.ts`
- `functions/src/shared/logging/serviceByFunctionEntry.ts`
- `functions/package.json`（`migrate:staff-status` 追加）

### Flutter

- `lib/Home/staffDetailPage.dart`
- `lib/Home/staffRetirementPage.dart`（2026-07-09 追記: 退職手続き専用ページ）
- `lib/Home/staffListPage.dart`
- `lib/StaffDate/shiftDateDialog.dart`
- `lib/AttendanceManagement/admin_attendance_editAndCreate_page.dart`

### LIFF

- `public/staff/index.html`

### 進捗管理

- `docs/残タスク_0623/カテゴリA_システム構築/進捗管理.md`
- `docs/残タスク_0623/カテゴリA_システム構築/A-3_スタッフ退職処理/changeSpec.md`（§24 実装完了追記）

## 実装しなかった内容（仕様どおり）

- `retirement_scheduled` / 退職予定日 scheduler
- Firebase Auth `disabled`
- `getFirebaseCustomToken` での retired ブロック
- 店舗端末からの退職取消・復旧・再雇用
- 再登録の店舗承認待ち UI
- 既存 QR の即時無効化
- 給与未払いによる退職ブロック
- `hourlyWage` / `bankInfo` の削除・マスク
- `retiredByDeviceId` 等の監査フィールド
- shifts rules / IDOR / `getShifts` auth 全面改修
- `createStaffByApp` 補助経路の再登録フロー整備

## テスト結果

### Functions（A-3 関連）

```bash
cd functions && npm test -- __tests__/staff --runInBand
# 8 suites, 23 tests passed
```

### Functions（全体）

`npm test` は全スイート実行のため時間がかかる。A-3 追加テストは上記で通過確認済み。

### Flutter / LIFF

| ID | 内容 | 結果 |
|----|------|------|
| T-24 | 詳細から退職手続き（確認 → 専用ページ → 実行） | **実機確認済み** |
| T-25 | 退職済みバッジ | **実機確認済み** |
| T-26 | 一覧に退職ボタンなし | コード確認済み |
| LIFF | retired → 再登録画面 | **実機確認済み** |
| LIFF | 再登録 → active 復帰 | **実機確認済み** |

## lint / build / analyze 結果

| コマンド | 結果 |
|----------|------|
| `npm run build` | 成功 |
| `npm run lint` | 成功 |
| `npm test -- __tests__/staff` | 成功（23件） |
| `flutter analyze`（変更ファイル） | info/warning のみ（既存由来含む）。error なし |

## migration スクリプトの扱い

- スクリプト: `functions/scripts/migrateStaffStatusActive.ts`
- 実行: `cd functions && npm run migrate:staff-status`
- **本番実行済み**（2026-07-09。`status` 未設定 35 件に `active` 付与）
- ランタイムは `normalizeStaffStatus` で未設定を `active` 扱い（lazy write なし）

## 本番デプロイ前に必要な作業

1. ~~`migrateStaffStatusActive.ts` を本番 Firestore で実行~~ **完了（2026-07-09）**
2. ~~Cloud Functions デプロイ~~ **完了（2026-07-09）**
3. ~~`public/staff/index.html` の Hosting デプロイ~~ **完了（2026-07-09）**
4. ~~Flutter 店舗端末アプリのビルド・配布~~ **完了（2026-07-09）**
5. ~~LIFF / 管理者 UI の手動確認~~ **完了（2026-07-09）**
6. `shiftRequests` 複合インデックスが必要な場合はエラーログで確認・作成（未発生）

## 残課題・要確認（スコープ外・後続）

- `createStaffByApp` 経路スタッフ（uid ≠ LINE）の再登録はスコープ外（C-01）
- シフト履歴画面（`shiftHomePage` 等）への退職済みバッジは未対応
- 退職者 PII 保持期間・削除方針は後続検討事項

## 手動テストチェックリスト（LIFF）

- [x] 退職済みスタッフで LIFF 起動 → 再登録画面表示
- [x] 合意チェックなしでは送信不可
- [x] `reactivateStaffAccount` 成功後 → 通常ホーム表示
- [x] 退職済み中は QR / シフト / 勤怠画面へ遷移不可（二重防御）

---

## 本番前確認（2026-07-09）

| 区分 | 結果 |
|------|------|
| changeSpec 準拠 | コードレビュー上、Phase 0〜8・スコープ外除外は仕様どおり |
| A-3 単体テスト（Emulator あり） | 8 suites / 23 tests 成功 |
| Functions lint / build | 成功 |
| Flutter test | 399 tests 成功 |
| 全体 npm test（Emulator あり） | 177 passed / 27 failed（A-3 以外の既存失敗。例: `reporting/*.spec.ts` TS 型エラー） |
| LIFF / 管理者 UI | **実機確認済み**（2026-07-09） |
| migration | **実行済み**（2026-07-09。total=35, updated=35, skipped=0） |
| functionEntry 対応表 | `retireStaff` / `reactivateStaffAccount` 追記済み |

## 本番デプロイ（2026-07-09）

| 区分 | 結果 |
|------|------|
| コマンド | `firebase deploy --only functions,hosting`（project: `amuse-app-template`） |
| 新規 CF | `retireStaff`, `reactivateStaffAccount` 作成成功 |
| 更新 CF（A-3 関連） | `clockIn`, `createStaffAccount`, `generateQRCode`, `getStaffListForAttendance`, `lineWebhook`, `ensureStaffRichMenu` 他 |
| Hosting | `public/staff/index.html` 含む 1 ファイル更新 → https://amuse-app-template.web.app |
| migration | **実行済み**（2026-07-09。total=35, updated=35, skipped=0） |
| Flutter 店舗端末 | **配布・実機確認済み**（2026-07-09） |

---

## 管理者UI 退職手続き導線変更（2026-07-09）

### 実装した内容

- 管理者UIの退職導線を専用ページ方式へ変更
- UI文言を「退職処理」から「退職手続き」に変更
- スタッフ詳細ページでは `retireStaff` を直接呼ばず、確認後に専用ページへ遷移する

### 作成したファイル

- `lib/Home/staffRetirementPage.dart`

### 修正したファイル

- `lib/Home/staffDetailPage.dart`
- `docs/残タスク_0623/カテゴリA_システム構築/A-3_スタッフ退職処理/changeSpec.md`（§13-1, §25）
- 本ファイル

### 変更後の画面遷移

```text
StaffDetailPage（AppBar「退職手続き」）
  → 確認ダイアログ
  → StaffRetirementPage（退職日・理由入力・注意事項・確認チェック）
  → retireStaff 実行
  → pop(true) → StaffDetailPage 再取得 → 退職済み表示
```

---

## staff status migration 実行結果（2026-07-09）

| 項目 | 内容 |
|------|------|
| 実行日 | 2026-07-09 |
| 対象 project | `amuse-app-template` |
| 実行コマンド | `cd functions && npm run migrate:staff-status` |
| 実行結果 | **成功** |
| total | 35 |
| updated | 35 |
| skipped | 0 |

全 35 件の `staffs` doc が `status` 未設定だったため、いずれも `status: "active"` に更新された。`active` / `retired` の既存 doc は存在せず skip 0 件。

### 補足（エージェント環境からの先行試行）

2026-07-09 にエージェント環境から実行を試みたが、認証トークン期限切れ（`invalid_grant`）のため未実行。ローカル端末で認証更新後、上記のとおり本実行完了。

### 残課題

- ~~Flutter 店舗端末アプリのビルド・配布（退職手続き導線変更反映）~~ **完了（2026-07-09）**
- 実行後の `status` 未設定 doc 0 件確認（任意・未実施）

---

## UI追加修正（2026-07-09 本番確認フィードバック）

### 実装した内容

- 退職手続き専用ページで、最終実行前に確認ダイアログを追加
- 再登録画面（LIFF）の合意チェック説明を明確化
- 再登録画面で未チェック・未入力時に不備項目を表示（押下時バリデーション）
- `fullNameKana` スペースなし初期値を姓欄へ全量投入しないよう修正
- 履歴・給与系表示で退職済みマークを表示

### 作成したファイル

- `lib/Home/staff_retired_ui_helpers.dart`

### 修正したファイル

- `lib/Home/staffRetirementPage.dart`
- `lib/payroll/widgets/staff_card.dart`
- `lib/payroll/widgets/result_tab.dart`
- `lib/payroll/widgets/calc_tab.dart`
- `lib/payroll/widgets/candidate_section.dart`
- `lib/payroll/widgets/payment_management.dart`
- `lib/payroll/widgets/staff_detail_page.dart`
- `lib/payroll/widgets/error_view.dart`
- `lib/AttendanceManagement/all_staff_attendance_page_from_adminHome.dart`
- `lib/AttendanceManagement/staff_attendance_detail_page_from_allStaffAttendance.dart`
- `lib/AttendanceManagement/admin_attendance_list_page.dart`
- `public/staff/index.html`
- `changeSpec.md`（§26）
- 本ファイル

---

## 本番確認後の追加修正（2026-07-09 後半）

### 実装した内容

- LIFF 再登録画面: 合意説明の重複削除（簡潔化）
- LIFF 再登録: `fullNameKana` を新規登録と同様スペースなし結合に修正（CF バリデーション整合）
- 退職手続きページ: 確認チェック未入力時は実行ボタン disabled に復帰

### 修正したファイル

- `public/staff/index.html`
- `lib/Home/staffRetirementPage.dart`

### Hosting 追加デプロイ

- 2026-07-09 `firebase deploy --only hosting`（`fullNameKana` 結合修正・UI 簡潔化）

---

## タスククローズ（2026-07-09）

| 項目 | 内容 |
|------|------|
| ステータス | **完了** |
| 確認済み | migration、CF、Hosting、Flutter 店舗端末、LIFF 再登録、管理者退職手続き |
| 進捗管理 | `進捗管理.md` A-3 を「完了」に更新 |
| スコープ外（後続） | `createStaffByApp` 再登録、`shiftHomePage` 退職済みバッジ、退職者 PII 保持期間 |
