# Step 8: 既存データ・オンライン移行

## 実施方針：スキップ

**本ステップは実装を行わない。**

- **理由**：リリース前アプリのため、既存の scheduledTournament は運用側で削除する。オンライン移行が不要
- **前提**：本番投入前に既存データを削除し、Step 2 以降のドキュメントのみが存在する状態で運用する
- **詳細**：`changeSpec.md` を参照

## 他ステップへの影響

- Step 4 の `schedulePlanVersion ?? 0` フォールバックは防御的に維持（変更なし）
- Step 6 の controlHook も同上
- Step 9（Firestore ルール・インデックス）は Step 8 に依存しないため、従来どおり実施する
