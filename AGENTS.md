# AGENTS.md

この repository で、資料・台帳・sheet・md・説明資料・管理表・更新ルール・命名規則・ID 設計・資料間参照・Excel 設計・ChatGPT for Excel 用プロンプト設計・rules / memory 自体の作成や更新に関わる作業を行う場合、まず正本ルールを読むこと。

## 最初に読む正本

- `docs/残タスク_0623/共通資料/01_資料ライフサイクル・更新ルール.md`
- `docs/残タスク_0623/共通資料/02_用語・ステータス定義.md`

## 00_資料台帳に関する追加ルール

- `00_資料台帳`、`00_資料台帳_説明`、`共通資料`、`資料台帳`、`資料一覧`、`資料目録` などの話題を扱う時は、出力前に必ず次の 3 資料を読むこと
- `docs/残タスク_0623/共通資料/00_資料台帳_説明.md`
- `docs/残タスク_0623/共通資料/02_用語・ステータス定義.md`
- `docs/残タスク_0623/共通資料/01_資料ライフサイクル・更新ルール.md`
- 読む順番は `00_資料台帳_説明` -> `02_用語・ステータス定義` -> `01_資料ライフサイクル・更新ルール`
- `00` に関する提案、設計、更新、Excel プロンプト作成、rules / memory 補助文面の作成では、この順番を崩さない

## このルールを適用する作業

- 新規資料を作るか検討する時
- 資料の構成、章立て、列、カラム、フォーマットを決める時
- md 資料や sheet 台帳を新規作成する時
- 既存資料を更新、統合、分割、廃止、移設する時
- 正本と参照資料の関係を考える時
- 資料間の参照関係、命名規則、ID ルール、更新ルールを決める時
- Excel / spreadsheet / 台帳 / 管理シートを設計する時
- ChatGPT for Excel に作成・更新・抽出プロンプトを作る時
- Cursor、Codex、ChatGPT の rules や memory 自体を作る時
- `docs/残タスク_0623` 配下のカテゴリ B 以降と共通資料に関する資料を作成、更新、検討する時
- 将来 `00_資料台帳` に登録される資料群を扱う時

このルールを適用すべきか判断しづらい場合は、出力を始める前に必ずユーザーへ確認すること。

## 最重要原則

- 1つの事実は 1つの正本にだけ持つ
- 同じ事実を複数資料に直書きしない
- 一覧管理は台帳 `sheet`、説明は `md` を原則とする
- `sheet` を正本にする資料は、必ず対応する説明用 `md` を持つ
- 説明用 `md` は補助であり、基本的には `sheet` 単体でも内容が理解できるものを目指す
- 1ファイル 1目的、1資料 1正本、1行 1件、1列 1意味、1セル 1値を原則とする
- 資料完成と実体反映は分けて扱う
- 資料の削除よりアーカイブを優先する

## 説明用 md の必須項目

すべての `md` 資料の先頭には最低限以下を持つこと。

- 概要
- 主な目的
- 正本区分
- 対象
- 更新区分
- 参照元
- 参照先

`sheet` 正本の説明用 `md` には、上記に加えて以下も必須。

- 対応する workbook 名
- 主シート名
- 1行の単位
- 主な列
- 更新時の注意点

## 構造ルール

- 色は意味の補助としてのみ使い、意味は必ず列値でも持つ
- 結合セルは禁止
- 空行・空列を構造として使わない
- 行削除より状態変更を優先
- 色、装飾、セル位置だけに意味を持たせない
- 意味のある状態、重要度、区分は必ず文字列または列として保持する

## 標準記法

- 日付は `YYYY-MM-DD`
- 時刻は `HH:mm`
- 日時は `YYYY-MM-DD HH:mm`
- タイムゾーンが意味を持つ場合は `JST` または `UTC` を明記
- `sheet` 上の真偽値は `TRUE/FALSE`
- 空欄を意味値として使わない
- 不明は `未確認`
- 将来確定予定は `TBD`
- 該当しない場合は `該当なし`
- 廃止済みは `廃止`
- repo 内資料参照は原則 repo ルートからの相対パス
- 他資料から参照される対象には原則固有 ID を持たせる
- 他資料から参照する時は原則 `ID + 短い名称`

## ID 接頭辞

- 文書: `DOC-xxx`
- function: `FN-xxx`
- config: `CFG-xxx`
- flow: `FLOW-xxx`
- test: `TEST-xxx`
- defect: `DEF-xxx`
- 成果物: `ART-xxx`

一度採番した ID は再利用しない。名称変更があっても ID は維持する。

## 参照と更新のルール

- 正本更新時は、参照資料を網羅的に洗い出して更新要否を確認する
- 正本の更新が完了するまで、参照資料のみを先に更新しない
- 将来的に資料が移設される可能性を前提とし、現在のフォルダ構造に強く依存した書き方を避ける
- 参照は可能な限り `資料ID + repo相対パス` の組み合わせで表現する
- フォルダの近さや同階層であることを前提に意味を持たせない

## 資料作成・更新のルール

- 新しい資料は、作成前に資料台帳へ登録する前提で考える
- 目的が曖昧な資料は作成しない
- カラムや章立ては「何を判断するために必要か」で決める
- 便利そうという理由だけで列を増やさない
- 共通項目と資料固有項目を分ける
- 未確認事項は空欄ではなく `未確認` / `判断待ち` / `保留` などで残す
- 推測は推測と明記する
- 正本にない事実を断定しない
- 暫定記載には暫定と分かる表現を付ける
- 開発時専用情報は原則として削除ではなく `非表示` / `アーカイブ` / `別シート退避` で扱う
- 仕様変更、画面変更、設定変更、権限変更、フロー変更、運用変更のいずれかが発生した場合は関連資料の更新要否を確認する
- 意味変更を伴う更新と体裁だけの更新は区別する

## 更新区分

- `継続`
- `変更時`
- `節目`
- `単発`

## sheet / Excel ルール

- 原則 1資料 = 1 workbook
- 原則 1資料 = 1主シート
- 主シートは Excel のテーブル機能を使う
- 先頭行は固定
- フィルタは有効化
- 状態、重要度、区分、真偽値、更新区分などは入力規則によるリスト化を積極的に使う
- 条件付き書式は一般的な範囲で積極的に利用してよい
- ただし色だけに意味を持たせない
- 数式列がある場合は説明用 `md` にどの列が数式列か記載する
- ID 列は途中で意味変更しない
- 複雑な `sheet` は、生成時に用いた代表プロンプトの要約を説明用 `md` に残す

## ChatGPT for Excel ルール

ChatGPT for Excel に依頼する時は、少なくとも以下を指定すること。

- workbook 名
- 主シート名
- 目的
- 1行 1件の単位
- 必須列と列順
- 各列の意味
- 各列の入力値の型
- 入力規則にする列と候補値
- 数式列の有無
- フィルタ、固定、テーブル化の要否
- 条件付き書式の要否
- 作ってはいけないもの
- 出力形式

抽出依頼の時は必ず以下を含めること。

- 対象 workbook 名
- 対象シート名
- 何を知りたいか
- 必要な列だけ
- 必要な行だけ
- 抽出条件
- 並び順
- 出力形式
- 件数
- 抽出日時

運用上の必須ルール:

- ChatGPT for Excel への抽出依頼は必ず出力形式を指定する
- 出力形式はその時点で最も AI が理解しやすいものを選ぶ
- 抽出結果には件数・抽出日時・抽出条件を必ず含める
- 構造変更とデータ更新を同時依頼しない
- 大きな更新は構造変更と内容更新を分けて依頼する

## AI が sheet を理解する時のルール

- まず説明用 `md` を読む
- その後、必要な情報だけを ChatGPT for Excel に抽出させる
- 抽出結果は可能な限り機械可読な形式で受け取る
- 必要な列だけを抽出し、全件出力は避ける
- 意味解釈が必要な時は、行データと説明用 `md` をセットで読む

## 導入開始後の更新ルール

- 運用中の資料は正本として維持する
- 運用開始後の仕様変更、設定変更、障害対応、運用改善が発生した場合は関連資料の更新要否を確認する
- 月次またはリリース単位で資料未反映がないか点検する
- 単発資料は運用中に継続更新しない
- 節目資料は次の節目まで履歴として保持する
- 継続資料でも更新のたびに構造を変えない

## 廃止・置換ルール

- 廃止理由を残す
- 置換先を明記する
- 廃止した資料は削除せず `廃止済` または `アーカイブ` にする
- 旧資料には「この資料は正本ではない」と明記する

## rules / memory 反映ルール

- `docs` 側の正本ルールを基準にする
- `docs` 側のルールを更新したら rules / memory も網羅的に更新する
- rules や memory だけを先に変更しない
- 変更順は `docs 正本更新 -> rules 更新 -> memory 更新`
- ルール適用が必要か判断しづらい場合は、出力前に必ずユーザーに確認する

## 追加前提

- 今後 `00_資料台帳` の内容が共有された後は、`00` に登録された資料の作成・更新・検討で必ずこのルールを適用する
- その段階で必要なら、この `AGENTS.md` と関連 guidance を再整理・追加更新する
